fastapi>=0.100.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
uvicorn>=0.21.0
# Test tooling lives in [project.optional-dependencies].dev (pytest, pytest-cov, pytest-asyncio, httpx).
# Example client dependency lives in [project.optional-dependencies].examples (requests).
starlette>=0.27.0
