"""
Setup script for the GEO-INFER-API package.

All metadata lives in pyproject.toml; this shim only exists for legacy
tooling that still invokes setup.py directly.
"""
from setuptools import setup

setup()
