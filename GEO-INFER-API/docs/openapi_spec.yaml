# Regenerated from the live application: ``create_app().openapi()`` in
# ``src/geo_infer_api/app.py`` (scope item GS19-09). Paths, tags, and
# component schemas are machine-derived from the implemented routes; do
# not hand-edit them — regenerate instead. The ``info``/``servers`` blocks
# are the only hand-maintained part.
openapi: 3.1.0
info:
  title: GEO-INFER-API
  description: 'Standardized Interfaces for Geospatial Interoperability


    Regenerated from the implemented FastAPI application (``create_app().openapi()``). Health endpoints
    are mounted at the application root; every other endpoint is mounted under the ``/api/v1`` prefix
    (see ``src/geo_infer_api/app.py``). The surface comprises a GeoJSON feature collection API, a registered
    processing-algorithm registry, and polygon geometry operations. There is no authentication and no
    OGC Processes/jobs surface; those endpoints do not exist in this application.'
  version: 0.3.0
  contact:
    name: GEO-INFER Development Team
    url: https://github.com/ActiveInferenceInstitute/GEO_INFER
  license:
    name: CC BY-NC-SA 4.0
    url: https://creativecommons.org/licenses/by-nc-sa/4.0/
servers:
- url: http://localhost:8000
  description: Local development server
paths:
  /health:
    get:
      tags:
      - Health
      summary: Health check
      description: 'Simple health check endpoint.


        Returns basic status information about the running API instance.'
      operationId: health_check_health_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Health Check Health Get
  /health/detailed:
    get:
      tags:
      - Health
      summary: Detailed health check
      description: 'Detailed health check with real component status information.


        Returns live values for memory usage, uptime, and feature flags.'
      operationId: detailed_health_check_health_detailed_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Detailed Health Check Health Detailed Get
  /api/v1/collections:
    get:
      tags:
      - GeoJSON
      summary: List available feature collections
      description: 'List available feature collections.


        Follows the OGC API Features standard for listing collections.'
      operationId: list_collections_api_v1_collections_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response List Collections Api V1 Collections Get
  /api/v1/collections/polygons:
    get:
      tags:
      - GeoJSON
      summary: Get polygon collection metadata
      description: 'Get metadata about the polygon collection.


        Follows the OGC API Features standard for describing a collection.'
      operationId: get_polygon_collection_api_v1_collections_polygons_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Get Polygon Collection Api V1 Collections Polygons Get
  /api/v1/collections/polygons/items:
    get:
      tags:
      - GeoJSON
      summary: List polygon features
      description: 'List polygon features with optional bounding-box filtering.


        Follows the OGC API Features standard for listing items in a collection.'
      operationId: list_polygon_features_api_v1_collections_polygons_items_get
      parameters:
      - name: bbox
        in: query
        required: false
        schema:
          anyOf:
          - type: string
          - type: 'null'
          description: Bounding box (minLon,minLat,maxLon,maxLat)
          title: Bbox
        description: Bounding box (minLon,minLat,maxLon,maxLat)
      - name: limit
        in: query
        required: false
        schema:
          type: integer
          maximum: 1000
          minimum: 1
          description: Maximum number of features to return
          default: 10
          title: Limit
        description: Maximum number of features to return
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeatureCollection'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    post:
      tags:
      - GeoJSON
      summary: Create a new polygon feature
      description: 'Create a new polygon feature.


        The in-memory store is capped at ``settings.polygon_store_max_size``

        entries; creation beyond the cap is rejected with HTTP 409 rather than

        letting the store grow without bound.'
      operationId: create_polygon_feature_endpoint_api_v1_collections_polygons_items_post
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolygonFeature'
      responses:
        '201':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeature'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/collections/polygons/items/{feature_id}:
    get:
      tags:
      - GeoJSON
      summary: Get a specific polygon feature
      description: Get a specific polygon feature by ID.
      operationId: get_polygon_feature_api_v1_collections_polygons_items__feature_id__get
      parameters:
      - name: feature_id
        in: path
        required: true
        schema:
          type: string
          description: ID of the feature to retrieve
          title: Feature Id
        description: ID of the feature to retrieve
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeature'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    put:
      tags:
      - GeoJSON
      summary: Update a polygon feature
      description: Update an existing polygon feature.
      operationId: update_polygon_feature_api_v1_collections_polygons_items__feature_id__put
      parameters:
      - name: feature_id
        in: path
        required: true
        schema:
          type: string
          description: ID of the feature to update
          title: Feature Id
        description: ID of the feature to update
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolygonFeature'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeature'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
    delete:
      tags:
      - GeoJSON
      summary: Delete a polygon feature
      description: Delete a polygon feature.
      operationId: delete_polygon_feature_api_v1_collections_polygons_items__feature_id__delete
      parameters:
      - name: feature_id
        in: path
        required: true
        schema:
          type: string
          description: ID of the feature to delete
          title: Feature Id
        description: ID of the feature to delete
      responses:
        '204':
          description: Successful Response
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operations/polygon/area:
    post:
      tags:
      - GeoJSON
      summary: Calculate polygon area
      description: 'Calculate the approximate area of a polygon in square kilometers.


        Uses a planar shoelace approximation; accurate for small areas.'
      operationId: calculate_area_api_v1_operations_polygon_area_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolygonFeature'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Calculate Area Api V1 Operations Polygon Area Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operations/polygon/simplify:
    post:
      tags:
      - GeoJSON
      summary: Simplify a polygon
      description: 'Simplify a polygon using the Ramer-Douglas-Peucker algorithm.


        Higher tolerance values produce simpler polygons.'
      operationId: simplify_polygon_endpoint_api_v1_operations_polygon_simplify_post
      parameters:
      - name: tolerance
        in: query
        required: false
        schema:
          type: number
          maximum: 1.0
          minimum: 0.001
          description: Simplification tolerance
          default: 0.01
          title: Tolerance
        description: Simplification tolerance
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolygonFeature'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeature'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operations/polygon/contains:
    post:
      tags:
      - GeoJSON
      summary: Check if a polygon contains a point
      description: Check if a polygon contains a point using the ray casting algorithm.
      operationId: check_polygon_contains_point_api_v1_operations_polygon_contains_post
      parameters:
      - name: lon
        in: query
        required: true
        schema:
          type: number
          maximum: 180
          minimum: -180
          description: Longitude of the point
          title: Lon
        description: Longitude of the point
      - name: lat
        in: query
        required: true
        schema:
          type: number
          maximum: 90
          minimum: -90
          description: Latitude of the point
          title: Lat
        description: Latitude of the point
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolygonFeature'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                type: object
                additionalProperties: true
                title: Response Check Polygon Contains Point Api V1 Operations Polygon Contains Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operations/polygon/buffer:
    post:
      tags:
      - GeoJSON
      summary: Create a buffer around a polygon
      description: 'Create a bounding-box buffer zone around a polygon at a specified distance.


        Returns an axis-aligned rectangle expanded by `distance` on each side.'
      operationId: create_buffer_endpoint_api_v1_operations_polygon_buffer_post
      parameters:
      - name: distance
        in: query
        required: true
        schema:
          type: number
          minimum: 0
          description: Buffer distance in the specified unit
          title: Distance
        description: Buffer distance in the specified unit
      - name: unit
        in: query
        required: false
        schema:
          type: string
          description: Unit for the buffer distance
          enum:
          - meters
          - kilometers
          - miles
          default: kilometers
          title: Unit
        description: Unit for the buffer distance
      - name: segments
        in: query
        required: false
        schema:
          type: integer
          maximum: 100
          minimum: 8
          description: Segments for buffer approximation
          default: 16
          title: Segments
        description: Segments for buffer approximation
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/PolygonFeature'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeature'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operations/polygon/intersection:
    post:
      tags:
      - GeoJSON
      summary: Calculate bounding-box intersection of multiple polygons
      description: 'Calculate the bounding-box intersection of multiple polygon features.


        Raises HTTP 422 if the polygons do not overlap.'
      operationId: calculate_intersection_endpoint_api_v1_operations_polygon_intersection_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MultiPolygonRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeature'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operations/polygon/union:
    post:
      tags:
      - GeoJSON
      summary: Calculate bounding-box union of multiple polygons
      description: Calculate the bounding-box union of multiple polygon features.
      operationId: calculate_union_endpoint_api_v1_operations_polygon_union_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/MultiPolygonRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/PolygonFeature'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/operations/polygon/distance:
    post:
      tags:
      - GeoJSON
      summary: Calculate centroid distance between two polygons
      description: Calculate the centroid-to-centroid distance between two polygon features.
      operationId: calculate_distance_endpoint_api_v1_operations_polygon_distance_post
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/DistanceRequest'
        required: true
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response Calculate Distance Endpoint Api V1 Operations Polygon Distance Post
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/algorithms:
    get:
      tags:
      - Algorithms
      summary: List registered processing algorithms
      description: List all registered processing algorithms and their parameters.
      operationId: list_algorithms_api_v1_algorithms_get
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                additionalProperties: true
                type: object
                title: Response List Algorithms Api V1 Algorithms Get
  /api/v1/algorithms/{algorithm_id}:
    get:
      tags:
      - Algorithms
      summary: Describe one algorithm
      description: Return the declaration for a single registered algorithm.
      operationId: get_algorithm_api_v1_algorithms__algorithm_id__get
      parameters:
      - name: algorithm_id
        in: path
        required: true
        schema:
          type: string
          title: Algorithm Id
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlgorithmOut'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
  /api/v1/algorithms/{algorithm_id}/run:
    post:
      tags:
      - Algorithms
      summary: Run a registered processing algorithm
      description: Run one registered algorithm against the supplied layers/parameters.
      operationId: run_algorithm_api_v1_algorithms__algorithm_id__run_post
      parameters:
      - name: algorithm_id
        in: path
        required: true
        schema:
          type: string
          title: Algorithm Id
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/AlgorithmRunRequest'
      responses:
        '200':
          description: Successful Response
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/AlgorithmRunResponse'
        '422':
          description: Validation Error
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/HTTPValidationError'
components:
  schemas:
    AlgorithmOut:
      properties:
        id:
          type: string
          title: Id
        name:
          type: string
          title: Name
        description:
          type: string
          title: Description
        parameters:
          items:
            $ref: '#/components/schemas/AlgorithmParameterOut'
          type: array
          title: Parameters
      type: object
      required:
      - id
      - name
      - description
      title: AlgorithmOut
      description: A registered processing algorithm, as seen by API clients.
    AlgorithmParameterOut:
      properties:
        id:
          type: string
          title: Id
        label:
          type: string
          title: Label
        required:
          type: boolean
          title: Required
          default: false
        default:
          anyOf:
          - {}
          - type: 'null'
          title: Default
        type:
          type: string
          title: Type
          default: auto
      type: object
      required:
      - id
      - label
      title: AlgorithmParameterOut
      description: Declared parameter for a registered algorithm.
    AlgorithmRunRequest:
      properties:
        layers:
          items:
            additionalProperties: true
            type: object
          type: array
          title: Layers
        parameters:
          additionalProperties: true
          type: object
          title: Parameters
      type: object
      title: AlgorithmRunRequest
      description: Request body for running a registered algorithm.
    AlgorithmRunResponse:
      properties:
        algorithm_id:
          type: string
          title: Algorithm Id
        result:
          anyOf:
          - {}
          - type: 'null'
          title: Result
        logs:
          items:
            type: string
          type: array
          title: Logs
      type: object
      required:
      - algorithm_id
      title: AlgorithmRunResponse
      description: Result of running a registered algorithm.
    DistanceRequest:
      properties:
        polygon1:
          $ref: '#/components/schemas/PolygonFeature'
        polygon2:
          $ref: '#/components/schemas/PolygonFeature'
      type: object
      required:
      - polygon1
      - polygon2
      title: DistanceRequest
      description: Request body for polygon distance calculation.
    HTTPValidationError:
      properties:
        detail:
          items:
            $ref: '#/components/schemas/ValidationError'
          type: array
          title: Detail
      type: object
      title: HTTPValidationError
    MultiPolygonRequest:
      properties:
        polygons:
          items:
            $ref: '#/components/schemas/PolygonFeature'
          type: array
          title: Polygons
      type: object
      required:
      - polygons
      title: MultiPolygonRequest
      description: Request body containing multiple polygon features for set operations.
    Polygon:
      properties:
        type:
          type: string
          const: Polygon
          title: Type
          default: Polygon
        coordinates:
          items:
            items:
              prefixItems:
              - type: number
              - type: number
              type: array
              maxItems: 2
              minItems: 2
            type: array
          type: array
          title: Coordinates
          description: Array of linear rings
      additionalProperties: false
      type: object
      required:
      - coordinates
      title: Polygon
      description: 'GeoJSON Polygon geometry.


        The first array of coordinates represents the exterior ring.

        Any subsequent arrays represent interior rings (holes).'
    PolygonFeature:
      properties:
        type:
          type: string
          const: Feature
          title: Type
          default: Feature
        geometry:
          $ref: '#/components/schemas/Polygon'
        properties:
          anyOf:
          - additionalProperties: true
            type: object
          - type: 'null'
          title: Properties
        id:
          anyOf:
          - type: string
          - type: integer
          - type: 'null'
          title: Id
      additionalProperties: false
      type: object
      required:
      - geometry
      title: PolygonFeature
      description: A GeoJSON Feature with a Polygon geometry.
    PolygonFeatureCollection:
      properties:
        type:
          type: string
          const: FeatureCollection
          title: Type
          default: FeatureCollection
        features:
          items:
            $ref: '#/components/schemas/PolygonFeature'
          type: array
          title: Features
      additionalProperties: false
      type: object
      required:
      - features
      title: PolygonFeatureCollection
      description: A GeoJSON FeatureCollection containing only Polygon features.
    ValidationError:
      properties:
        loc:
          items:
            anyOf:
            - type: string
            - type: integer
          type: array
          title: Location
        msg:
          type: string
          title: Message
        type:
          type: string
          title: Error Type
      type: object
      required:
      - loc
      - msg
      - type
      title: ValidationError
