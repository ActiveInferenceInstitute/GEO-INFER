# Agent
: utils ## Scope
 This directory contains utils components for the module. It provides 6 classes and 1 functions. ## Classes
 and Functions ### DataSourceConfi
g
 Configuration for data sources ### DataValidationResul
t
 Results of data validation ### EconomicDataLoade
r
 data loading system for economic analysis. **Methods**: - `register_data_source(config: DataSourceConfig) -> None`: Register a data source for loading. - `load_economic_data(source_name: str, filters: Optional[Dict[str, Any]], validate: bool) -> pd.DataFrame`: Load economic data from a registered source. - `load_regional_data(source_name: str, geometry_column: str) -> gpd.GeoDataFrame`: Load regional/spatial economic data. - `validate_economic_data(data: pd.DataFrame, source_name: str) -> DataValidationResult`: Validate economic data according to predefined rules. - `preprocess_economic_data(data: pd.DataFrame, preprocessing_steps: List[str]) -> pd.DataFrame`: Preprocess economic data with common cleaning and transformation steps. - `merge_economic_datasets(datasets: List[pd.DataFrame], merge_keys: List[str], how: str) -> pd.DataFrame`: Merge multiple economic datasets. - `export_economic_data(data: pd.DataFrame, file_path: Path, format: str, **kwargs) -> None`: Export economic data to various formats. ### EconomicIndicator
s
 Utility class for calculating various economic indicators and metrics. **Methods**: - `calculate_growth_rate(values: Union[pd.Series, np.ndarray], periods: int, method: str) -> Union[pd.Series, np.ndarray]`: Calculate growth rates for time series data. - `calculate_gini_coefficient(values: np.ndarray) -> float`: Calculate Gini coefficient for inequality measurement. - `calculate_theil_index(values: np.ndarray) -> float`: Calculate Theil index for inequality measurement. - `calculate_unemployment_rate(unemployed: Union[float, np.ndarray], labor_force: Union[float, np.ndarray]) -> Union[float, np.ndarray]`: Calculate unemployment rate. - `calculate_inflation_rate(prices: Union[pd.Series, np.ndarray], base_period: int) -> Union[pd.Series, np.ndarray]`: Calculate inflation rate from price index. - `calculate_gdp_per_capita(gdp: Union[float, np.ndarray], population: Union[float, np.ndarray]) -> Union[float, np.ndarray]`: Calculate GDP per capita. - `calculate_productivity_growth(output: np.ndarray, inputs: np.ndarray, method: str) -> np.ndarray`: Calculate productivity growth. - `calculate_economic_complexity_index(exports_matrix: np.ndarray, countries: List[str], products: List[str]) -> Dict[str, float]`: Calculate Economic Complexity Index (ECI) for countries. - `calculate_regional_convergence(regional_data: pd.DataFrame, value_column: str, time_column: str) -> Dict[str, float]`: Calculate regional convergence indicators. - `calculate_economic_distance(region1_data: Dict[str, float], region2_data: Dict[str, float], method: str, weights: Optional[Dict[str, float]]) -> float`: Calculate economic distance between regions with weighting. - `calculate_spatial_economic_indicators(regional_data: pd.DataFrame, spatial_weights: np.ndarray) -> Dict[str, Any]`: Calculate spatial economic indicators including spatial autocorrelation and clustering. - `calculate_economic_resilience(time_series_data: pd.DataFrame, shock_period: Optional[str]) -> Dict[str, Any]`: Calculate economic resilience indicators from time series data. - `calculate_sectoral_composition(sectoral_data: pd.DataFrame, classification: str) -> Dict[str, Any]`: Calculate sectoral composition and diversity indicators. - `calculate_trade_integration(trade_data: pd.DataFrame, regions: List[str]) -> Dict[str, Any]`: Calculate trade integration and connectivity indicators. - `calculate_human_development_index(data: Dict[str, pd.DataFrame], weights: Optional[Dict[str, float]]) -> Dict[str, float]`: Calculate Human Development Index (HDI) components and overall index. ### ModelValidato
r
 Utility class for validating economic models and results. **Methods**: - `validate_regression_assumptions(residuals: np.ndarray, fitted_values: np.ndarray, X: Optional[np.ndarray]) -> Dict[str, Any]`: Validate regression model assumptions. - `cross_validate_model(model: Any, X: np.ndarray, y: np.ndarray, cv_folds: int, scoring: str) -> Dict[str, Any]`: Perform cross-validation for model performance assessment. - `validate_economic_model_results(predictions: np.ndarray, actual: np.ndarray, model_type: str, additional_metrics: bool) -> Dict[str, Any]`: validation of economic model results. - `validate_model_assumptions(model, X: np.ndarray, y: np.ndarray, assumptions: List[str]) -> Dict[str, Any]`: Validate key econometric model assumptions. - `validate_spatial_model_assumptions(model, X: np.ndarray, y: np.ndarray, W: np.ndarray) -> Dict[str, Any]`: Validate assumptions for spatial econometric models. ### ResultsVisualize
r
 Utility class for visualizing economic analysis results. **Methods**: - `plot_economic_indicators(data: pd.DataFrame, indicators: List[str], title: str, save_path: Optional[Path]) -> plt.Figure`: Plot economic indicators over time or across regions. - `create_choropleth_map(gdf: gpd.GeoDataFrame, value_column: str, title: str, cmap: str, save_path: Optional[Path]) -> plt.Figure`: Create a choropleth map for spatial economic data. - `plot_policy_comparison(comparison_data: Dict[str, Dict[str, float]], metrics: List[str], title: str, save_path: Optional[Path]) -> plt.Figure`: Create a comparison chart for policy scenarios. - `plot_distributional_effects(effects: Dict[str, float], title: str, save_path: Optional[Path]) -> plt.Figure`: Plot distributional effects across income quintiles or regions. - `create_spatial_heatmap(data: pd.DataFrame, value_column: str, title: str, save_path: Optional[Path]) -> plt.Figure`: Create a spatial heatmap visualization. - `plot_time_series_decomposition(time_series: pd.Series, decomposition_type: str, title: str, save_path: Optional[Path]) -> plt.Figure`: Plot time series decomposition (trend, seasonal, residual). - `create_model_diagnostics_plot(model_results: Dict[str, Any], title: str, save_path: Optional[Path]) -> plt.Figure`: Create model diagnostics visualization. - `plot_spatial_autocorrelation(data: gpd.GeoDataFrame, variable: str, spatial_weights: np.ndarray, title: str, save_path: Optional[Path]) -> plt.Figure`: Plot spatial autocorrelation analysis including Moran's I scatterplot. - `create_interactive_dashboard(data_dict: Dict[str, Any], dashboard_type: str, output_path: Optional[Path]) -> str`: Create interactive dashboard (simplified - would use Plotly/Dash in practice). ### example_data_loadin
g
 `example_data_loading()` Example usage of the EconomicDataLoader ## Capabilities
 - **6 classes** for core functionality - **1 functions** for utility operations ## Integration
 - **Location**: `GEO-INFER-ECON/src/geo_infer_econ/utils` - **Type**: Directory Node 