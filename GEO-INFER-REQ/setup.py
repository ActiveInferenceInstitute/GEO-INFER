#!/usr/bin/env python3
# -*- coding: utf-8 -*-

"""
Setup shim for GEO-INFER-REQ.

All metadata (name, version, dependencies, entry points, ...) is defined in
pyproject.toml, which is the canonical source.  This file exists only so the
PLACE pattern (``python setup.py ...``) keeps working for legacy tooling.
"""

from setuptools import setup

setup()