# SCOPE-2026-09-19 — GEO-INFER fresh scoping pass (14-lane read-only swarm)

> Produced 2026-09-19 by a 14-lane read-only scout swarm against
> `main @ 07a5fe31` (the v0.3.0 release commit; CI green at tip — run
> 35268910816, all 14 jobs success). Tree state at scan: tracked-dirty churn
> only (`manuscript/config.yaml` generator-owned `MANUSCRIPT_SOURCE_DATE`
> bump, `__pycache__` pyc churn) — excluded as phantom evidence per lane
> contract. Spec-only: no implementation is authorized by this document;
> items enter execution only on an explicit go. Does not re-open
> SCOPE-2026-09-11 / SCOPE-2026-09-15 items, and does not re-scope the open
> TODO.md rows (SPACE-01, PLACE-V14, PLACE-04, CODE-01, REL-01 residual,
> TEST-GNN-01, TEST-04 — externally blocked or coordinator-recurring).

## Method

One `task` batch fanned 14 read-only scout lanes: 5 package-level lenses
(CI/gates, packaging, docs-truth, test-estate, pipeline/perf/security) + 9
module batches covering all 45 `GEO-INFER-*` packages. Lane contract
(enforced on every lane): READ-ONLY; no suites/formatters/linters (one <60 s
pytest probe allowed when a finding demands proof — two lanes used none, all
findings are read/grep-proven); `uv run --no-sync` only; `__pycache__`/pyc
ignored; 3–8 quality-over-volume items; every item carries file:line evidence
from a file the lane actually read, a bounded next step, an acceptance probe,
and tier per the ledger rubric. Absence claims were re-verified by direct
read/grep (glob listings elide files in this repo). Coordinator-side receipts
folded in: tag/release/run state verified against the GitHub API
(`v0.3.0` → `07a5fe31` local+remote; release run 35274969824; tag-push CI run
35274969748; main CI run 35268910816; v0.2.1 has no GitHub release object).

**Result: 78 unique items — 1 Major, 14 Medium, 63 Minor.** (One pipeline-lens
item, PL-03, is byte-identical in evidence to mod-5's M5-04 and is merged.)

## Fleet-health positives (verified this pass — not scoped, recorded as receipts)

- **v0.3.0 fleet version surface fully coherent**: all 45 pyprojects, all 45
  runtime `__version__` literals, all 45 uv.lock editable entries, root
  framework, and CITATION.cff all agree at 0.3.0 (packaging-lens, full sweep).
- **CI gate coverage complete**: all 13 `GEO-INFER-TEST/validate_*.py` are
  exercised in CI; all four ruff surfaces defined and invoked; PR triggers
  unchanged since the 09-15 pass (ci-lens).
- **Centralization Rule holds**: zero TODO/FIXME/XXX/HACK markers in any of
  the 45 module src trees; zero `NotImplementedError`/pass-body stubs in
  public callables (all 9 module lanes + test lane).
- Silent-fallback hygiene broadly good post-campaign: zero bare `except:`
  repo-wide; remaining silent-fallback items below are concentrated in leaf
  code paths (pipeline-lens).
- Fully clean modules (honest empty, verified): **CIV, ORG, REQ, API, APP,
  BIO, AGENT, INTRA, FOREST-src, EDU-src (except M4-08)**.

## Major — release-scale

### CI-01 — v0.3.0 wheels were never published; release unreconciled

- **Tier/Effort**: Major, M. **Lane**: ci-lens (coordinator-corroborated).
- **Motivation/evidence**: release.yml run 35274969824 (head `v0.3.0 @
  07a5fe31`) concluded **failure** 2026-09-17T21:36Z — its ci-gate observed
  the tag-push CI run 35274969748 on the same SHA fail at validate step 13
  "Enforce module coverage floors" (ci.yml:148-165), so the release job was
  **skipped** and no `geo-infer-wheels` artifact exists. The identical-SHA
  main-push CI run 35268910816 had concluded success 33 min earlier. The
  `gh release view v0.3.0` object carries only
  `GEO-INFER-manuscript-2026-09-17.pdf` (1 asset) vs **45 wheel assets** on
  v0.2.0 — the tag, CHANGELOG, and CITATION.cff all claim a release that
  produced no wheels. Recurrence signal: tag `v0.2.1` also has **no GitHub
  release object at all** despite a successful release.yml run
  (35053351237). The GS-002 gate behaved correctly; the defect underneath is
  CI-02's tag-event diff-scope divergence.
- **Bounded next step**: Pull run-35274969748's coverage-floor failure log
  (`gh run view 35274969748 --log-failed`), land CI-02, then re-run
  release.yml via `workflow_dispatch` at `07a5fe31` (release user-authorized;
  external side effect) — or record a dated no-wheel decision in REL-01.
- **Acceptance probe**: A dispatched release run at `07a5fe31` concludes
  success with a 45-asset `v0.3.0` release (`gh release view v0.3.0 --json
  assets` → 45), or REL-01 carries the dated decision receipt.

## Medium — self-serve, multi-session (14)

### CI-02 — Tag-push diff gates measure a different diff scope than main
- **Lane**: ci-lens. Both `BASE_SHA` blocks use
  `github.event.pull_request.base.sha || github.event.before`
  (ci.yml:70, ci.yml:150); on a new tag ref `event.before` is the zero SHA —
  passes the 40-hex regex, fails `git cat-file -e`, silently degrades to
  `HEAD^` (ci.yml:74-76, 160-162). Observed consequence: same-SHA CI runs
  diverged exactly at these gates (run 35268910816 success vs run
  35274969748 failure at the coverage-floor step, 21-min vs short coverage
  measurement). `ci.yml` triggers on `tags: v*` (:6-7), so this fires on
  every release. **Next**: make the tag-event base explicit (zero-SHA
  detection → GS-004 empty-tree branch or full-tree measurement on tag
  events) in both blocks; pin in `test_ci_workflow_contracts.py`. **Probe**:
  contract test green with the new tag-event pin; a re-tag/dispatch CI run
  logs the intended coverage module set.

### CI-05 — GNN pair pin drifted behind GNN main again (custody surface)
- **Lane**: ci-lens. `.github/gnn-pair.json:3` pins `0e2a2170df47…`; GNN
  main tip is `659fe4e31b04…` (2026-09-19) with ≥4 newer commits. Same class
  as GS15-04. **Next**: in a session holding both checkouts, diff the
  interchange surface between `0e2a2170` and GNN main; bump per the
  documented pair-pin bump procedure — **through the SC-22 paired-custody
  ritual, never unilaterally** (shared custody with the `gnn` herdr
  project) — or record a dated receipt that the delta touches no
  interchange surface. **Probe**: `gh api
  repos/ActiveInferenceInstitute/GeneralizedNotationNotation/compare/0e2a2170...main
  --jq .ahead_by` verified; bump commit keeps gnn-interchange.yml green.

### TST-01 — Test-contract validator has two structural scan gaps
- **Lane**: test-lens. `validate_test_contracts.py` exempts every conftest.py
  from BOTH forbidden-control and marker checks (:104-105), so
  `GEO-INFER-ACT/tests/conftest.py:29` `pytest.importorskip("geopandas")` on
  a hard root dependency passes silently; `test_files()` globs only
  `GEO-INFER-*/tests/**/*.py` + `tests/**/*.py` (:31), so nested trees
  (`GEO-INFER-PLACE/locations/cascadia/tests/**`) are never scanned; three
  divergent marker registries exist (pyproject static registry vs
  TEST-conftest runtime markers :765-771 vs root conftest.py:60-66).
  **Next**: extend the glob to `GEO-INFER-*/**/tests/**/*.py`; scan conftests
  for FORBIDDEN_TEXT only; reconcile marker registries. **Probe**:
  `validate_test_contracts.py --strict` flags `ACT/tests/conftest.py:29`
  pre-fix and passes post-remediation (with TST-02/03 landed).

### TST-02 — Cascadia integration suite doubly dead (12 skips, uncollected)
- **Lane**: test-lens. `GEO-INFER-PLACE/locations/cascadia/tests/integration/
  test_bioregion_pipeline.py` carries 12 `pytest.skip(...)` calls under
  `except ImportError` guards for hard deps (h3, folium; fastapi is a `web`
  extra). Root `testpaths` (pyproject.toml:359-362) never collects it and
  the validator never scans it (TST-01), so the repo's no-skip conversion
  and terminal guard never see it. **Next**: one decision — wire it into
  collection with skips converted to explicit failures (h3/folium guards
  removed), or delete the tree as unmaintained. **Probe**:
  `pytest GEO-INFER-PLACE/locations/cascadia/tests -q` exits green with zero
  skip-contract failures.

### TST-03 — Availability guards around HARD dependencies (incl. dual-pass test)
- **Lane**: test-lens. `GEO-INFER-ACT/tests/integration/
  test_space_integration.py:35-51` wraps `import h3` (hard root dep) and
  asserts `"success"` when present but `"error"` when absent — the test
  passes by asserting breakage. Dead-guard variants: HAS_XARRAY in
  ENERGY/FOREST/MARINE integration tests, HAS_MATPLOTLIB/HAS_GEOPANDAS in
  BIO (API's HAS_FASTAPI is legitimate — `web` extra only). **Next**: plain
  module-level imports, delete unused HAS_* flags, collapse the dual-pass
  assertions. **Probe**: `grep -rn "HAS_XARRAY\|HAS_GEOPANDAS\|
  HAS_MATPLOTLIB\|HAS_SHAPELY" GEO-INFER-*/tests` returns no test-file hits;
  the ACT space-integration test stays green h3-present only.

### TST-07 — SPACE `nested` messaging/analytics half has zero tests
- **Lane**: test-lens. `nested/messaging/routing.py` (650 lines,
  `RoutingStrategy`/`RouteMetric` heapq pathfinding), `protocols.py`, and
  `nested/analytics/{flow_analysis,pattern_detection,hierarchy_metrics,
  performance_metrics}.py` are never imported by any test;
  `test_nested_comprehensive.py` covers only the engines/boundary/broker.
  **Next**: one focused test file per family (routing under declared
  strategies/metrics; analytics on a small synthetic NestedH3Grid).
  **Probe**: new test files green; `grep -rln "nested.messaging\|
  nested.analytics" GEO-INFER-SPACE/tests` returns ≥2 files.

### PL-05 — O(n²) interpreted pairwise loops where cKDTree is the repo norm
- **Lane**: pipeline-lens. Four sites: ANT flocking separation per-pair
  `np.linalg.norm` (`analysis/patterns/analyzer.py:314-323`, O(T·n²)),
  nearest-neighbor via `np.delete` in-loop (:1394-1397), ANT sensor
  co-location double loop (`applications/environmental.py:778-781`), AI
  pairwise belief distances
  (`models/predictive/geospatial_ai.py:1579-1582`). Norm evidence:
  cKDTree in RISK exposure_model.py:476-478 and SPACE analytics; GS-106/151
  vectorized the identical pattern in TIME/CLIMATE. **Next**: port to
  cKDTree (`tree.query(positions, k=2)`) or broadcasting; outputs
  bit-identical on existing fixtures. **Probe**: 200-agent trajectory
  through the flocking analyzer an order of magnitude faster; existing
  ANT/AI unit tests pass unchanged.

### M2-ART-02 — Four public ART modules have zero test references
- **Lane**: mod-2. `utils/validators.py` (9 documented public functions),
  `core/generation/custom_algorithms.py` `CustomAlgorithmFramework`
  (top-level `__all__`), `core/generation/performance_optimizer.py`
  (`PerformanceOptimizer` + `cache_result`/`parallel_map`/
  `time_execution`), `utils/animation.py` `save_animation_with_fallback` —
  no test imports any of them (utils/README.md:13-23 documents them).
  **Next**: unit test module starting with pure validators + a
  `CustomAlgorithmFramework` register/execute/save/load round-trip (pin the
  silent bad-algorithm skip at custom_algorithms.py:211-213). **Probe**: new
  `tests/unit/test_validators.py` green with visible import.

### M3-02 — DATA ships two same-named `GeospatialValidator` classes
- **Lane**: mod-3. `core/validation.py:92` (consumed by DataQualityManager)
  and `utils/validation.py:76` (consumed by MultiSourceDataIngestion and
  pipeline) are distinct implementations of the same contract;
  utils/validation.py:31-33 documents the acknowledged duplication.
  Standing confusion + double maintenance surface. **Next**: consolidate to
  the utils implementation, re-point core/validation.py and its test in a
  clean import cutover. **Probe**: `from geo_infer_data.core.validation
  import GeospatialValidator as A; from ...utils.validation import ... as B;
  A is B` → True post-fix; both validation suites green.

### M6-01 — Three IOT public exports have zero tests
- **Lane**: mod-6. `SensorCalibration` (init:48; utils/calibration.py, 487
  lines incl. guarded-HAS_SCIPY degraded mode), `NetworkTopology`
  (init:47; models/network.py), `SensorAPI` (init:41; api/sensor_api.py) —
  zero test matches repo-wide. **Next**: one unit file covering
  calibration round-trip + drift (both HAS_SCIPY branches via monkeypatch),
  topology construction, SensorAPI route registration. **Probe**: new test
  file green; grep no longer returns zero files.

### M6-02 — IOT websocket endpoint registers subscriptions, forwards nothing
- **Lane**: mod-6. `sensor_stream_websocket` (api/streaming_api.py:96):
  forward loop is `# In a real implementation…` + `await asyncio.sleep(1)`
  + heartbeat (lines 119-122) — subscribers never receive measurement data,
  and the no-forwarding behavior is unpinned. **Next**: wire the loop to the
  ingestion queue the class already holds and forward matching measurements;
  test asserting a subscribed websocket receives a pushed measurement.
  **Probe**: new forward test green; `grep "In a real implementation"`
  returns no match in streaming_api.py.

### M6-07 — METAGOV `monitor_performance` fabricates non-reproducible data
- **Lane**: mod-6. `AdaptiveGovernanceSystem.monitor_performance`
  (core/adaptation.py:78) hardcodes base scores (:110-119) then injects
  **unseeded** `random.uniform(-0.1, 0.1)` (:122-125) and stamps fabricated
  `data_quality` fields (:134-139); docstring claims "real performance
  tracking" (:86). Every call returns different numbers from a governance
  API — contradicts the module's own AGENTS.md rule against fake behavior.
  Silent-fabrication class. **Next**: replace unseeded random with a
  deterministic seeded derivation, wire `data_sources` or make
  `"simulated": true` explicit; determinism-pin test (two calls → identical
  dicts), mirroring the MATH MCMC precedent. **Probe**: determinism probe +
  `test_adaptation.py` green.

### M7-02 — PEP api_router: 10 placeholder endpoints, zero router tests
- **Lane**: mod-7. `api/__init__.py` exports `api_router` (package `__all__`
  at `__init__.py:85`); 10 endpoints return echo/constant placeholders with
  no persistence (performance reviews :78-88, learning :93-108, conflicts
  :112-128, surveys :133-146); no test mounts the aggregate router.
  **Next**: implement store-backed create/get for `/pep/performance/reviews`
  on the existing `PerformanceReview` model + `pep_data_manager` pattern;
  mark remaining placeholder domains DESCOPED in `docs/api_schema.yaml`
  (convention already defined, header :5-8); TestClient test in the existing
  app fixture. **Probe**: existing endpoint tests stay green + new
  performance-review endpoint test passes.

### DOC-03 — Manuscript catalog measured literals are ungated (drift class)
- **Lane**: docs-lens. The 45 catalog sections are tracked prose whose
  measured quantities (versions, test-file/class/function census, LOC) are
  literals combined verbatim at render (`scripts/render_manuscript_pdf.py::
  _module_catalog_sections` reads them straight from the tree, "no tokens" by
  design); only the index table is generator-measured. DOC-02's two stale
  0.2.1 literals prove the class is live; census literals (water.md "9 test
  files / 38 classes / 116 functions", test.md "40 files / 405 functions")
  are accurate today but maintained by no gate. **Next**: generator-time
  audit (alongside `audit_bibliography`) reconciling each section's stated
  census against `collect_inventory`'s measured counts, failing the build on
  drift — or migrate census numbers to per-module tokens. **Probe**: a
  deliberately wrong census literal fails
  `manuscript/generate_research_artifacts.py`; correct tree passes.

## Minor — bounded single-session (63)

| ID | Area | Evidence (file:line) | Bounded next step | Acceptance probe | Eff |
| --- | --- | --- | --- | --- | --- |
| CI-03 | CI | CITATION.cff unpinned by validate_packaging.py (no CITATION rule in GEO-INFER-TEST) | Add --strict check: CITATION version == fleet-majority member version; warn stale date-released at tag time | gate exits 0 today, exits 1 with version 9.9.9 in CITATION.cff | S |
| CI-04 | CI | test_ci_workflow_contracts.py:64-74 pins 8 gates, none of the 4 ruff surfaces nor packaging/documentation/logging/rewrite --check | Extend pinned marker list | scratch-removal of a ruff step fails the contract test | S |
| PKG-01 | PKG | ENERGY + WATER pyprojects declare no [project].classifiers at all (43/45 declare 3-Alpha); undeclared treated as diagnostic-only (validate_packaging.py:245-258) | Add the fleet-majority classifier block to both; consider escalating undeclared to warning | validate_packaging --strict green, no undeclared-classifier diagnostics | S |
| PKG-02 | PKG | Stale inter-member floors behind 0.3.0 fleet: ACT >=0.2.0 (:46,62-63,70), BIO >=0.1.0 (:73-75), same class in RISK/TRANSPORT | Bump cross-member floors to >=0.3.0 | grep floors across ACT/BIO/RISK/TRANSPORT pyprojects; uv lock --check | S |
| DOC-01 | docs | CHANGELOG.md has two `## [0.2.0]` headings (:92 dated 09-11, :151 dated 09-10); single compare link :866 | Fold the 09-10 deep-horizon block under the single 09-11 heading, keep link refs | `grep -c '^## \[0\.2\.0\]'` == 1; validate_documentation --strict green | M |
| DOC-02 | docs | manuscript/sections/ag.md:3 and git.md:5 hardcode stale "version 0.2.1" (actual 0.3.0 on all surfaces) | Correct the two literals (or rephrase to unpinned wording) | `grep -rn '0\.2\.1' manuscript/sections/` → no matches | S |
| DOC-04 | docs | config.yaml metadata declares documentclass extarticle/9pt but render_manuscript_pdf.py::_metadata_file hardcodes 12pt/article; geometry IS honored — [INFERENCE] template-lane consumers | Trace the template lane's consumption; wire _metadata_file to config or annotate template-lane-only | rendered _metadata.yaml carries config values, or config carries consumer-naming comment | M |
| DOC-05 | docs | ~17 hand-authored docs carry `Last Updated: 2026-02-24` stamps predating two releases (ACT/AGENT/ART/API/ECON docs, CODE_OF_CONDUCT.md:39) | Refresh or drop stale stamps; optional validate_documentation staleness check | grep 2026-02-24 stamps → no matches | M |
| TST-04 | tests | Plumbing assertions: inspect.getsource source-text counts (ACT test_geospatial_runner_outputs.py:519-523), hasattr-only "tests" (ECON test_bioregional_economics.py:20-23, GIT/COMMS/CIV test_core.py) | Rewrite as behavior tests or delete | `grep -rn "inspect.getsource" GEO-INFER-*/tests` → 0 hits; touched suites green | S |
| TST-05 | tests | TEST conftest shims masking product behavior: SeriesGroupBy.trend injection w/ silent fallback (:82-105), dead h3_legacy shim targeting nonexistent test_spatial_functions (:128-201,193) | Delete both dead shim blocks | grep conftest → 0 hits; GEO-INFER-TEST/tests green | S |
| TST-06 | tests | Stale TEST inventories list nonexistent test_spatial_functions.py and test_test_orchestrator.py (tests/unit/README.md:24-25, AGENTS.md:41-42; gnn_continuation doc :290-292 claims "still present") | Regenerate inventories from actual listing; add content-accuracy check to validator inventory audit | diff script comparing README entries vs glob reports zero mismatches | S |
| TST-08 | tests | 22 arbitrary tests/**/__init__.py files across module test trees (some roots, some subdirs) — harmless under importlib but breaks dotted-path tooling | One policy (recommend: none, matching importlib mode), remove 22 files in one commit | `git ls-files 'GEO-INFER-*/tests/**/__init__.py'` empty; representative suites green | S |
| PL-01 | PLACE | Four timeout-less requests.get in cascadia tree: download_empirical_data.py:71, improvements/data_sources.py:68,96, mortgage_debt/geo_infer_mortgage_debt.py:69 (siblings use timeout=15..300) | Add explicit timeout=(30,300)-style values + verify policy matching real_data_acquisition.py:562 | `grep -n "requests.get(" … -r \| grep -v timeout` → no matches | M |
| PL-02 | OPS | deployment.py:79,101 docker build/push subprocess.run(check=True) with no timeout | Add configurable timeout + TimeoutExpired handling | grep shows timeout on every site; sleeping-stub probe returns within bound | S |
| PL-03/M5-04 | INSURANCE | data_integration.py:531-532,556-557 (`get_credit_score`, `get_property_history`) `except Exception: return None` unlogged on bureau/property fetches — outage indistinguishable from no-record (also :488 health_check) | Narrow to expected failure classes, log warning before None, let unexpected propagate; caplog probe proves warning | pytest green + unreachable-source call logs warning | S |
| PL-04 | ACT | utils/analysis.py:276-281 entropy loop appends 0.0 sentinel on any exception (malformed ragged belief skews mean/variance toward certainty); same at ~:366-370 | Narrow to (TypeError, ValueError) + log offending index before sentinel | malformed-ragged probe emits warning; targeted pytest green | S |
| PL-06 | DATA | utils/caching.py:374-378 memory accounting: any pickle failure silently reports fixed 1KB/entry | Narrow to (TypeError, OSError, PicklingError) + log + expose estimated count in stats dict | unpicklable-entry probe asserts stats expose estimated_entries | S |
| M1-01 | AG | api/resources.py (444 lines, FieldsResource/CropsResource/YieldResource re-exported at two levels) zero test references | Add tests/unit/api/test_resources.py (create/validate/list/filter/delete) | new test file green; grep finds import | M |
| M1-02 | AG | README.md:36 shapely>=1.8.0 vs pyproject >=2.0.0; :44 requests listed as core though only in `integrations` extra | Correct README tiering | grep matches pyproject tier per dep | S |
| M1-03 | AI | geospatial_ai.py silent 0.0 fallbacks at :441-443, :465-467 (+~765, ~842, ~1326) | Narrow excepts + logger.debug(exc_info=True) | no bare except-constant without log; unit suite green | S |
| M1-04 | ACT | utils/math.py complexity metrics silent constant fallbacks: :749-751, :761-763, :770-773, :836-838 (module logger exists :13-15) | Add logger.debug(exc_info=True); narrow to math-justified exception types | remaining handlers log; math contract suites green | S |
| M1-05 | ANT | Six test modules wrap imports in try/except ImportError → pytest.fail("not fully implemented") (test_core.py:23-30 et al.) masking real import errors; module is fully implemented per its own status doc | Replace with plain imports, delete stale comments | imports unguarded; grep "not fully implemented" → 0 | S |
| M2-ART-01 | ART | cli.py:357-371 parameter_sweep branch ignores --parameter/--values, silently renders hardcoded style cycle (parser advertises them :777-787); generative_map.py:668-671 style sweep regenerates at fixed abstraction_level | Wire the branch to create_animation or reject loudly; CLI test asserting sweep values affect frames | `-k animate` test exists+passes; "would need to be implemented" grep → 0 | M |
| M2-BAYES-01 | BAYES | tfp_interface.py docstrings promise TFP delegation (:4-5, :53-58) but :17-20 hardwire tfp=None/TFP_AVAILABLE=False; SKILL.md:3 also claims TFP | Correct docstrings/SKILL.md to NumPy/SciPy reality, or add the guarded TFP import | grep "TFP is available" shows truth-consistent text | S |
| M2-BAYES-02 | BAYES | Root `__init__` imports CRESCENT_CITY_OBSERVATIONS_SCHEMA + load_crescent_city_geo_observations (:44-46) but omits both from `__all__` (:48-60); consumers use submodule path | Add both to `__all__` or drop the root imports | `import geo_infer_bayes; [n for n in __all__ if 'crescent' in n.lower()]` consistent | S |
| M3-01 | DATA | requirements.txt:27 urllib3>=1.26.0 vs pyproject:59 >=2.0.6 (header claims mirror) | Edit requirements.txt line 27 | identical specifiers in both files | S |
| M3-03 | DATA | connectors/api.py APIConnector (:23) and STACConnector (:528) zero test references (GraphQLConnector only statically tested) | Add unit file monkeypatching session (pattern: test_cloud_connectors.py:77): auth modes, pagination termination, STAC query build | grep finds connectors in tests; new file green | M |
| M3-04 | COMMS | websocket_api.py:239 imports pyjwt, `except ImportError: pass` (:255-256) falls to hash-identity (:258-262) — with COMMS_JWT_SECRET configured but PyJWT absent, any token string authenticates; pyjwt undeclared in pyproject/requirements (only transitively in uv.lock) | Declare pyjwt>=2.0.0; make secret-configured-but-PyJWT-absent a hard reject + log | find_spec('jwt') assert + unit test rejecting auth in that configuration | S |
| M3-05 | COMMS | Package `__init__` omits CollaborationManager import/__all__ entry while exporting its session models; sibling managers all exported | Add import + `__all__` entry | `from geo_infer_comms import CollaborationManager` passes | S |
| M3-06 | COG | load_cognitive_profile, save_cognitive_model, ProfileManager exported (init:47,51,72) zero test references | Add tests/unit/test_profiles_helpers.py (round-trips via tmp_path) | grep finds tests; new file green | M |
| M3-07 | CLIMATE | pyproject:38 declares pandas>=1.3.0 runtime; zero src imports (tests/examples only) | Move pandas to dev extra; README update | src grep stays empty; unit suite green via dev extra | S |
| M4-01 | ECON | Five exported classes zero test refs: EndogenousGrowthModels, RegionalConvergenceAnalysis, TechnologyDiffusionModels, EcologicalEconomicsEngine, BioregionalMarketDesign (growth_models.py:298,462,627; ecological_economics.py:745; bioregional_markets.py:66) | One behavioral unit test per class (ctor + representative op) | grep ≥1 hit per class; unit suite green | M |
| M4-02 | ENERGY | tests/conftest.py:10-12 imports geopandas/h3/shapely undeclared (dev extra = pytest+pytest-timeout only); resolve only transitively | Mirror EMERGENCY's declared test-only dev-extra block; re-lock | pyproject grep ≥3; uv lock --check clean | S |
| M4-03 | ENERGY | Docstring-only dead subpackages utils/ + api/ (zero references repo-wide) | Delete both | glob empty; unit suite green | S |
| M4-04 | ENERGY | Six unit tests sys.path.insert CWD-relative hack (test_carbon_footprint.py:9 et al.) though package is installed editable | Delete the six sys.path blocks | grep empty; test suite green | S |
| M4-05 | EMERGENCY | README.md:27-32 lists test-only numpy/geopandas/shapely as runtime deps; shapely>=1.8.0 vs dev >=2.0.0 (requirements.txt agrees with pyproject) | Patch README Dependencies section to networkx only | sed 27,32p shows networkx only | S |
| M4-06 | EXAMPLES | generate_orchestrators.py is a stale placeholder generator with overwrite-mode writes (:227,:447,:479) that would clobber 43 real orchestrator scripts; MODULES dict omits IOT | Delete it (superseded by committed scripts + _lib.py) or quarantine + refuse-to-overwrite guard | file absent (or generator exits before write); git diff on orchestrators empty | S |
| M4-07 | EXAMPLES | test_orchestrator_scripts_execute.py:29 SUBSET = 8 of 43 orchestrators; 35 never executed incl. ECON/EDU/EMERGENCY/ENERGY (verified synthetic-data, no-network) | Extend SUBSET with the four after timing check (<180 s) | test → 12 passed, each status ok | M |
| M4-08 | EDU | exercises.py:296 generated starter code imports nonexistent spatial_analysis_lib — ImportError on scaffold line 1 | Stdlib-only scaffold or explicit illustrative note | grep spatial_analysis_lib → empty/confined; exercise suites green | S |
| M5-01 | GIT | utils/observability.py (985 lines) + utils/performance.py (~795) zero callers; sole psutil importers justifying the hard dep | Confirm no consumer, then delete both + drop psutil — or wire ObservabilityManager into rest_api startup + test; pick one | post-deletion grep returns only self/doc refs; suite green | M |
| M5-02 | FOREST | docs/getting_started.md:18 and docs/index.md:90 claim 0.2.0 (all authoritative surfaces 0.3.0) | Update two lines | grep 0.2.0 in docs → none | S |
| M5-03 | HEALTH | geopandas declared unconditional runtime (pyproject:44) but cli.py guards ImportError with graceful degrade (:441-444, :572-576); core/models never import it | Pick one stance (extra vs required + delete guards); align README/requirements.txt | pyproject placement matches stance; suite green | S |
| M5-05 | INSURANCE | outputs/risk_engine.log churn sits next to package; root .gitignore patterns root-anchored only, module has no .gitignore | Add module .gitignore (outputs/, cache/); git rm --cached log if tracked | git check-ignore matches new rule | S |
| M5-06 | GIT | core/advanced_git.py:596 _analyze_conflict wraps read+parse in bare except → None; failure path untested | Catch OSError, log path warning; add failure-path test | advanced_git suite green incl. new test | S |
| M6-03 | LOG | utils/optimization.py (394 lines, solve_tsp/solve_vrp) guarded-imports ortools, declared nowhere (not even an extra); both functions zero test refs | Add `solver = ["ortools>=9.0"]` extra + absent-ImportError test | grep ortools pyproject → extra; new test green | S |
| M6-04 | MARINE | utils/biodiversity.py (only util; consumed by both core consumers) zero direct tests; all-zero-species edge unpinned | Add tests/unit/test_biodiversity.py (hand-computable distributions incl. all-zero) | new test green | S |
| M6-05 | MATH | psutil>=5.8.0 core dep (pyproject:41 + READMEs) imported zero times anywhere | Remove from pyproject+READMEs, or wire into gpu_acceleration memory reporting — one decision | grep psutil src/pyproject → no code/manifest hits; suite green | S |
| M6-06 | MATH | core/symbolic_math.py (1,098 lines, package-exposed) zero test references; carries :189-191 `except Exception: pass` on derivative path, :447-449 sympify fallback, prover.py:145,163 broad fallbacks | Add tests/unit/test_symbolic_math.py (both backends, round-trips); narrow :189-191 | new test green; grep :189 block for bare pass → 0 | M |
| M7-01 | OPS | app.py create_app (:27-83; Dockerfile CMD target, CORS, metrics mount, /health, /version, config-fallback block :35-49) zero test references | Add tests/unit/test_app.py via TestClient (fallback path, /health, /version == __version__, metrics mount) | new file green from repo root | M |
| M7-03 | PEP | methods.py:60-72 __all__ = 12 functions, only 3 tested; PEPEngine import/dashboard wrappers (:56-175) untested | Extend test_methods.py: tmp-CSV import round-trips ×3, dashboard shape assertions ×3 (empty + populated) | test_methods.py green, GS-249/251 stay green | M |
| M7-04 | PLACE | README.md:34 shapely>=1.8.0 vs pyproject:40 >=2.0.0; omits urllib3/networkx (both runtime imports); identical stale block repeats in ~20 generated sub-READMEs | Fix top-level README; trace the generator emitting the repeats and fix at source | grep shapely>=1.8.0 → 0; urllib3+networkx present | S |
| M7-05 | NORMS | compliance_tracking.py threshold dispatch (:325-332) and evaluation_type chain (:324,341,362) have no final else — unknown metric config silently scores non-compliant (misleading "does not meet threshold" note :335-336) | Add else branches surfacing misconfiguration (ValueError into the :370 handler or explicit note); test invalid evaluation_type is surfaced | compliance suite green + new misconfiguration test | S |
| M8-01 | SIM | CellularAutomata + SystemDynamicsModel exported (init:14-15, paradigms init:5-7) zero test refs; real deterministic implementations, regression ships silently | Two unit files: CA neighbor rules/blink/shape-ValueError/seeded determinism; SD stock-flow conservation/clamp/reset/ValueError | new tests green | M |
| M8-02 | RISK | catastrophe_models.py:697-729 _analyze_intensity_distribution stores params of LAST successful fit (:729) while best_distribution chosen by min AIC (:721-723) — mismatched family params feed intensity sampling; :725-726 hides fit failures; all-fail NameError swallowed by :731 | Track best_params alongside best_aic; store at :728-729; narrow + log per-fit except; regression test on params-family fidelity | reproducibility suite + new params-fidelity test green | S |
| M8-03 | SEC | README.md:38 + requirements.txt:10 say shapely>=1.8.0; canonical pyproject:46 >=2.0.0 | Fix both lines | all three files show >=2.0.0 | S |
| M8-04 | RISK+SEC | sklearn/joblib used behind guards (RISK catastrophe_models.py:810; SEC cognitive_security.py:26-37) but undeclared in both pyprojects (no extra) — diverges from SIM's documented mesa-extra pattern | Add `ml = ["scikit-learn>=1.0","joblib>=1.0"]` extras with explanatory comment | grep scikit-learn both pyprojects → extras hit | S |
| M8-05 | SPACE | .gitignore:1-3 comment admits ~35 dashboard HTMLs committed from earlier runs; test_output/ (20+ comprehensive_dashboard_*.html 2026-01/02) and reports/ (21+) still populated and tracked | Coordinator-owned `git rm -r --cached` of the four dirs; keep/commit READMEs explicitly | `git ls-files` on the four dirs → empty (READMEs only if kept) | S |
| M9-01 | TEST | validate_repo_contracts.py:115-121 locally redefines expected_package_name, duplicating _validator_common.py:88-93 (already imports six other helpers :27-34) | Import from _validator_common, delete local def | grep local def → 0; validator --strict-source-language passes | S |
| M9-02 | WATER | src api/ and utils/ subpackages are docstring-only empties with zero references; ship as empty wheel packages + signpost overhead | Delete both; regen signposts | grep geo_infer_water.(api\|utils) → nothing; WATER unified tests pass | S |
| M9-03 | TRANSPORT | docs/api_reference.md contradicts code ×3: route() signature w/ mode/avoid/via (:106-117 vs routing.py:87-92), model_type "bpr/akcelik/hcm" (:186 vs traffic.py:81-85 bpr-only), EmissionsCalculator claim (:102 vs zero refs) | Rewrite the three sections to real signatures (SKILL.md wording); note route_source/fallback semantics | grep avoid\|via:\|akcelik\|EmissionsCalculator → 0; TRANSPORT unified tests pass | M |
| M9-04 | SPM+TIME | Empty [project.scripts] tables: SPM pyproject:77-78, TIME :67-68 (CHANGELOG:498 records the identical phantom removal for TEST) | Delete both tables | grep -c '^\[project.scripts\]' → 0; validate_packaging --strict green | S |
| M9-05 | TEST | Silent excepts: validators.py:324-326 SpatialValidator swallows pd.to_numeric failure; module_health.py:217-219 _extract_dependencies returns [] on parse failure → corrupt pyproject reports status "ok", total 0 (:257) | Narrow both; record parse failure as status "unknown"+reason | suites green + new invalid-pyproject case asserting status != "ok" | S |
| M9-06 | TEST | run_full_system_test (public export init:30,66; validators.py:1034-1047) zero test references | Add one parametric case to test_validators_parametric.py (delegate parity vs QualityController) | new case green; grep finds test hit | S |

## Verified non-findings (recorded so future lanes don't re-sweep)

- CIV, ORG, REQ, API, APP, BIO, AGENT, INTRA: honest empty results (full census
  receipts in lane reports — exports test-referenced, deps coherent, no smells).
- Bare `except:` zero repo-wide; no uncontext-managed `open` in src; lru_caches
  bounded; `np.random` fully migrated to `default_rng` + seed contract.
- Four ruff surfaces all defined and invoked (ci.yml:92-96,125-131); all 13
  validators CI-invoked; PR triggers unchanged since 09-15.
- ACT backends-extra libs genuinely used; TIME NotImplementedError sites are the
  documented abstract-transport contract; MARINE netcdf4 declared for xarray's
  .nc engine (legitimate); COG gazetteer limitation documented, not scoped.
- GS-062 (root-glob blind spot) verified FIXED at validate_test_contracts.py:31.
- Spec-only — TODO.md row updates (REL-01 residual: v0.3.0 wheels) are staged in
  the ledger handoff, not applied by this pass.

## Lane index

| Lane | Coverage | Items |
| --- | --- | --- |
| ci-lens | workflows ×4, 13 validators, signpost static check, gnn-pair vs GNN main, release runs | CI-01..05 |
| packaging-lens | 45 pyprojects + uv.lock + CITATION + classifiers + extras | PKG-01..02 |
| docs-lens | CHANGELOG, CITATION, manuscript catalog ×8/45 full + render script, stale-date sweep | DOC-01..05 |
| test-lens | validator scan surface, skips/guards/markers registry, plumbing assertions, TEST conftest shims | TST-01..08 |
| pipeline-lens | cross-module silent-fallback/subprocess/network/perf/RNG sweep | PL-01..06 |
| mod-1 | ACT AG AGENT AI ANT | M1-01..05 |
| mod-2 | API APP ART BAYES BIO | M2-ART-01..02, M2-BAYES-01..02 |
| mod-3 | CIV CLIMATE COG COMMS DATA | M3-01..07 |
| mod-4 | ECON EDU EMERGENCY ENERGY EXAMPLES | M4-01..08 |
| mod-5 | FOREST GIT HEALTH INSURANCE INTRA | M5-01..06 (04 ≡ PL-03) |
| mod-6 | IOT LOG MARINE MATH METAGOV | M6-01..07 |
| mod-7 | NORMS OPS ORG PEP PLACE | M7-01..05 |
| mod-8 | REQ RISK SEC SIM SPACE | M8-01..05 |
| mod-9 | SPM TEST TIME TRANSPORT WATER | M9-01..06 |

## Recommended execution order (on an explicit go)

1. **Wave R (release reconciliation)**: CI-02 fix → CI-01 diagnosis log pull →
   re-run release.yml for v0.3.0 (user-authorized) → REL-01 ledger receipt.
   Highest value; unblocks the release claim the tag already makes.
2. **Wave G (custody)**: CI-05 GNN pair-pin evaluation through the SC-22
   paired-custody ritual (coordinates with the `gnn` herdr project).
3. **Wave T (test contracts)**: TST-01 → TST-03 → TST-02 (validator surface
   first, then remediations it enables) + TST-05/TST-06 (same package).
4. **Wave M (medium engineering)**: remaining Medium items (PL-05, M3-02,
   M6-01/02/07, M7-02, M2-ART-02, TST-07, DOC-03) — parallel lanes with
   disjoint module ownership.
5. **Wave S (minor sweep)**: 63 Minor items batched by module, 4-6 lanes,
   disjoint ownership; coordinator owns TODO.md/CHANGELOG/SCOPE and runs the
   signpost regen + full gate battery before each push.

Traps carried from the overnight-wave playbook: signpost regen after every src
change; replicate ALL FOUR ruff surfaces + validate_skills --check-xrefs +
validate_test_contracts --strict (+ validate_packaging --strict when versions
move) before push; reconcile against live main before merge.