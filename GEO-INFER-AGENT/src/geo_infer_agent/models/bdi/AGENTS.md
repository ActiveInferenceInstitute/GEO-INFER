# Agent
: bdi ## Scope
 This directory contains bdi components for the module. It provides 11 classes and 0 functions. ## Classes
 and Functions ### Belie
f
 A single belief representing a piece of information the agent holds about the world. **Methods**: - `update(value: Any, source: Optional[str], confidence: Optional[float]) -> None`: Update the value, source, and confidence of this belief. - `is_outdated(max_age_seconds: float) -> bool`: Check if this belief is outdated based on its timestamp. - `to_dict() -> Dict[str, Any]`: Convert this belief to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'Belief'`: Create a Belief instance from a dictionary. ### BeliefBas
e
 A collection of beliefs maintained by an agent. **Methods**: - `add(belief: Belief) -> None`: Add a belief to the belief base. - `update(name: str, value: Any, source: Optional[str], confidence: Optional[float]) -> None`: Update an existing belief. - `get(name: str) -> Belief`: Get a belief by name. - `get_value(name: str) -> Any`: Get the value of a belief by name. - `remove(name: str) -> None`: Remove a belief from the belief base. - `has_belief(name: str) -> bool`: Check if a belief with the given name exists. - `get_all() -> Dict[str, Belief]`: Get all beliefs in the belief base. - `get_all_values() -> Dict[str, Any]`: Get all belief values in the belief base. - `query(**kwargs) -> List[Belief]`: Query beliefs by various criteria. - `query_spatial(center: Dict[str, float], radius: float) -> List[Belief]`: Query beliefs by spatial reference within a radius of a center point. - `check_consistency() -> List[Tuple[str, str, Any, Any]]`: Check for consistency issues between beliefs. - `clean_outdated(max_age_seconds: float) -> int`: Remove beliefs that are older than a certain age. - `get_history(limit: Optional[int]) -> List[Dict[str, Any]]`: Get the history of belief changes. - `to_dict() -> Dict[str, Any]`: Convert the belief base to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'BeliefBase'`: Create a BeliefBase instance from a dictionary. ### DesireStat
e
 Enumeration of possible states for a desire. ### Desir
e
 A desire representing a goal the agent wants to achieve. **Methods**: - `is_active() -> bool`: Check if this desire is active. - `is_achieved() -> bool`: Check if this desire has been achieved. - `is_failed() -> bool`: Check if this desire has failed. - `activate() -> None`: Activate this desire. - `deactivate() -> None`: Deactivate this desire. - `suspend() -> None`: Suspend this desire. - `mark_achieved() -> None`: Mark this desire as achieved. - `mark_failed() -> None`: Mark this desire as failed. - `check_preconditions(belief_values: Dict[str, Any]) -> bool`: Check if the preconditions for this desire are met. - `check_success(belief_values: Dict[str, Any]) -> bool`: Check if the success conditions for this desire are met. - `check_failure(belief_values: Dict[str, Any]) -> bool`: Check if the failure conditions for this desire are met. - `to_dict() -> Dict[str, Any]`: Convert this desire to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'Desire'`: Create a Desire instance from a dictionary. ### DesireSe
t
 A collection of desires maintained by an agent. **Methods**: - `add(desire: Desire) -> None`: Add a desire to the desire set. - `get(name: str) -> Desire`: Get a desire by name. - `remove(name: str) -> None`: Remove a desire from the desire set. - `has_desire(name: str) -> bool`: Check if a desire with the given name exists. - `get_all() -> Dict[str, Desire]`: Get all desires in the desire set. - `get_active() -> Dict[str, Desire]`: Get all active desires in the desire set. - `get_achieved() -> Dict[str, Desire]`: Get all achieved desires in the desire set. - `get_failed() -> Dict[str, Desire]`: Get all failed desires in the desire set. - `select_desires(max_count: Optional[int]) -> List[Desire]`: Select desires based on priority. - `update_states(belief_values: Dict[str, Any]) -> Tuple[List[str], List[str]]`: Update the states of all desires based on the current beliefs. - `query_spatial(center: Dict[str, float], radius: float) -> List[Desire]`: Query desires by spatial reference within a radius of a center point. - `to_dict() -> Dict[str, Any]`: Convert the desire set to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'DesireSet'`: Create a DesireSet instance from a dictionary. ### IntentionStatu
s
 Enumeration of possible statuses for an intention. ### Intentio
n
 An intention representing a commitment to achieving a goal through a plan. **Methods**: - `is_active() -> bool`: Check if this intention is active. - `is_achieved() -> bool`: Check if this intention has been achieved. - `is_failed() -> bool`: Check if this intention has failed. - `is_dropped() -> bool`: Check if this intention has been dropped. - `is_suspended() -> bool`: Check if this intention is suspended. - `activate() -> None`: Activate this intention. - `suspend() -> None`: Suspend this intention. - `achieve() -> None`: Mark this intention as achieved. - `fail() -> None`: Mark this intention as failed. - `drop() -> None`: Drop this intention. - `set_progress(progress: float) -> None`: Set the progress of this intention. - `is_expired() -> bool`: Check if this intention has expired (missed its deadline). - `should_reconsider(belief_values: Dict[str, Any]) -> bool`: Check if this intention should be reconsidered based on current beliefs. - `to_dict() -> Dict[str, Any]`: Convert this intention to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'Intention'`: Create an Intention instance from a dictionary. ### IntentionStructur
e
 A structure for managing intentions. **Methods**: - `add(intention: Intention) -> None`: Add a intention to the intention structure. - `get(intention_id: str) -> Intention`: Get an intention by ID. - `remove(intention_id: str) -> None`: Remove an intention from the intention structure. - `has_intention(intention_id: str) -> bool`: Check if an intention with the given ID exists. - `has_intention_for_goal(goal: str) -> bool`: Check if there is an active intention for the given goal. - `get_all() -> Dict[str, Intention]`: Get all intentions in the intention structure. - `get_active() -> Dict[str, Intention]`: Get all active intentions in the intention structure. - `get_suspended() -> Dict[str, Intention]`: Get all suspended intentions in the intention structure. - `get_achieved() -> Dict[str, Intention]`: Get all achieved intentions in the intention structure. - `get_failed() -> Dict[str, Intention]`: Get all failed intentions in the intention structure. - `get_by_goal(goal: str) -> List[Intention]`: Get all intentions for a specific goal. - `select_intention(max_count: Optional[int]) -> List[Intention]`: Select intentions for execution based on priority. - `reconsider_intentions(belief_values: Dict[str, Any]) -> Tuple[List[str], List[str]]`: Reconsider intentions based on current beliefs. - `update_progress(intention_id: str, progress: float) -> None`: Update the progress of an intention. - `clean_completed(keep_achieved: bool, keep_failed: bool) -> int`: Remove completed intentions from the intention structure. - `to_dict() -> Dict[str, Any]`: Convert the intention structure to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'IntentionStructure'`: Create an IntentionStructure instance from a dictionary. ### PlanStatu
s
 Enumeration of possible statuses for a plan. ### Pla
n
 A plan representing a way to achieve a goal. **Methods**: - `is_applicable(belief_values: Dict[str, Any]) -> bool`: Check if this plan is applicable in the current context. - `can_start(belief_values: Dict[str, Any]) -> bool`: Check if this plan can start based on its precondition. - `has_succeeded(belief_values: Dict[str, Any]) -> bool`: Check if this plan has succeeded based on its postcondition. - `has_failed(belief_values: Dict[str, Any]) -> bool`: Check if this plan has failed based on its failure condition. - `start() -> None`: Start executing this plan. - `succeed() -> None`: Mark this plan as succeeded. - `fail() -> None`: Mark this plan as failed. - `reset() -> None`: Reset this plan to the PENDING status. - `to_dict() -> Dict[str, Any]`: Convert this plan to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'Plan'`: Create a Plan instance from a dictionary. ### PlanLibrar
y
 A collection of plans maintained by an agent. **Methods**: - `add(plan: Plan) -> None`: Add a plan to the plan library. - `get(name: str) -> Plan`: Get a plan by name. - `remove(name: str) -> None`: Remove a plan from the plan library. - `has_plan(name: str) -> bool`: Check if a plan with the given name exists. - `get_all() -> Dict[str, Plan]`: Get all plans in the plan library. - `get_by_goal(goal: str) -> List[Plan]`: Get all plans for a specific goal. - `select_plan(goal: str, belief_values: Dict[str, Any]) -> Optional[Plan]`: Select the most appropriate plan for a given goal in the current context. - `create_plan_instance(template_name: str, instance_name: str) -> Plan`: Create a instance of a plan from a template. - `to_dict() -> Dict[str, Any]`: Convert the plan library to a dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'PlanLibrary'`: Create a PlanLibrary instance from a dictionary. ## Capabilities
 - **11 classes** for core functionality ## Integration
 - **Location**: `src/geo_infer_agent/models/bdi` - **Type**: Directory Node 