# Agent
: models ## Scope
 This directory contains models components for the module. It provides 20 classes and 0 functions. ## Classes
 and Functions ### GenerativeMode
l
 Represents an agent's generative model of the world. **Methods**: - `update_likelihood(observation: np.ndarray, state: np.ndarray) -> None`: Update the likelihood mapping (A) based on observed data. - `update_transition(prev_state: np.ndarray, current_state: np.ndarray, action: int) -> None`: Update the transition mapping (B) based on observed state transitions. - `update_preferences(preferred_observations: np.ndarray) -> None`: Update preferences over observations. - `infer_state(observation: np.ndarray) -> np.ndarray`: Perform state inference given an observation. - `predict_next_state(current_state: np.ndarray, action: int) -> np.ndarray`: Predict the next state given current state and action. - `expected_free_energy(state_belief: np.ndarray, action: int, planning_horizon: int) -> float`: Calculate the expected free energy for a policy. - `select_action(current_state_belief: np.ndarray, planning_horizon: int) -> int`: Select the best action to minimize expected free energy. - `to_dict() -> Dict[str, Any]`: Convert model to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'GenerativeModel'`: Create a generative model from a dictionary. ### ActiveInferenceStat
e
 State for an Active Inference agent. **Methods**: - `update_with_observation(observation: np.ndarray) -> np.ndarray`: Update agent state with a observation. - `record_action(action: int, reward: float) -> None`: Record an action taken by the agent. - `update_preferences(preferred_obs: np.ndarray) -> None`: Update the agent's preferences. - `select_action(planning_horizon: int) -> int`: Select the next action using active inference. - `to_dict() -> Dict[str, Any]`: Convert state to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'ActiveInferenceState'`: Create agent state from dictionary. ### ActiveInferenceAgen
t
 Implementation of an Active Inference agent. ### Belie
f
 Represents a belief in the agent's belief base. **Methods**: - `update(value: Any, confidence: Optional[float], metadata: Optional[Dict[str, Any]]) -> None`: Update the belief with information. - `to_dict() -> Dict[str, Any]`: Convert to dictionary. - `from_dict(data: Dict[str, Any]) -> 'Belief'`: Create from dictionary. ### Desir
e
 Represents a desire in the agent's desire set. **Methods**: - `set_achieved(achieved: bool) -> None`: Mark the desire as achieved or not. - `is_expired() -> bool`: Check if the desire has expired. - `to_dict() -> Dict[str, Any]`: Convert to dictionary. - `from_dict(data: Dict[str, Any]) -> 'Desire'`: Create from dictionary. ### Pla
n
 Represents a plan in the agent's intention structure. **Methods**: - `next_action() -> Optional[Dict[str, Any]]`: Get the next action to execute. - `record_action_result(action_index: int, result: Dict[str, Any], success: bool) -> None`: Record the result of an action. - `advance() -> bool`: Advance to the next action in the plan. - `mark_complete(successful: bool) -> None`: Mark the plan as complete. - `to_dict() -> Dict[str, Any]`: Convert to dictionary. - `from_dict(data: Dict[str, Any]) -> 'Plan'`: Create from dictionary. ### BDIStat
e
 Extended agent state for BDI agents. **Methods**: - `add_belief(belief: Belief) -> None`: Add a belief or update an existing one. - `update_belief(name: str, value: Any, confidence: Optional[float], metadata: Optional[Dict[str, Any]]) -> None`: Update a belief with information. - `get_belief(name: str) -> Optional[Belief]`: Get a belief by name. - `add_desire(desire: Desire) -> None`: Add a desire. - `get_desire(name: str) -> Optional[Desire]`: Get a desire by name. - `get_desires_by_priority() -> List[Desire]`: Get all desires ordered by priority. - `add_intention(plan: Plan) -> None`: Add a intention (plan). - `set_current_intention(plan: Optional[Plan]) -> None`: Set the currently active intention. - `get_current_intention() -> Optional[Plan]`: Get the currently active intention. - `get_intentions_for_desire(desire_name: str) -> List[Plan]`: Get all intentions for a specific desire. - `remove_completed_intentions() -> int`: Remove completed intentions. - `to_dict() -> Dict[str, Any]`: Convert state to dictionary for serialization. - `from_dict(cls, data: Dict[str, Any]) -> 'BDIState'`: Create state from dictionary. ### BDIAgen
t
 Belief-Desire-Intention (BDI) agent implementation. **Methods**: - `update_beliefs(perception: Dict[str, Any]) -> None`: Update beliefs based on perceptions. ### SubAgentWrappe
r
 Wrapper for a sub-agent within a hybrid architecture. **Methods**: - `check_activation(context: Dict[str, Any]) -> bool`: Check if this sub-agent should be activated. - `record_decision(successful: bool, reward: float) -> None`: Record a decision made by this agent. - `to_dict() -> Dict[str, Any]`: Convert to dictionary representation. ### HybridStat
e
 State for a hybrid agent. **Methods**: - `add_sub_agent(wrapper: SubAgentWrapper) -> None`: Add a sub-agent. - `remove_sub_agent(agent_id: str) -> bool`: Remove a sub-agent. - `get_active_agents() -> List[SubAgentWrapper]`: Get all active sub-agents based on current context. - `update_context(key: str, value: Any) -> None`: Update a value in the shared context. - `get_context_value(key: str, default: Any) -> Any`: Get a value from the shared context. - `record_decision(agent_id: str, decision: Dict[str, Any]) -> None`: Record a decision made by a sub-agent. - `record_result(result: Dict[str, Any], success: bool, reward: float) -> None`: Record the result of an action. - `to_dict() -> Dict[str, Any]`: Convert to dictionary representation. ### HybridAgen
t
 Implementation of a hybrid agent architecture. ### Experienc
e
 Represents a single experience tuple (s, a, r, s', done). **Methods**: - `to_dict() -> Dict[str, Any]`: Convert to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'Experience'`: Create from dictionary representation. ### QTabl
e
 Simple Q-table for tabular Q-learning. **Methods**: - `get_value(state: int, action: int) -> float`: Get Q-value for state-action pair. - `update_value(state: int, action: int, value: float) -> None`: Update Q-value for state-action pair. - `get_best_action(state: int) -> int`: Get action with highest Q-value for state. - `to_dict() -> Dict[str, Any]`: Convert to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'QTable'`: Create from dictionary representation. ### ReplayBuffe
r
 Experience replay buffer for DQN and similar algorithms. **Methods**: - `add(experience: Experience) -> None`: Add experience to buffer. - `sample(batch_size: int) -> List[Experience]`: Sample a batch of experiences. - `size() -> int`: Get current size of buffer. - `to_dict() -> Dict[str, Any]`: Convert to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'ReplayBuffer'`: Create from dictionary representation. ### RLStat
e
 State for a Reinforcement Learning agent. **Methods**: - `select_action(state: Any) -> int`: Select action using epsilon-greedy policy. - `update_q_values(experience: Experience) -> None`: Update Q-values based on experience. - `record_episode_reward(reward: float, episode_done: bool) -> None`: Record reward for current episode. - `train_from_buffer(batch_size: Optional[int]) -> None`: Train from replay buffer. - `to_dict() -> Dict[str, Any]`: Convert to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'RLState'`: Create from dictionary representation. ### RLAgen
t
 Implementation of a Reinforcement Learning agent. ### Rul
e
 Represents a single rule with condition and action. **Methods**: - `matches(state: Dict[str, Any]) -> bool`: Check if rule condition matches the current state. - `to_dict() -> Dict[str, Any]`: Convert rule to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'Rule'`: Create rule from dictionary representation. ### RuleSe
t
 A collection of rules with management and selection functionality. **Methods**: - `add_rule(rule: Rule) -> None`: Add a rule to the rule set. - `remove_rule(rule_id: str) -> bool`: Remove a rule from the rule set. - `get_rule(rule_id: str) -> Optional[Rule]`: Get a rule by ID. - `enable_rule(rule_id: str) -> bool`: Enable a rule. - `disable_rule(rule_id: str) -> bool`: Disable a rule. - `find_matching_rules(state: Dict[str, Any]) -> List[Rule]`: Find all rules that match the current state. - `to_dict() -> Dict[str, Any]`: Convert rule set to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'RuleSet'`: Create rule set from dictionary representation. ### RuleBasedStat
e
 State for a rule-based agent. **Methods**: - `add_rule(rule: Rule) -> None`: Add a rule to the agent's rule set. - `remove_rule(rule_id: str) -> bool`: Remove a rule from the agent's rule set. - `update_fact(key: str, value: Any) -> None`: Update a fact in the agent's knowledge base. - `get_fact(key: str, default: Any) -> Any`: Get a fact from the agent's knowledge base. - `remove_fact(key: str) -> bool`: Remove a fact from the agent's knowledge base. - `record_execution(rule_id: str, action: Dict[str, Any], result: Dict[str, Any]) -> None`: Record rule execution in history. - `find_matching_rules() -> List[Rule]`: Find all rules that match the current facts. - `to_dict() -> Dict[str, Any]`: Convert state to dictionary representation. - `from_dict(cls, data: Dict[str, Any]) -> 'RuleBasedState'`: Create state from dictionary representation. ### RuleBasedAgen
t
 Implementation of a rule-based agent. ## Capabilities
 - **20 classes** for core functionality ## Integration
 - **Location**: `GEO-INFER-AGENT/src/geo_infer_agent/models` - **Type**: Directory Node 