# GEO-INFER-AGENT runtime dependencies.
# This file mirrors [project.dependencies] in pyproject.toml (the canonical
# source) plus the optional extras.  Keep both in sync.
#
# Runtime dependencies
numpy>=1.23.5
torch>=2.0.0
pyyaml>=6.0
requests>=2.28.2
fastapi>=0.104.0
pydantic>=2.5.0
pandas>=1.3.0
uvicorn>=0.24.0
#
# Optional: [project.optional-dependencies] integrations
#   psutil — try/except-guarded in geo_infer_agent.api.telemetry
psutil>=5.9.0
#
# Optional: [project.optional-dependencies] dev (tests / lint / docs)
pytest>=7.3.1
pytest-cov>=4.1.0
pytest-asyncio>=0.20.0
httpx>=0.24.0
mypy>=1.3.0
black>=23.3.0
