"""Setup shim for GEO-INFER-DATA.

All packaging metadata lives in ``pyproject.toml``; this file exists only so
legacy tooling that imports ``setup.py`` keeps working.
"""

from setuptools import setup

setup()
