# SCOPE-2026-09-15 — GEO-INFER TODO truth pass + scoping addendum

> Produced 2026-09-15 by a single-repo scope pass against `main @ f5e5baf1`
> (clean tree before this pass; CI green at tip). Docs-only: no
> implementation is authorized; items enter execution only on an explicit
> go. This pass verifies every open row of `TODO.md` at the tip and scopes a
> small set of fresh, evidence-grounded follow-ups. It does not re-open the
> delivered SCOPE-2026-09-11 campaign (189/189 items recorded in CHANGELOG
> `## [0.2.0] - 2026-09-11 - repo-wide quality campaign`); the two commits
> at the tip (`8d3cb5e2`, `f5e5baf1`) are the campaign's last-known test-flake
> repairs and are verified green below.

## Method

All probes lightweight (reads, greps, single-file pytest; no full suites, no
environment mutation, no network from this checkout):

1. Repo state: `git log --oneline -10`; `git status` (clean);
   `git rev-list --count 0e6a47d4..HEAD` = 9;
   `git rev-list --count v0.2.0..HEAD` = 18;
   `git log -1 v0.2.0` (tag at `0a30df63`, created 2026-09-11 20:01:41 -0700);
   `git branch -a --contains v0.2.0` (main + origin/main).
2. Docs read end-to-end: `TODO.md` (all 7 open rows), `SCOPE-2026-09-11.md`,
   `AGENTS.md`, `CHANGELOG.md` head, `pyproject.toml`
   `[tool.pytest.ini_options]` (testpaths `GEO-INFER-*/tests`).
3. `.gitnexus/meta.json`: `lastCommit` `0e6a47d4…`, indexedAt
   2026-09-15T02:32:38Z — vs tip `f5e5baf1` (staleness residual, GS15-02).
4. Module inventory sweep: all 45 `GEO-INFER-*` directories contain `tests/`
   and `README.md` (loop-verified) — no missing-test-dir/README gaps at that
   granularity.
5. `uv run --no-sync pytest -q GEO-INFER-MATH/tests/integration/test_integration_modules.py`
   → 25 passed (MCMC `np.random.seed(0)` determinism pin holds at the tip).
6. `uv run --no-sync pytest -q GEO-INFER-LOG/tests/unit/test_observability.py`
   → 4 passed (non-negative observability-timer assert holds at the tip).
7. `sed -n '65,90p' GEO-INFER-PLACE/tests/integration/test_regional_layer_acquisition.py`
   — fail-closed assertions confirmed at lines 76-80 (TODO row cites :73,81 —
   drifted citation, GS15-01).
8. `printenv` grep: no `OPENAI_API_KEY` (TEST-04 re-check);
   `grep -c starlette` in `GEO-INFER-{DATA,GIT,NORMS,PEP}/pyproject.toml` →
   1 each (post-tag packaging delta); `.github/gnn-pair.json` read;
   `grep -n "strict-import-smoke" GEO-INFER-TEST/validate_repo_contracts.py`
   (:822, :864 — GS-003 delivered by the campaign, not re-opened).

Release remote state taken from the coordinator's GitHub-API check this pass:
remote tag `v0.2.0` at `0a30df63`; `release.yml` run
[34669262158](https://github.com/ActiveInferenceInstitute/GEO_INFER/actions/runs/34669262158)
(head `v0.2.0`) concluded **success** 2026-09-12T03:01Z; the earlier run at
`db400e3b` failed and PR #31 ("release-fix-ops-wheel") fixed it.

## Cleared-item evidence

No TODO rows were cleared this pass — every row verified as still open for
the external/blocked reason recorded in its row (see Carried-Open). The one
"gaps" question this pass could have cleared is answered negative: the
45-module inventory has no module lacking `tests/` or `README.md`.

Campaign tail verification (the tip's two flake repairs hold):

- `8d3cb5e2`/`f5e5baf1` (MCMC determinism pin,
  `GEO-INFER-MATH/tests/integration/test_integration_modules.py`):
  25/25 passed (probe 5).
- `8d3cb5e2` (observability timer,
  `GEO-INFER-LOG/tests/unit/test_observability.py`): 4/4 passed (probe 6).

## Carried-open (all verified current at f5e5baf1)

| ID | Tier | Disposition |
| --- | --- | --- |
| SPACE-01 | Major | Open, hardware-blocked. Guide verified present (`GEO-INFER-SPACE/docs/GPU_ACCELERATION.md`). |
| PLACE-V14 | Major | Open on licensed-data acquisition. Boundary geojson absent; fail-closed pin verified at `test_regional_layer_acquisition.py:76-80` (row citation drifted → GS15-01). |
| PLACE-04 | Major | Open, Windows-runtime-blocked. POSIX worker surfaces verified present (`GEO-INFER-PLACE/tests/integration/test_regional_download_worker.py`). |
| CODE-01 | Major | Open (recurring). Index at `0e6a47d4`; 9 commits behind tip including source changes → refresh due (GS15-02). |
| REL-01 | Major | Truth-corrected: release succeeded (run 34669262158); residual is the go/no-go on the 18-commit post-tag delta (GS15-03). |
| TEST-GNN-01 | Medium | Open, already current: the row carries the 2026-09-15 induced-lock probe (SQLite lock contention ruled out; narrowed to I/O-class faults). Next probe design scoped as GS15-05. |
| TEST-04 | Minor | Open, blocked on account-level advisor auth; 2026-09-15 re-check found the environment unchanged (no `OPENAI_API_KEY`). |

## Improvements — Minor

**GS15-01 · MINOR · DOCS · PLACE-V14 row cites stale test line numbers**
- Motivation: The row's pinned-citation pointer predates a test-file edit; a reader following `:73,81` lands on the wrong assertions.
- Evidence: `TODO.md:34` cites `test_regional_layer_acquisition.py:73,81`; the fail-closed assertions (`test_whole_bioregion_boundary_remains_fail_closed`) sit at `GEO-INFER-PLACE/tests/integration/test_regional_layer_acquisition.py:76-80` (verified this pass).
- Bounded next step: Update the row's citation to `:76-80`.
- Acceptance probe: `sed -n '76,80p' GEO-INFER-PLACE/tests/integration/test_regional_layer_acquisition.py` shows exactly the fail-closed block the row points at.
- Effort: S

**GS15-02 · MINOR · REPO · GitNexus index refresh on the current tip (CODE-01 cadence)**
- Motivation: The index misses 9 commits including source changes, so index-based navigation is stale relative to the tree.
- Evidence: `.gitnexus/meta.json` `lastCommit` = `0e6a47d4…`; tip `f5e5baf1`; `git rev-list --count 0e6a47d4..HEAD` = 9, incl. ErrorHandlerMiddleware source in DATA/NORMS/GIT/PEP (`3f67344b`, `2cbb6782`, `aff9776e`) and starlette dependency declarations (`1037bc7b`).
- Bounded next step: `node .gitnexus/run.cjs analyze` on the new tip; strip the AGENTS.md block injection per the cadence note; count-refresh the CLAUDE.md block.
- Acceptance probe: `python3 -c "import json;m=json.load(open('.gitnexus/meta.json'));print(m['lastCommit'][:7])"` prints the then-current tip SHA.
- Effort: S

## Improvements — Medium

**GS15-03 · MEDIUM · RELEASE · v0.2.1 go/no-go on the post-tag delta (REL-01 residual)**
- Motivation: v0.2.0 shipped from `0a30df63` (2026-09-11); substantial source and packaging changes have landed since with no release decision recorded.
- Evidence: `git rev-list --count v0.2.0..HEAD` = 18; post-tag source changes: fleet-wide ErrorHandlerMiddleware in DATA/NORMS/GIT/PEP (`3f67344b`, `2cbb6782`, `aff9776e`) with starlette declared in those four pyprojects (`1037bc7b`, grep-verified 1 hit each); LOG endpoint contract tests for 14 endpoints (`ef3db992`); two flake repairs (`8d3cb5e2`, `f5e5baf1`); CHANGELOG still carries an `[Unreleased]` section (LOG-EXC-01).
- Bounded next step: Deliberate go/no-go per the ledger header rule. If go: fold `[Unreleased]` into `## [0.2.1]`, bump versions per the fleet classifier, run `validate_packaging --strict`, tag. If no-go: record the decision in REL-01's row.
- Acceptance probe: post-decision, either `git tag -l v0.2.1` resolves with a folded CHANGELOG section, or REL-01's row records the dated no-go.
- Effort: M

**GS15-04 · MEDIUM · INTERCHANGE · Cross-check the pinned GNN pair revision (SC-22 / pair-pin custody)**
- Motivation: The paired GNN interchange is pinned to a specific GNN revision; if GNN main has moved past the pinned pair commit, interchange runs validate against a stale contract surface.
- Evidence: `.github/gnn-pair.json:3-4` pins `ActiveInferenceInstitute/Generalized_Notation_Notation` at revision `903b9c3391ce277292fd1f3d5626e5fbd66d354a`; the bump procedure is documented and linked (`AGENTS.md:14-15` → anchor verified at `GEO-INFER-TEST/docs/gnn_space_time_2026_09.md:80`, "## GNN pair-pin bump procedure"); the workflow gates on the pin file (`.github/workflows/gnn-interchange.yml` paths). Whether `903b9c33` is still GNN main's pair-pinned commit cannot be verified from inside this repo.
- Bounded next step: In a session holding both checkouts, compare the pin against GNN main's pair-pin ledger; bump per the documented procedure if drifted.
- Acceptance probe: the two revisions match, or a bump commit updates `.github/gnn-pair.json` and `gnn-interchange.yml` runs green on the PR.
- Effort: M

**GS15-05 · MEDIUM · TEST · I/O-class fault probe for TEST-GNN-01's narrowed hypothesis**
- Motivation: Lock contention is ruled out; the historical SQLITE_IOERR-class failure needs a probe aimed at the surviving hypothesis class, or the row stays open indefinitely.
- Evidence: `GEO-INFER-TEST/docs/gnn_continuation_2026_09.md:366` (2026-09-15 section): verified external `BEGIN EXCLUSIVE` lock did not block PROJ CRS reads while ordinary SQLite readers got `database is locked`; conclusion narrows the trigger to I/O-level/file-content faults on the shared embedded proj.db; "not closed: the exact trigger remains unexplained".
- Bounded next step: Design the next minimal probe for the I/O class — e.g. a fault-injecting or checksummed re-read of a copied proj.db under `CRS.from_epsg`, or fs-level pressure reproduction — without mutating the shared worktree file.
- Acceptance probe: A dated receipt section in `gnn_continuation_2026_09.md` either reproduces an SQLITE_IOERR-class CRS failure or records the negative with method and versions.
- Effort: M

## Improvements — Major

None this pass. The only release-scale decision on the horizon (REL-01's
v0.2.1 go/no-go) is bounded self-serve work scoped as GS15-03; SPACE-01's
hardware verification remains the sole true external-resource row and is
already carried in `TODO.md`.
