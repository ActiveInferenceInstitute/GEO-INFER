"""Packaged configuration resources for GEO-INFER-GIT.

These YAML files ship inside the package tree and are resolved at runtime via
``importlib.resources`` (see ``geo_infer_git.utils.config_loader``).
"""