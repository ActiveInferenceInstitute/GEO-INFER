# SCOPE-2026-09-11 — GEO-INFER repo-wide scoping spec

> Produced 2026-09-11 by a 14-lane scoping swarm (5 package-level lenses + 9 module-batch lenses covering all 45 modules) against `main @ a998ae91`. 189 canonical items after cross-lane integration: **48 Medium / 141 Minor / 0 Major**; 6 probe-confirmed by live runs. This document scopes only — **no implementation is authorized**; items enter execution only on an explicit go. Ids (`GS-<NNN>`) are stable and sparse by lane range. Evidence cites the tree at `a998ae91`; entries without line numbers are whole-file citations copied verbatim from the lane reports.

## Rubric

- **Major** — external resources or release-scale decisions. None this pass: every finding is self-serve. The single MAJOR-graded lane finding (BIO missing subpackage `__init__.py`, GS-201) was re-tiered Medium — bounded 3-file fix — and carries a release-blocking flag for the next wheel build.
- **Medium** — self-serve, multi-session engineering (48 items).
- **Minor** — bounded single-session change (141 items).

## Exclusions

The seven rows already open in `TODO.md` (SPACE-01, PLACE-V14, PLACE-04, TEST-04, TEST-GNN-01, CODE-01, REL-01) and the September-2026 landed work are out of scope. Lane probes ran `uv run --no-sync` only (no environment mutation); the audited tree is exactly `main @ a998ae91`.

## Medium items (48)

### Package-level surfaces

**GS-002 · MEDIUM · CI-GATES · Tag-push releases skip wheel install-verification and have no CI-success gate**
- Motivation: On the production tag-push v* path the inputs context is empty, so --verify is never appended and verify_wheels never runs: no clean-venv install, no import-origin/site-packages assertion, no version-metadata agreement check, no packaged-resource read-back. Separately, nothing gates the release on CI success: a tag may point at any commit and the job runs only validate_packaging + validate_repo_contracts --skip-import-smoke (no test contracts, no unit smoke).
- Evidence: `release.yml:13-20` · `release.yml:63-67` · `build_package_wheels.py:157-238` · `release.yml:57-62`
- Bounded next step: Invert the conditional to `github.event.inputs.run_isolated_verify != 'false' && '--verify' || ''` so tag builds always verify with a manual opt-out; add a CI-success dependency (workflow_run-triggered release, or a required `test` job via reusable workflow) before wheel upload.
- Acceptance probe: `dispatch the workflow with the input unset and assert the job log contains the per-wheel `probe_token ... status:ok` lines from verify_wheels; post-change, `grep -n "run_isolated_verify != 'false'" .github/workflows/release.yml``
- Effort: M

**GS-003 · MEDIUM · CI-GATES · Import-smoke and skills cross-reference failures can never fail CI (vacuous gates)**
- Motivation: validate_repo_contracts records every import-probe failure via report.warning and exits 1 only on report.errors, so ci.yml's per-package import + origin gate is advisory: a package that raises on import or resolves from site-packages instead of its src tree prints WARNING and the job stays green. Same shape in validate_skills: --check-xrefs appends cross-reference problems to all_warnings only and returns 1 only on all_errors, so broken README/AGENTS↔SKILL cross-references pass by construction.
- Evidence: `validate_repo_contracts.py:523-586` · `validate_repo_contracts.py:~840` · `validate_repo_contracts.py:~617-633` · `ci.yml:97-105` · `validate_skills.py:262-266` · `ci.yml:110` · `validate_skills.py:304`
- Bounded next step: Add --strict-import-smoke to validate_repo_contracts (promote the three warning sites in validate_import_smoke) and a warnings-fatal flag to validate_skills; triage current warnings to zero, then flip the CI invocations to strict.
- Acceptance probe: `break one package's import on a scratch branch (e.g. remove a runtime dep from its pyproject), run `uv run python GEO-INFER-TEST/validate_repo_contracts.py --strict-source-language` — today exit 0 with a WARNING; post-fix exit 1`
- Effort: M

**GS-005 · MEDIUM · CI-GATES · Gates duplicated per matrix leg, categories strictly serial, changed modules tested twice**
- Motivation: The test job runs two matrix legs and each leg serially executes six interpreter-independent gate steps that are identical across legs: repo contracts incl. 45 import probes, syntax+test contracts, model contracts+audit, source hygiene, gitleaks full-history scan, coverage floor. Unit→integration→performance→system→H3 then run strictly serially inside one job although each category is an independent per-module subprocess sweep; a PR touching module M additionally runs M's unit+integration suite twice (once under pytest-cov, again in the category steps).
- Evidence: `ci.yml:25-31` · `ci.yml:97-105` · `ci.yml:107-110` · `ci.yml:112-115` · `ci.yml:117-125` · `ci.yml:127-140` · `ci.yml:142-155` · `ci.yml:157-227` · `measure_module_coverage.py:57-104`
- Bounded next step: Split a single `validate` job (contracts, hygiene, gitleaks, model audit, coverage floor) out of the matrix; make each test category its own matrix job so wall time is max(category) not sum(category); consider feeding the coverage gate from category runs via --cov instead of a second sweep. Keep fail-fast=false and artifact names stable for required checks.
- Acceptance probe: `on a docs-only PR, compare job timelines before/after — validate job roughly unchanged, test legs no longer execute the six duplicated steps; `runs:` count in ci.yml increases accordingly`
- Effort: L

**GS-023 · MEDIUM · PACKAGING · 10 modules parent-climb __file__ to repo dirs that installed wheels never contain**
- Motivation: validate_packaging.py --diagnostics reports 13 parent-climbing sites across 10 modules: canonical package-data globs exist, but actual runtime configs live in module-root config/ directories that packages.find never includes, so installed wheels cannot find runtime configs. The validator's docstring explicitly tells authors to migrate these sites, yet no item owned the migration.
- Evidence: `GEO-INFER-TEST/validate_packaging.py:44-46` · `GEO-INFER-HEALTH/src/geo_infer_health/utils/config.py:256` · `GEO-INFER-HEALTH/MANIFEST.in:5` · `GEO-INFER-ACT/geo_infer_act/utils/h3_adapter.py` · `GEO-INFER-OPS/geo_infer_ops/utils/config.py` · `GEO-INFER-OPS/geo_infer_ops/core/deployment.py` · `GEO-INFER-GIT/utils/config_loader.py` · `GEO-INFER-GIT/logging_utils.py` · `GEO-INFER-SPACE/utils/config_loader.py` · `GEO-INFER-PLACE/core/unified_backend.py` · `GEO-INFER-PLACE/locations/del_norte_county/*` · `GEO-INFER-RISK/utils/validation.py` · `GEO-INFER-SEC/core/digital_security.py` · `GEO-INFER-INTRA/utils/config.py` · `GEO-INFER-EXAMPLES/core/module_orchestrator.py`
- Bounded next step: Per module, move runtime-loaded configs under src/<pkg>/ so the canonical "*.yaml"/"*.json" package-data globs cover them, switch loaders to importlib.resources discovery with a repo-checkout fallback only behind an explicit env var, then flip validate_source_traversal from diagnostic to error for the migrated modules.
- Acceptance probe: `uv run --no-sync python GEO-INFER-TEST/validate_packaging.py --diagnostics (expect no parent-climb diagnostics for migrated modules); plus a throwaway check that the config file resolves from the installed package dir: python -c "import geo_infer_health.utils.config as c; print(c.__file__)" + resources path`
- Effort: L

**GS-060 · MEDIUM · TEST-ESTATE · Real pytest files stranded outside every collection lane (cascadia, TEST demo)**
- Motivation: discover_geo_infer_modules only takes top-level GEO-INFER-* dirs whose tests/ sits directly inside, and root testpaths globs GEO-INFER-*/tests which cannot reach nested locations, so the cascadia test tree and the Crescent City demo test are collected by no lane. Two cascadia integration files are not pytest files at all: comprehensive_test.py is a __main__ shim doing an implicit-relative import that raises ImportError at collection under the root --import-mode=importlib if pytest were ever pointed there.
- Evidence: `GEO-INFER-TEST/run_unified_tests.py:31-32` · `GEO-INFER-TEST/run_unified_tests.py:69-84` · `pyproject.toml:361-364` · `pyproject.toml:365` · `GEO-INFER-PLACE/locations/cascadia/tests/README.md:35` · `GEO-INFER-PLACE/locations/cascadia/tests/README.md:53` · `GEO-INFER-PLACE/locations/cascadia/tests/integration/comprehensive_test.py:1-4` · `GEO-INFER-TEST/demo/test_crescent_city_civic_intel_demo.py:3-11` · `GEO-INFER-TEST/demo/test_crescent_city_civic_intel_demo.py:16-22` · `GEO-INFER-TEST/tests/conftest.py:28-29` · `GEO-INFER-TEST/tests/unit/test_crescent_city_bundled_seed_uniqueness.py:32`
- Bounded next step: (1) move demo/test_crescent_city_civic_intel_demo.py to GEO-INFER-TEST/tests/unit/ where the conftest path hack already makes demo importable; (2) rename the three non-pytest scripts in cascadia tests/integration/ (comprehensive_test.py, focused_framework_test.py, run_comprehensive_validation.py) out of *_test.py names; (3) add the cascadia tests dir to the unified runner as an explicit extra test-path (or lift unit/test_import.py + test_geo_infer_integrations.py into GEO-INFER-PLACE/tests/), keeping the licensed-data-dependent integration files marked per the existing PLACE-V14 deferral rather than wiring them red
- Acceptance probe: `uv run --no-sync python GEO-INFER-TEST/run_unified_tests.py --module TEST (demo test now collected, 0 failed); uv run --no-sync pytest --collect-only -q GEO-INFER-PLACE/locations/cascadia/tests/unit resolves without ImportError`
- Effort: M

**GS-061 · MEDIUM · TEST-ESTATE · conftest sys.path hacks and logging mutation contradict the runner contract**
- Motivation: 17 module test conftests re-add src to sys.path although the unified runner already injects every workspace src dir via PYTHONPATH and uv sync --all-packages installs all members. The hacks let tests pass on a module whose packaging is broken while validate_packaging.py --strict certifies the installed surface, and GEO-INFER-TEST/tests/conftest.py calls logging.basicConfig at import, mutating the root logger for every test process in the session.
- Evidence: `GEO-INFER-LOG/tests/conftest.py:10-13` · `GEO-INFER-RISK/tests/conftest.py:10-13` · `GEO-INFER-AI:10-11` · `COMMS:10-11` · `TIME` · `CIV` · `COG` · `METAGOV` · `ORG` · `PEP` · `SIM` · `APP` · `AG` · `API:15-18` · `EXAMPLES:6-7` · `INTRA:15-16` · `GEO-INFER-TEST/run_unified_tests.py:79-90` · `GEO-INFER-TEST/tests/conftest.py:32-35` · `GEO-INFER-TEST/tests/conftest.py:28-29`
- Bounded next step: delete the sys.path blocks from the conftests that only add <module>/src (runner PYTHONPATH + installed workspace covers it), keep only GEO-INFER-TEST/tests/conftest.py:28-29 (needed for demo.* imports); remove the basicConfig; INTRA:15-16 inserts the module root rather than src and must go too; run the unit category to confirm nothing regressed
- Acceptance probe: `uv run --no-sync python GEO-INFER-TEST/run_unified_tests.py --category unit --timeout 600 green after removal; grep -rl 'sys.path.insert' GEO-INFER-*/tests/conftest.py shrinks to the deliberate cases`
- Effort: M

**GS-080 · MEDIUM · PIPELINE-SEC · PERF-01 receipt environment contradicts uv.lock and baseline; provenance ephemeral**
- Motivation: The receipt records Python 3.12.13, pandas 3.0.5, scipy 1.17.1 while the same-day baseline records Python 3.12.11 and scipy 1.16.1, and uv.lock pins scipy 1.16.1 and pandas 2.3.1; the receipt's venvs were built with uv pip install -e, which ignores uv.lock. Additionally all provenance (probe script, importtime dumps, report copy, venvs) lives in /tmp and is gone after a reboot. PERF-01's 12.9 s attribution is not comparable to the locked environment and the receipt's 2x-regression policy ('same package versions') cannot be verified against uv.lock.
- Evidence: `GEO-INFER-TEST/docs/import_latency_2026_09.md:12,14,16` · `GEO-INFER-TEST/docs/benchmark_baseline_2026-09-10.md:17` · `.python-version` · `uv.lock:10372-10373` · `uv.lock:478` · `GEO-INFER-TEST/docs/import_latency_2026_09.md:27-29` · `GEO-INFER-TEST/docs/import_latency_2026_09.md:88-92`
- Bounded next step: (a) Commit the probe wrapper to GEO-INFER-TEST/tools/ (or attach under docs/) and fix the receipt's Provenance section to point there; (b) append a dated note to the receipt stating the venvs were non-locked (pandas 3.0.5 / scipy 1.17.1 do not match uv.lock) and its numbers are environment-specific, or re-measure under a uv sync env.
- Acceptance probe: `grep -n "scipy" uv.lock (1.16.1) vs grep -n "scipy" GEO-INFER-TEST/docs/import_latency_2026_09.md; receipt Provenance section must reference repo paths`
- Effort: S

**GS-082 · MEDIUM · PIPELINE-SEC · matrix_log_det silently returns a different mathematical quantity on failure**
- Motivation: np.log(np.linalg.det(matrix)) is wrapped in except Exception, falling back to sum(log(eigenvals[eigenvals > 0])). For a negative-definite or singular matrix the fallback silently computes the log of the pseudo-determinant, and for an indefinite matrix it reports a log-det that is not the log-det — no log, no warning; callers doing free-energy/Bayes computations get wrong numbers with no signal. Not an availability probe (no import under the except).
- Evidence: `GEO-INFER-ACT/src/geo_infer_act/utils/math.py:592-606`
- Bounded next step: Detect the actual failure (det <= 0 → return -inf explicitly, matching the empty-eigenvals branch; log a debug with the determinant value) and drop the bare except Exception to except (np.linalg.LinAlgError, ValueError).
- Acceptance probe: `throwaway uv run --no-sync python -c probe: matrix_log_det of [[0,1],[1,0]] (det=-1) must not return a positive finite value that silently contradicts det=-1`
- Effort: S

**GS-083 · MEDIUM · PIPELINE-SEC · _compute_obs_belief_correlation injects an error sentinel into its float-valued data dict**
- Motivation: On any exception the function returns {"error": str(e)} where the contract (per the try block) is a dict keyed state_<i>_obs_<j> with float values. Consumers that read correlations["state_0_obs_0"] get a KeyError, and consumers that iterate values get a string where a float belongs — the failure surfaces as a downstream TypeError far from the cause. The logger.warning is there, but the sentinel shape is the hazard.
- Evidence: `GEO-INFER-ACT/src/geo_infer_act/utils/analysis.py:417-420`
- Bounded next step: Return {} (or re-raise) on failure, or return a typed result object; migrate the in-module caller(s) that check for the "error" key.
- Acceptance probe: `grep for correlations["error"] / .get("error" in the repo to enumerate consumers, then a probe asserting the public caller produces only float-valued entries`
- Effort: M

**GS-084 · MEDIUM · PIPELINE-SEC · scenarios.py: nine identical unlogged except-Exception plotly-to-HTML-table fallbacks**
- Motivation: Each report builder wraps the whole plotly construction in except Exception: and silently downgrades to a static HTML table, with no logging and nine copy-pasted fallback bodies (~2,100-3,100 lines of duplication in one file). Any bug in one fig.update_layout call (e.g. a renamed column in row) silently degrades every generated report to a static table; the graceful-degradation intent is fine, the silence and 9x duplication are not.
- Evidence: `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:1324` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:1533` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:2123` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:2193` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:2272` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:2344` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:2411` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:2486` · `GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py:3086`
- Bounded next step: Extract one _plotly_or_table(fig_builder, table_rows, headers, title) helper that logs at warning level (module name + exception) and renders the table; replace the nine sites.
- Acceptance probe: `grep -c "include_plotlyjs=" GEO-INFER-ACT/src/geo_infer_act/runners/scenarios.py drops to the helper's single definition; run one scenarios smoke report and confirm the HTML output still contains the table when plotly is present`
- Effort: M

### GeoCore (SPACE · TIME · DATA · MATH)

**GS-100 · MEDIUM · SPACE · Moran's I variance uses wrong S2 term and clamps away negative variances**
- Motivation: statistics.py:105 computes S2 as only row sums instead of the Cliff-Ord sum((row_sum_i + col_sum_i)^2) used by MATH's morans_i_variance, making SPACE's S2 4x too small on symmetric row-standardized matrices and inflating z-scores. statistics.py:118 clamps variance to 1e-10 so degenerate/negative variance silently yields spurious significant p-values, while MATH raises. Two divergent Moran's I implementations (SPACE vs MATH) drift; SKILL.md's 'MATH -> spatial weights' integration claim glosses over it.
- Evidence: `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:105` · `GEO-INFER-MATH/src/geo_infer_math/core/spatial_statistics.py:52` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:120` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:118` · `GEO-INFER-MATH/src/geo_infer_math/core/spatial_statistics.py:~72-110` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:60-141`
- Bounded next step: Fix S2 to (row_sums + col_sums)**2; drop the max(variance, 1e-10) clamp in favor of an explicit error key when variance <= 0; delegate to geo_infer_math.core.spatial_statistics.morans_i_variance or add a cross-module parity test pinning Var(I) between both implementations on a fixed weight matrix.
- Acceptance probe: `uv run --no-sync python -c "import numpy as np; from geo_infer_space.core.statistics import SpatialStatistics; from geo_infer_math.core.spatial_statistics import morans_i_variance; ..." — assert SPACE variance matches MATH morans_i_variance within 1e-6 on a shared 6-cell adjacency matrix`
- Effort: M

**GS-101 · MEDIUM · SPACE · Weight-matrix build silently substitutes fully-connected graph on backend failure**
- Motivation: statistics.py:152 hardcodes a 'h3' backend default that bypasses the dispatcher's deliberate no-implicit-default policy. If H3 is missing or per-cell lookups fail (inner except: continue), the outer handler logs a warning and fabricates an all-connected row-standardized matrix; Moran's I on it is numerically meaningless yet returned as a normal result, indistinguishable from real adjacency. Same hardcoded-'h3' pattern recurs at statistics.py:215, 307, 522.
- Evidence: `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:152` · `GEO-INFER-SPACE/src/geo_infer_space/core/dispatcher.py:96-115` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:166-167` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:168-174` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:215` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:307` · `GEO-INFER-SPACE/src/geo_infer_space/core/statistics.py:522`
- Bounded next step: Route backend resolution through dispatcher._resolve_backend_name (or raise UnsupportedSpatialOperationError); remove the silent all-connected fallback — raise or return an error key like the n<3 branch; mirror the warning-with-degradation pattern pinned by tests/unit/test_analytics_warnings.py.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_space.core.statistics import SpatialStatistics; s = SpatialStatistics(); r = s.moran_i(['nope1','nope2','nope3','nope4'], [1.0,2.0,3.0,4.0])" — must raise or return an explicit error key, not a p-value`
- Effort: M

**GS-102 · MEDIUM · SPACE · SKILL.md examples call API that does not exist (grid_disk, tessellate)**
- Motivation: SKILL.md:64 shows backend.grid_disk(cell, k=2) and SKILL.md:71 shows backend.tessellate(region, resolution=8) on H3Backend; neither method exists anywhere in SPACE src, tests, or docs (repo-wide grep: zero hits outside SKILL.md). The real method is H3Backend.get_cells_within_radius (backends/h3/h3_backend.py:311); no tessellation entry point exists at all. SKILL.md is the module's agent-facing contract, so both snippets fail on first use; its other examples (latlng_to_cell, NestedH3Grid, GISManager) are accurate.
- Evidence: `GEO-INFER-SPACE/SKILL.md:64` · `GEO-INFER-SPACE/SKILL.md:71` · `GEO-INFER-SPACE/src/geo_infer_space/backends/h3/h3_backend.py:311`
- Bounded next step: Replace the two snippets with the real API (backend.get_cells_within_radius(cell, k=2); polygon coverage via backend.polygon_to_cells) or, if tessellation is genuinely wanted, add it as a thin alias over polygon_to_cells — one or the other, not both.
- Acceptance probe: `uv run --no-sync python -c "import re, pathlib, geo_infer_space, inspect; from geo_infer_space.backends.h3 import H3Backend; assert hasattr(H3Backend, 'get_cells_within_radius')" plus a grep gate that every backend.<method> in SKILL.md resolves to a real attribute (script check)`
- Effort: S — Note: INTRA SKILL.md spread of the same phantom claims is tracked by PlatformDocs items GS-291/GS-296

**GS-107 · MEDIUM · DATA · 'rollback' error-recovery strategy is advertised but silently unimplemented**
- Motivation: ErrorRecoveryStrategy.ROLLBACK exists and the constructor docstring promises rollback-on-failure, but the transformation error branch only handles FAIL_FAST and SKIP, and _handle_transformation_error/_handle_error act on RETRY and INTELLIGENT_RETRY only — ROLLBACK falls through every branch, records the failure, and continues as if nothing happened. No test covers it; tests only pin enum string values.
- Evidence: `GEO-INFER-DATA/src/geo_infer_data/core/pipeline.py:54` · `GEO-INFER-DATA/src/geo_infer_data/core/pipeline.py:424-428` · `GEO-INFER-DATA/src/geo_infer_data/core/pipeline.py:845-854` · `GEO-INFER-DATA/src/geo_infer_data/core/pipeline.py:1002-1010` · `GEO-INFER-DATA/tests/unit/test_pipeline.py:209-211`
- Bounded next step: Either implement rollback semantics (or fail loudly with NotImplementedError until real), or remove ROLLBACK from the enum and docstrings — the constructor accepts it today and users get silent no-op behavior.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-DATA/tests/unit/test_pipeline.py -q after adding a case: pipeline configured with error_recovery="rollback" + a failing transformation must either restore prior state or raise, never silently continue`
- Effort: M

**GS-108 · MEDIUM · DATA · RETRY re-enters execute_workflow recursively, no attempt cap, discards retry result**
- Motivation: The RETRY branch sleeps 5s then awaits execute_workflow from inside _handle_error, itself called from execute_workflow's except block immediately before raise. On persistent failure this recurses without an attempt counter (stack grows; each level re-appends execution history), and even when the retried workflow succeeds its result is thrown away — the caller still gets the original exception. _intelligent_error_recovery has the same recursive re-entry with the same discard-on-success path.
- Evidence: `GEO-INFER-DATA/src/geo_infer_data/core/pipeline.py:1003-1006` · `GEO-INFER-DATA/src/geo_infer_data/core/pipeline.py:674-676` · `GEO-INFER-DATA/src/geo_infer_data/core/pipeline.py:1055-1056`
- Bounded next step: Add max_retries (config, default e.g. 3) with iterative backoff; on successful recovery, return the retry's result instead of re-raising; on exhaustion, raise with the accumulated error.
- Acceptance probe: `throwaway script constructing IntelligentETLPipeline(error_recovery="retry") with an always-failing transformation — must terminate after max_retries with a clear error (today: unbounded recursion)`
- Effort: M

### Inference (BAYES · SPM · COG · ACT)

**GS-122 · MEDIUM · SPM · SPMAPI upload/fit/contrast/results pipeline untested with doc-drift and stubbed format**
- Motivation: Public SPMAPI (exported at __init__.py:80) has zero test references in GEO-INFER-SPM/tests; the whole stateful surface (upload_data, fit_model, run_contrast, get_results, shared next_id counter) is untested; upload_data docstring claims 'json', 'csv', 'geojson' but code raises ValueError for anything but json/csv; and get_results(format="visualization") serves a placeholder under a public format option.
- Evidence: `src/geo_infer_spm/__init__.py:80` · `src/geo_infer_spm/api/endpoints.py:36-64` · `src/geo_infer_spm/api/endpoints.py:66-104` · `src/geo_infer_spm/api/endpoints.py:106-157` · `src/geo_infer_spm/api/endpoints.py:159-187` · `src/geo_infer_spm/api/endpoints.py:32-34` · `src/geo_infer_spm/api/endpoints.py:49-52` · `src/geo_infer_spm/api/endpoints.py:344-345`
- Bounded next step: (1) Implement geojson upload or fix the docstring; (2) make format="visualization" delegate to visualization/interactive.py helpers or document it as a raw payload; (3) add unit tests covering json/csv upload round trips, fit->contrast->get_results(summary|full), unknown-dataset/-result errors, invalid-format rejection.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-SPM/tests/unit/test_api_endpoints.py -q (new file) and grep -n geojson GEO-INFER-SPM/src/geo_infer_spm/api/endpoints.py shows claim/behavior aligned`
- Effort: M

**GS-126 · MEDIUM · COG · Constraint reasoning enumerates the full Cartesian product of assignments (exponential)**
- Motivation: _solve_spatial_constraints builds one domain variable per unordered region pair (domain = all topological relations) then itertools.products over all of them, filtering afterward; cost is k^(n(n-1)/2) — 5 regions x RCC-8-style relation set is about 8^10, roughly 10^9 assignments, reachable from the public reason_about_space path when constraint reasoning is selected; there is no pruning, pair cap, or early consistency check, and the code comment itself says 'in practice would use CSP solver'.
- Evidence: `core/spatial_reasoning.py:795-818` · `core/spatial_reasoning.py:758-792` · `core/spatial_reasoning.py:809-811` · `core/spatial_reasoning.py:806`
- Bounded next step: (a) Restrict domain variables to premise pairs plus transitive neighbors, (b) check fixed constraints incrementally during assignment, (c) hard-cap evaluated assignments with a logged degradation warning; add a >=5-region test that completes in bounded time.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-COG/tests/unit/test_spatial_reasoning.py -q plus a new 5-region timing test asserting completion < 2s`
- Effort: M

**GS-128 · MEDIUM · ACT · SKILL says pymdp_adapter is the sole pymdp bridge; two modules import directly**
- Motivation: SKILL.md Method Contracts declare geo_infer_act.utils.pymdp_adapter as the only production runtime bridge to inferactively-pymdp==1.0.3, but models/ecological.py imports pymdp.utils.obj_array_zeros directly at three sites with bare except fallbacks to plain zeros that would build a degenerate all-zero A/C/D model if pymdp were absent (despite pymdp being a hard dep), and utils/integration.py imports jax.random and pymdp.agent.Agent directly, constructing an Agent outside the adapter's version-validation/RNG contract.
- Evidence: `models/ecological.py:76` · `models/ecological.py:198` · `models/ecological.py:220` · `models/ecological.py:79` · `models/ecological.py:201` · `models/ecological.py:223` · `pyproject.toml:32` · `utils/integration.py:302-304` · `utils/pymdp_adapter.py:43-49`
- Bounded next step: Either (a) route both modules through pymdp_adapter (add small obj_array_zeros + build_agent facades there) — preferred, keep the exact-version pin — or (b) amend SKILL.md to state the precise exclusivity scope (only bridge for runtime perception-action steps; matrix-shaping utils and IntegrationUtils construct agents directly).
- Acceptance probe: `grep -rn "from pymdp" GEO-INFER-ACT/src/geo_infer_act --include=*.py shows only utils/pymdp_adapter.py (or SKILL wording updated to match); uv run python GEO-INFER-TEST/validate_h3_active_inference_contract.py stays green`
- Effort: M

### Risk & Earth (RISK · INSURANCE · CLIMATE · EMERGENCY)

**GS-143 · MEDIUM · INSURANCE · Location risk analysis fabricates geospatial facts**
- Motivation: _analyze_location_risk returns flood_risk computed from a constant elevation=10.0 (always medium) and coastal_risk measured as distance from a hard-coded NYC anchor (40.7, -74.0); for any property outside NYC the result is fabricated, contradicting the module ethos (SKILL.md: guarded integrations say so in results rather than guessing).
- Evidence: `underwriting/core/risk_assessment.py:422-447 (elevation constant :433, anchor math :442)`
- Bounded next step: Require elevation/water-proximity inputs (raise or mark risk_factors: {flood_risk: unavailable, reason} when absent) following the RISK api:// rejection pattern; remove the fixed anchor
- Acceptance probe: `assess_risk on a Sydney-coordinates application no longer reports flood_risk/coastal_risk as computed values`
- Effort: M

**GS-146 · MEDIUM · INSURANCE · Underwriting estate has large untested surfaces and padding tests**
- Motivation: RiskAssessmentEngine (incl. the location-risk path), UnderwritingRulesEngine/RuleEvaluator semantics, UnderwritingDecisionEngine boundaries, ComplianceEngine, DataIntegrationManager and UnderwritingReporter have no behavioral tests; tests/unit/test_models.py is import-and-enum-only padding (e.g. test_import asserts Decision is not None).
- Evidence: `tests/ tree (only identifier generation, pricing/claims, portfolio, one integration happy-path)` · `tests/unit/test_models.py:16-45` · `utils/compliance.py:335-345 (compliance evaluation is a category-keyed context lookup, untested)`
- Bounded next step: Add unit tests for rules->decision boundaries (refer/thresholds), ComplianceEngine against a declared framework, PolicyManager lifecycle (bind/endorse/cancel); delete import-only test_models.py bodies per test rubric
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-INSURANCE/tests -q passes with new rule/decision/compliance cases; grep confirms no assert X is not None body-only tests`
- Effort: M

### Environment (WATER · MARINE · FOREST · ENERGY)

**GS-160 · MEDIUM · WATER · Flood/drought composite contradicts documented equal weighting; soil double-weighted**
- Motivation: flood_drought.py:26-31 docstring says each 0/1 indicator is averaged in with the same weight and SKILL.md:71 calls it an equal-weight screening heuristic; the implementation chains means to 0.25/0.25/0.5 weights (flood_drought.py:57-64) and the drought analog double-weights soil moisture (:106-111); real risk scores differ materially from the documented model.
- Evidence: `flood_drought.py:26-31` · `flood_drought.py:57-64` · `flood_drought.py:106-111` · `SKILL.md:71` · `tests/unit/test_flood_drought.py:31-43`
- Bounded next step: Collect 0/1 indicators in a list and take one arithmetic mean; update docstring and SKILL.md:71.
- Acceptance probe: `throwaway `uv run --no-sync python` asserting flood_risk == (e+l+s)/3 on synthetic one-hot indicators (0.333 vs current 0.5 for sat-only hazard); tests only assert [0,1] range so nothing pins the old weights`
- Effort: S

**GS-166 · MEDIUM · MARINE · Sea-level projections inflated ~45x: datetime64 year arithmetic + per-index trend units** · [probe-confirmed]
- Motivation: sea_level.py:51-54 computes years_ahead from datetime64[Y] astype(int), which yields years-since-epoch (2010 -> 40, probe output confirms), not the calendar year; the trend from _calculate_trend (:64-65) is a per-time-index slope multiplied by that wrong year count; 2000-2010 record at 10 mm/yr gives 2050 rcp45 projection of 20,150 mm vs correct ~450 mm; second bug: 2D historical data raises TypeError (np.polyfit x-length 11 vs flattened 22, probe-confirmed); tests (tests/unit/test_sea_level.py:20-33) assert only ordering so the bug passes CI.
- Evidence: `sea_level.py:51-54` · `sea_level.py:64-65` · `tests/unit/test_sea_level.py:20-33`
- Bounded next step: Derive calendar year (astype('datetime64[Y]').astype(int) + 1970), normalize trend to per-year units, validate 1D input with clear error, add an absolute-value regression test (projection ~ mean + trend*years_ahead).
- Acceptance probe: `same probe must report ~450 mm for 2050 rcp45 on the test fixture; targeted pytest run of test_sea_level.py`
- Effort: M

**GS-177 · MEDIUM · ENERGY · Demand forecast: per-index-step trend applied per year; multi-dim input crashes** · [probe-confirmed]
- Motivation: energy_demand.py:43-44 `np.polyfit(np.arange(len(time)), values.flatten())` raises TypeError for (time,space) rasters (probe-confirmed: 'expected x and y to have same length'); even for 1D annual data the slope is per-index-step multiplied by forecast year count (:87) - silently wrong for monthly data.
- Evidence: `energy_demand.py:43-44` · `energy_demand.py:87`
- Bounded next step: Validate 1D annual input (derive slope per year from the time coordinate) or resample; add dimension validation with clear errors.
- Acceptance probe: `2D input raises clear ValueError post-fix; annual-input output unchanged`
- Effort: M

### Human geography (PLACE · TRANSPORT · AG · CIV · BIO)

**GS-190 · MEDIUM · PLACE · detect_trend hardcodes r_squared=1.0 and significant=True for the TIME backend**
- Motivation: module_bridge.py:247-248 returns 'r_squared': 1.0, 'significant': True whenever the GEO-INFER-TIME backend responds, regardless of actual fit quality; only trend_strength is real (244-246). The built-in numpy path computes both honestly (module_bridge.py ~262-285). Downstream run_full_analysis temporal results therefore claim perfect, significant trends for every series — a wrong-numbers-in-reports bug, not just a stub.
- Evidence: `module_bridge.py:247-248` · `module_bridge.py:244-246` · `module_bridge.py:~262-285`
- Bounded next step: Map the real fields from the TIME result (or derive r² from the returned series) and set significant from a threshold on the actual value; add a unit test asserting r_squared is not constant 1.0 with _HAS_TIME monkeypatched on.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_place.core.module_bridge import PlaceTemporalAnalyzer as P; import geo_infer_place.core.module_bridge as m; m._HAS_TIME=True; p=object.__new__(P); import types; p._analyzer=type('A',(),{'detect_trend':lambda s,ts:{'trend_direction':'increasing','trend_strength':0.1}})(); print(p.detect_trend([1,2,3])['r_squared'])" → must not be 1.0`
- Effort: M

**GS-194 · MEDIUM · AG · SKILL.md claims no external-service code while AgriculturalAPI is a live HTTP client**
- Motivation: SKILL.md:106-107 states AG contains no external-service code, but AgriculturalAPI (exported in __all__, geo_infer_ag/__init__.py) performs network calls: base_url = "https://api.geo-infer.ag" (agricultural_api.py:15), _request GETs it (agricultural_api.py:~71-85), and get_crop_data/get_soil_data/get_weather_forecast all hit that host. The domain does not resolve, so every documented network method is a guaranteed RuntimeError wrapper — and the Guidelines sentence is false either way.
- Evidence: `SKILL.md:106-107` · `geo_infer_ag/__init__.py` · `agricultural_api.py:15` · `agricultural_api.py:~71-85`
- Bounded next step: Either correct SKILL.md ("AG ships an AgriculturalAPI HTTP client targeting api.geo-infer.ag; requires a reachable service") and note it in Key Imports, or descope/delete the network half of AgriculturalAPI if the service was never real. Check whether tests cover only analyze_crop_yield (offline path).
- Acceptance probe: `grep -n "geo-infer.ag" GEO-INFER-AG/SKILL.md GEO-INFER-AG/src/geo_infer_ag/api/agricultural_api.py — SKILL.md must acknowledge the client after the fix`
- Effort: M

**GS-201 · MEDIUM · BIO · api/, core/, utils/ lack __init__.py; wheel install drops them, breaking SKILL.md imports**
- Motivation: Glob of GEO-INFER-BIO/src shows __init__.py only at geo_infer_bio/ root; api/ (rest_api.py, graphql_api.py), core/ (sequence_analysis.py), utils/ (validation.py, visualization.py) have none — while PLACE/AG/CIV/TRANSPORT have __init__.py in every subpackage. packages.find where=["src"] (pyproject) uses find_packages, which ships only regular packages: a built wheel omits all three subdirectories, so from geo_infer_bio.api.rest_api import app (tests/unit/test_rest_api.py:15, SKILL.md:73-74) and from ..core.sequence_analysis import SequenceAnalyzer resolve in-repo only via namespace-package fallback. In-repo tests cannot detect this; only a built-distribution check can.
- Evidence: `pyproject.toml (packages.find where=["src"])` · `tests/unit/test_rest_api.py:15` · `SKILL.md:73-74`
- Bounded next step: Add the three __init__.py files (root pattern: """GEO-INFER-BIO <subpkg> package.""" + re-exports), then add a packaging smoke test (or CI step) that uv build + install into a temp venv and python -c "from geo_infer_bio.api.rest_api import app" passes.
- Acceptance probe: `uv build GEO-INFER-BIO && unzip -l dist/geo_infer_bio-*.whl | grep -c "api/__init__.py" ≥ 1 (currently 0)`
- Effort: M — Note: re-tiered per rubric: majors need external resources or release-scale; bounded 3-file fix; release-blocking flag applies at the next wheel build

### Infrastructure & operations (API · APP · GIT · OPS · LOG · SEC · COMMS)

**GS-212 · MEDIUM · APP · Test gap: widget and config-form public surfaces have zero tests**
- Motivation: SKILL.md lists AgentWidget/WebAgentWidget and AgentConfigForm as core capabilities, but tests/ cover only factory, configuration, api, interface, visualization, bdi; agent_widget.py (456 lines: _update_loop, callback register/unregister in initialize/set_agent, WebAgentWidget HTML rendering) and agent/agent_config_form.py are entirely untested.
- Evidence: `SKILL.md` · `components/agent_widget.py` · `components/agent/agent_config_form.py`
- Bounded next step: Unit tests for initialize/shutdown/set_agent lifecycle with an AgentManager stub, WebAgentWidget HTML output for a known state, and AgentConfigForm valid/invalid payloads.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-APP/tests/unit -k widget -q (new files) passes without sleeps (update interval injectable)`
- Effort: M

**GS-214 · MEDIUM · GIT · Test gap: SKILL-documented advanced surfaces have zero tests**
- Motivation: SKILL.md lists multi-platform discovery as a core capability; core/README.md publishes advanced_git.py (SubmoduleManager/CherryPickManager/RebaseManager/AdvancedGitOperations, 930 lines) and multi_platform_api.py (GitLabAPI/BitbucketAPI/LocalGitAPI/MultiPlatformAPI). Grep of tests/ shows zero imports for advanced_git, multi_platform_api, observability.py (~980 lines), performance.py, secure_serialization.py, logging_utils.py. Submodule/merge operations can destroy local repo state — highest-risk untested code in the module.
- Evidence: `src/geo_infer_git/core/README.md:18-37` · `advanced_git.py` · `multi_platform_api.py` · `observability.py` · `performance.py` · `secure_serialization.py` · `logging_utils.py`
- Bounded next step: Conftest fixture git repo with a submodule; round-trip tests for SubmoduleManager load/init/update/status, CherryPick/Rebase conflict paths; MultiPlatformAPI request construction against a stubbed HTTP layer (mirror test_github_api.py).
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-GIT/tests -k 'advanced or multi_platform' -q (new files) passes; all subprocess work confined to tmp_path`
- Effort: L

**GS-215 · MEDIUM · GIT · Robustness: DistributedCoordinator binds network ports in __init__ and is untestable**
- Motivation: DistributedCoordinator calls _start_services() from __init__; coordinator/master roles immediately bind a UDP discovery socket and a TCP coordination listener plus heartbeat/scheduler threads. Documented construction (create_distributed_coordinator()) has network side effects; any port conflict fails the constructor. Zero tests exist for the 879-line module.
- Evidence: `core/distributed_coordinator.py:153` · `core/distributed_coordinator.py:225` · `core/distributed_coordinator.py:283-286`
- Bounded next step: Split into pure __init__ + explicit start()/stop() lifecycle; add unit tests for NodeInfo/JobInfo bookkeeping, message dispatch, heartbeat timeouts.
- Acceptance probe: `Constructing DistributedCoordinator(role='worker') binds no ports (lsof check); pytest for job scheduling passes on the refactored lifecycle`
- Effort: M

**GS-219 · MEDIUM · OPS · Dead code: utils/error_handling.py is an unimported, untested 496-line framework**
- Motivation: utils/error_handling.py (GeoInferError hierarchy, classify_error, retry machinery, handle_error) claims it can be used across all GEO-INFER modules; grep across the workspace finds zero importers of geo_infer_ops.utils.error_handling and no tests. It also shadows the builtin PermissionError (line 130) then checks isinstance(error, (FileNotFoundError, PermissionError, OSError)) at line 264 where the name resolves to the custom class — harmless today only because OSError subsumes the builtin; a latent trap on reorder.
- Evidence: `utils/error_handling.py:4-8` · `utils/error_handling.py:130` · `utils/error_handling.py:264`
- Bounded next step: One cutover — delete the module (and its cross-module docstring promise) or adopt it where OPS modules raise bare exceptions; add tests for whatever survives.
- Acceptance probe: `grep -rn 'error_handling' --include='*.py' GEO-INFER-OPS GEO-INFER-TEST shows no orphan module (or importers with passing tests)`
- Effort: M

**GS-220 · MEDIUM · OPS · Duplication: three parallel get_logger/LoggingContext implementations**
- Motivation: utils/logger.py (get_logger + LoggingContext), core/logging.py (get_logger), and utils/shared_logging.py (get_logger + LoggingContext) all provide structlog accessors/context managers; SKILL.md imports utils.logger.get_logger, app.py and tests import core.logging.get_logger and shared_logging. Three names for one concept already drift (utils/logger.LoggingContext lacks the defensive get_contextvars guard shared_logging has).
- Evidence: `utils/logger.py:24-71` · `core/logging.py:21-31` · `utils/shared_logging.py:95-130` · `utils/shared_logging.py:182-220` · `SKILL.md:31` · `tests/test_logging.py:13`
- Bounded next step: Keep shared_logging as the single app-level config entry and core/logging.get_logger as the passive accessor; re-export from utils/logger.py or delete it and migrate callers; update SKILL.md imports.
- Acceptance probe: `grep -rn 'class LoggingContext' GEO-INFER-OPS/src shows exactly one definition; uv run --no-sync python -m pytest GEO-INFER-OPS/tests/test_logging.py GEO-INFER-OPS/tests/unit/test_logging_hygiene.py -q passes`
- Effort: M

**GS-222 · MEDIUM · LOG · API truth: /transport network and traffic endpoints are unreachable by construction**
- Motivation: api/transport.py defines NetworkMetricsRequest.network_id, but get_network_metrics ignores it and calls analyzer.calculate_network_metrics(), which raises ValueError (Network must be loaded before calculating metrics) unless load_network was called; same for critical-links, simulate (which also requires set_time_periods), and congestion. The router exposes no endpoint that loads a network or sets time periods, so every /transport/network/* and /traffic/* call deterministically returns 400; network_id is accepted-and-discarded, actively misleading API consumers.
- Evidence: `api/transport.py:94-100` · `core/transport.py:371-372` · `core/transport.py:417-418` · `core/transport.py:637-641` · `core/transport.py:696-697`
- Bounded next step: Add POST /transport/network/load (+ time-periods) endpoints backing the singletons, or make network_id select among loaded networks; minimum fix: remove the dead field and document the precondition per route.
- Acceptance probe: `With the fix, a targeted TestClient request to /transport/network/metrics returns 200 against a loaded fixture gpickle (or 400 with a body naming the missing network instead of ignoring the field)`
- Effort: M

**GS-224 · MEDIUM · SEC · Test gap: integrated_security, physical_security, and the CLI have zero tests**
- Motivation: core/integrated_security.py (953 lines: correlation rules, incident lifecycle, orchestration threads), core/physical_security.py (~700 lines), and cli.py (714 lines, the documented geo-infer-sec command surface) have no test imports in tests/ (grep-verified; only cognitive/digital/compliance get partial coverage via test_acceptance_sec.py). _correlate_cross_domain_threat (~line 283) swallows correlation errors with a broad except Exception — exactly the logic needing characterization tests.
- Evidence: `core/integrated_security.py` · `core/physical_security.py` · `cli.py` · `core/integrated_security.py:283`
- Bounded next step: Unit tests for IntegratedSecurityManager correlation-rule matching with synthetic PhysicalThreat/SecurityAlert inputs (threads disabled), PhysicalSecurityManager sensor/threshold logic, and CLI handlers with tmp_path files.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-SEC/tests -k 'integrated or physical or cli' -q (new files) passes deterministically`
- Effort: L

**GS-226 · MEDIUM · COMMS · Stub behavior: WebSocket real-time broadcasting is never wired to the system**
- Motivation: api/websocket_api.py _register_broadcasters defines three broadcast closures then states in a real implementation they would be registered with the system; the closures are discarded, so broker-published messages/notifications/events never reach WebSocket clients. get_stats hardcodes broadcasts_sent: 0. SKILL.md lists the WebSocket API as a core capability and the module docstring promises real-time communication. Additionally WebSocketManager.broadcast_message (line 87) and RealTimeMessageBroadcaster.broadcast_message (544) call asyncio.create_task from sync methods — RuntimeError when invoked outside a running loop (e.g. a broker worker thread), so even manual wiring needs a loop-aware handoff; TestEstate-lane COMMS evidence likewise flags the WebSocket broadcast stub as never wired to the broker.
- Evidence: `api/websocket_api.py:637-653` · `api/websocket_api.py:700-704` · `websocket_api.py:1-5` · `websocket_api.py:87` · `websocket_api.py:544`
- Bounded next step: Register the three callbacks at the system's message/notification/event emission points (or delete _register_broadcasters and fix SKILL/docs to say manual use), and make sync broadcast entrypoints loop-safe (get_running_loop guard + run_coroutine_threadsafe fallback).
- Acceptance probe: `Integration test starting WebSocketAPIManager + GeospatialCommunicationSystem, publishing via the broker, asserting a connected websockets client receives it; grep -n 'Would track in real implementation|For now, they are available' returns nothing`
- Effort: M — Note: absorbs TestEstate-lane COMMS evidence

**GS-227 · MEDIUM · COMMS · Test gap: events.py (938 lines), collaboration.py, websocket_api.py have zero tests**
- Motivation: SKILL.md documents EventManager/EventScheduler and CollaborationManager as core capabilities; tests/ imports cover messaging, notifications, channels, streaming, spatial_routing, rest_api, email_providers, validation — none import core.events, core.collaboration, or api.websocket_api (grep over tests/). EventManager runs a background thread, priority queue, spatial index, and subscription filters — high-risk untested concurrency. TestEstate-lane COMMS evidence adds: events/collaboration/websocket subsystems are never covered by tests, and the events persistence layer is dead config.
- Evidence: `core/events.py` · `core/collaboration.py` · `api/websocket_api.py`
- Bounded next step: Unit tests for publish to subscribe delivery (event-type + geospatial filter), unsubscribe semantics, EventFilter geospatial/temporal filters, EventScheduler recurring publication with a clock stub; CollaborationManager session join/leave flows.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-COMMS/tests -k 'events or scheduler or collaboration' -q (new files) passes without wall-clock sleeps`
- Effort: L — Note: absorbs TestEstate-lane COMMS evidence

### Society (ECON · EDU · HEALTH · METAGOV · NORMS · ORG · PEP)

**GS-235 · MEDIUM · ECON · Okun's-law sign inversion in assess_fiscal_policy** · [probe-confirmed]
- Motivation: employment_change = gdp_change / okun_coefficient with default okun_coefficient = -2.0, so a positive GDP impact produces a NEGATIVE employment impact (employment_impact of -7.5 vs gdp_impact +150,000 for a +10 spending shock; probe-confirmed). Okun's law is du = -beta*dGDP; the code divides by the already-negative coefficient instead of negating. Additionally okun_coefficient=0 raises ZeroDivisionError (no guard).
- Evidence: `GEO-INFER-ECON/src/geo_infer_econ/core/policy_engine.py:158-159` · `GEO-INFER-ECON/tests/unit/test_policy_engine.py:61-73`
- Bounded next step: Change to employment_change = -gdp_change / abs(okun_coefficient) (guard okun_coefficient == 0 with ValueError), fix the test to assert sign correctness.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_econ.core.policy_engine import *; e=PolicyAnalysisEngine(); e.add_baseline_data('gdp',{'r':1e6}); s=PolicyScenario(name='s',description='d',policy_type=PolicyType.FISCAL,parameters={'government_spending_change':10.0}); i=e.assess_fiscal_policy(s); assert i.employment_impact['r']>0"`
- Effort: S

**GS-242 · MEDIUM · HEALTH · 564-line CLI has zero tests, and validate silently succeeds on failure**
- Motivation: run_validation catches Exception, logs "Validation failed: {e}", and returns without re-raising; main() only exits 1 on raised exceptions, so geo-infer-health validate --input corrupt.geojson exits 0. --schema and --type are parsed but never referenced in run_validation — advertised behavior that doesn't exist. No tests/test_cli* exists at all.
- Evidence: `GEO-INFER-HEALTH/src/geo_infer_health/cli.py:540-560` · `GEO-INFER-HEALTH/src/geo_infer_health/cli.py:159` · `GEO-INFER-HEALTH/src/geo_infer_health/cli.py:160-163`
- Bounded next step: Re-raise in run_validation (or sys.exit(1)), either use or remove --schema/--type; add a CliRunner-style test for the exit-code contract.
- Acceptance probe: `uv run --no-sync python -m geo_infer_health.cli validate --input /nonexistent -> currently exit 0; should be non-zero`
- Effort: S

**GS-245 · MEDIUM · METAGOV · Governance structure IDs collide after deletion, silently overwriting live structures** · [probe-confirmed]
- Motivation: governance_id = f"gov_{len(self.governance_structures)}" — probe-confirmed: create -> gov_0, create -> gov_1, delete gov_0, create -> gov_1 again; the second governance_structures[governance_id] = structure assignment replaces the still-live gov_1 record with no error. Tests cover delete -> 404 but never post-delete re-creation.
- Evidence: `GEO-INFER-METAGOV/src/geo_infer_metagov/api/rest_api.py:82` · `GEO-INFER-METAGOV/src/geo_infer_metagov/api/rest_api.py:100` · `GEO-INFER-METAGOV/tests/unit/test_api.py:87-90`
- Bounded next step: Use uuid.uuid4() or a monotonic self._next_id counter for ID generation; add a regression test (create, create, delete first, create -> assert three distinct IDs and original data intact).
- Acceptance probe: `the exact probe already run: ids printed gov_0 gov_1 gov_1 pre-fix; post-fix third id must differ`
- Effort: S

**GS-249 · MEDIUM · PEP · Failed onboarding leaves a partial employee record in the shared store**
- Motivation: methods.py appends the new Employee to _employees_db BEFORE the benefits/learning client invocations; the broad except Exception -> return False means ANY downstream failure (client raise, non-callable benefits_client -> the module's own TypeError) leaves the unprocessed employee in the process-wide shared store the FastAPI layer also serves, despite the contract implying a clean abort. The TypeError guards are effectively dead: they raise inside the try and get swallowed into return False.
- Evidence: `GEO-INFER-PEP/src/geo_infer_pep/methods.py:164` · `GEO-INFER-PEP/src/geo_infer_pep/methods.py:177-190` · `GEO-INFER-PEP/src/geo_infer_pep/methods.py:205-207` · `GEO-INFER-PEP/src/geo_infer_pep/methods.py:178` · `GEO-INFER-PEP/src/geo_infer_pep/methods.py:189`
- Bounded next step: Move the append after client invocations (or roll back the append in the except path), and let TypeError escape the try block.
- Acceptance probe: `seed candidate with benefits_client=lambda e: (_ for _ in ()).throw(RuntimeError()); run workflow -> assert returns False AND len(pep_data_manager.employees) unchanged`
- Effort: S

### Agents & meta (AGENT · AI · ANT · ART · SIM · IOT · REQ)

**GS-264 · MEDIUM · AI · core/training: hyperparameter_search silent zero-scores and random_state ctor crash**
- Motivation: hyperparameter_search hard-codes model_class(random_state=random_state, **params), raising TypeError for sklearn estimators lacking that parameter (KNeighborsClassifier, SVR, LinearRegression). Worse, score_key only resolves when scoring is exactly accuracy or r2; any other documented string falls through to .get(score_key, 0.0) so every candidate scores 0.0, best_params becomes the first combination, and the search reports best_score 0.0 with no error. The result dict is then trusted downstream (sorted, returned as best).
- Evidence: `src/geo_infer_ai/core/training.py:439` · `src/geo_infer_ai/core/training.py:444` · `src/geo_infer_ai/core/training.py:429`
- Bounded next step: Instantiate via a guarded ctor (inspect signature / try-with-and-without random_state), and raise ValueError for scoring names outside the supported {accuracy, r2} set (or implement the mapping).
- Acceptance probe: `uv run --no-sync python -c "import numpy as np; from sklearn.neighbors import KNeighborsClassifier; from geo_infer_ai import ModelTrainer; X=np.random.default_rng(0).random((40,3)); y=(X[:,0]>0.5).astype(int); r=ModelTrainer().hyperparameter_search(KNeighborsClassifier, {'n_neighbors':[3,5]}, X, y)" (currently TypeError) and the same with scoring='f1' (currently all-zero best_score) — both must either work correctly or raise a clear error.`
- Effort: M

**GS-270 · MEDIUM · ANT · analysis: patterns.py/environmental.py single-file giants resisting focused review**
- Motivation: patterns.py (74.3KB), environmental.py (60.7KB), aco.py (48.7KB), population.py (43.9KB) are multi-class modules where a focused change requires wading through thousands of lines; IMPLEMENTATION_STATUS.md documents the behavior surface well, which makes refactoring safe, but the current files are the module's main maintainability tax.
- Evidence: `src/geo_infer_ant/analysis/patterns.py (74.3KB)` · `src/geo_infer_ant/applications/environmental.py (60.7KB)` · `src/geo_infer_ant/algorithms/aco.py (48.7KB)` · `src/geo_infer_ant/core/population.py (43.9KB)`
- Bounded next step: Highest-value first cut — split analysis/patterns.py along its public class boundaries into an analysis/patterns/ package (or extract pure helper functions into analysis/_metrics_helpers.py) with re-export shims removed in the same change (clean cutover, no aliases).
- Acceptance probe: `uv run python GEO-INFER-TEST/run_unified_tests.py --module ANT stays green after the split; grep confirms no consumer imported the moved internals directly.`
- Effort: M

**GS-274 · MEDIUM · SIM · core: SimulationConfig.parallel_execution is dead config**
- Motivation: parallel_execution: bool = False has no other reference anywhere in GEO-INFER-SIM/src — the engine is strictly sequential and the field misleads callers into expecting parallel stepping (checkpoint/export code never consults it).
- Evidence: `src/geo_infer_sim/core/simulation_engine.py:40` · `src/geo_infer_sim/core/simulation_engine.py:~300-433`
- Bounded next step: Remove the field (clean cutover; no caller in-repo sets it — grep confirms) or implement thread-pool stepping for stateless step_funcs; removal is the honest option given the SKILL.md contract centers on deterministic single-stream runs.
- Acceptance probe: `grep for parallel_execution returns zero src matches; uv run python -m pytest GEO-INFER-SIM/tests/ -v green.`
- Effort: S — Note: Report effort: S (remove) / M (implement).

**GS-275 · MEDIUM · SIM · module_simulations: 2,633-line monolith of ~20 near-identical toy simulate methods**
- Motivation: module_simulations.py is 89KB/2,633 lines; each simulate_<module> method hand-rolls the same SimulationConfig + SimulationEngine + history-list + step_func + result-dict boilerplate, differing only in step math and result keys; the module docstring itself warns these are toy smoke-tests. Adding the 45th module means copying ~80 lines again.
- Evidence: `src/geo_infer_sim/module_simulations.py:66-165` · `src/geo_infer_sim/module_simulations.py:167-237` · `src/geo_infer_sim/module_simulations.py:239-300` · `src/geo_infer_sim/module_simulations.py:1-24`
- Bounded next step: Extract one _run_toy_model(step_fn, *, result_keys, extra) helper performing engine setup/run/history extraction; each public method reduces to its step function + result mapping. No public behavior change (same keys, same histories).
- Acceptance probe: `uv run python -m pytest GEO-INFER-SIM/tests/unit/test_module_simulations_contracts.py GEO-INFER-SIM/tests/test_simulation_engine_integration.py -q green; line count drops materially; a quick script asserts simulate_act/simulate_ag outputs are byte-identical pre/post on a fixed seed.`
- Effort: M

**GS-277 · MEDIUM · IOT · config: geo_infer_iot.config (468 lines) is an entirely unwired speculative surface**
- Motivation: config/__init__.py defines IoTConfig (storage backends influxdb/postgis/minio, security JWT/AES-256-GCM, ML autoencoder/anomaly config, API/visualization schemas) plus ConfigurationManager; grep across src, tests, examples, deployment finds zero imports of geo_infer_iot.config or ConfigurationManager(. The runtime does not consume it and it is untested — exactly the planned, fake surface the module's own AGENTS.md rule prohibits.
- Evidence: `src/geo_infer_iot/config/__init__.py:~44-380` · `src/geo_infer_iot/config/__init__.py:~383-468`
- Bounded next step: Delete the config package (the real configuration lives in IoTDataIngestion/IoTSystem constructor dicts and config/example.yaml) or wire IoTSystem to load IoTConfig — deletion is the honest cutover given nothing consumes it.
- Acceptance probe: `Grep returns zero references post-removal; uv run python -m pytest GEO-INFER-IOT/tests/ -q green; uv run --no-sync python -c "import geo_infer_iot" still imports (config is not in __all__).`
- Effort: S — Note: Report effort: S (delete) / M (wire).

### Platform docs & test (TEST · INTRA · EXAMPLES)

**GS-290 · MEDIUM · TEST · Script validators almost untested; test_validators*.py cover the wrong layer**
- Motivation: tests/unit/test_validators.py and test_validators_parametric.py import only DataQualityValidator/SpatialValidator/IoTValidator/BayesianValidator/PerformanceValidator/QualityController from geo_infer_test.core.validators and never exercise the repo-contract script validators, despite the parametric file's docstring claiming to exercise 'every GEO-INFER-TEST validator'; dedicated script-validator tests exist only for validate_packaging.py, validate_repo_contracts.py, validate_skills.py, validate_h3_active_inference_contract.py. Untested: validate_test_contracts.py, validate_documentation.py, validate_model_contracts.py, validate_act_geospatial_contract.py, validate_act_script_orchestration.py, validate_logging_hygiene.py, validate_gnn_interchange.py, check_coverage_floor.py, measure_module_coverage.py, and all 8 *_metric.py scripts - several of which are CI gates. No dead validators found: every script is wired (CI workflows, run_unified_tests.py --h3-migration, or autoresearch.sh:20-28).
- Evidence: `GEO-INFER-TEST/tests/unit/test_validators.py:1-19` · `GEO-INFER-TEST/tests/unit/test_validators_parametric.py:1-19` · `.github/workflows/ci.yml:100-115` · `GEO-INFER-TEST/autoresearch.sh:20-28`
- Bounded next step: Add tmp-repo-tree fixture tests for the two highest-leverage gates first (validate_test_contracts.py --strict, validate_documentation.py --strict), then check_coverage_floor.py (happy path + baseline-missing path).
- Acceptance probe: `grep -rln 'validate_test_contracts\|validate_documentation\|check_coverage_floor' GEO-INFER-TEST/tests/ returns 0 hits today`
- Effort: M

**GS-291 · MEDIUM · INTRA · Hub-wide phantom API surface in docs hub; validate_documentation blind to import drift**
- Motivation: The hub's core value (SKILL.md:21 'Consolidated API documentation for all 45 modules') is undermined by imports of symbols no module defines: verified-phantom top-level symbols include geo_infer_space.SpatialAnalyzer, geo_infer_data.DataManager, geo_infer_api.APIManager, geo_infer_iot.IoTManager, geo_infer_ag.AgriculturalFramework, geo_infer_agent.MultiAgentSystem, geo_infer_ai.AIEngine, geo_infer_org.OrganizationalMapper; wrong-path imports include geo_infer_bayes.core.gaussian_process/core.samplers/core.inference.GaussianProcess (real GP class lives at geo_infer_bayes/__init__.py:126), geo_infer_math.core.statistics (real API is geo_infer_math.core.spatial_statistics.MoranI), geo_infer_space.io.converters, geo_infer_data.datasets, geo_infer_time.temporal_analysis/patterns, geo_infer_act.models.GenerativeModel + geo_infer_act.inference (GenerativeModel is core.generative_model), geo_infer_space.models.GeoDataFrame, geo_infer_act.EnvironmentalMonitoringModel, and geo_infer_space import indexing/environment/transformations; a repo-wide class-definition grep for the phantom set across those 8 modules' src returns exactly one hit (EnvironmentalHealthAnalyzer, via an unverified top-level path); meanwhile validate_documentation.py's 31-page AUTHORITATIVE_DOCS set excludes the hub pages where the phantom imports live (INTRA architecture/, advanced/, examples/, guides/, modules/, integration/, developer_guide/autonomous_agent_guide.md) and its 5 FORBIDDEN_CURRENT_STATE_PATTERNS are textual regexes that cannot catch API drift, while README:33-35 claims the validator covers the maintained docs hub - so the enforcement gap this item owns is an import-truth doc checker. GeoCore lane cross-confirms the missing SPACE methods. DocsTruth lane flagged GEO-INFER-INTRA/docs/user_guide/cookbook.md (18/18 phantom imports, no descope banner).
- Evidence: `advanced/performance_optimization.md:25` · `advanced/custom_models.md:293` · `materiality/geo-infer-materiality.md:93,95` · `modules/geo-infer-act.md:229,340` · `modules/geo-infer-ag.md:99` · `modules/geo-infer-agent.md:107` · `modules/geo-infer-ai.md:103` · `modules/geo-infer-api.md:84` · `guides/MODULE_INTEGRATION_GUIDE.md:23,27,31,35,43,51` · `guides/ENVIRONMENTAL_MONITORING_INTEGRATION.md:25` · `advanced/scaling_guide.md:72` · `examples/agricultural_applications.md:205` · `examples/climate_modeling.md:438` · `examples_gallery.md:83,236` · `integration/geo_infer_modules.md:66,135` · `architecture/cross_module_interaction.md:12,16,32,36,40` · `geospatial/data_formats/index.md:32` · `developer_guide/autonomous_agent_guide.md:58,70` · `GEO-INFER-SPACE/src/geo_infer_space/__init__.py:33-50` · `geo_infer_bayes/__init__.py:126` · `geo_infer_data/__init__.py:22-24` · `geo_infer_time/__init__.py:11-27` · `GEO-INFER-TEST/validate_documentation.py:29-60` · `GEO-INFER-TEST/validate_documentation.py:62-68` · `GEO-INFER-TEST/README.md:33-35` · `GEO-INFER-INTRA/SKILL.md:21` · `GEO-INFER-INTRA/docs/user_guide/cookbook.md (DocsTruth lane: 18/18 phantom imports, no descope banner)`
- Bounded next step: Build the import-truth doc checker: extract 'from geo_infer_<x> import <y>' / 'from geo_infer_<x>.<mod> import <y>' lines from GEO-INFER-INTRA/docs/**, import the package, and assert the symbol resolves, with a small explicit allowlist for legacy narrative pages; add architecture/, guides/, advanced/ index pages to AUTHORITATIVE_DOCS incrementally; then sweep page-by-page starting with guides/MODULE_INTEGRATION_GUIDE.md + modules/geo-infer-*.md (most-linked), rewriting each snippet against real exports (e.g. SpatialIndexingInterface, MultiSourceDataIngestion, DataAPI, ActiveInferenceModel)
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_space import SpatialAnalyzer" -> ImportError, while uv run --no-sync python GEO-INFER-TEST/validate_documentation.py --strict exits 0 (proving the gate is blind to the drift); after the sweep the checker extracts 0 phantom imports from GEO-INFER-INTRA/docs`
- Effort: L

**GS-300 · MEDIUM · EXAMPLES · Missing-dependency path advertises [full] extra that 42 of 43 modules lack**
- Motivation: examples/module_orchestrators/_lib.py:33 defaults requires_extra: str = 'full' and :56 emits the remediation 'requires': 'geo-infer-<module>[full]' on ImportError. Only GEO-INFER-PLACE/pyproject.toml:53 defines a 'full' optional group; e.g. GEO-INFER-SPACE defines dev/docs/optional/viz/all (pyproject.toml:48-95). Every non-PLACE orchestrator that hits an ImportError tells users to install a nonexistent extra - the graceful-degradation path is itself misleading. No orchestrator overrides requires_extra (repo grep: only _lib.py mentions it).
- Evidence: `GEO-INFER-EXAMPLES/examples/module_orchestrators/_lib.py:33` · `GEO-INFER-EXAMPLES/examples/module_orchestrators/_lib.py:56` · `GEO-INFER-PLACE/pyproject.toml:53` · `GEO-INFER-SPACE/pyproject.toml:48-95`
- Bounded next step: Change the default to 'all' (present in SPACE and most modules) or accept a per-module mapping verified against each pyproject's optional groups; add a test asserting the emitted extra exists in the module's pyproject for at least the sampled orchestrators.
- Acceptance probe: `grep -c '^full = \[' GEO-INFER-*/pyproject.toml returns 1 today; after the fix, run any orchestrator with a stripped env and confirm the 'requires' field names an extra that exists in that module's pyproject`
- Effort: S

**GS-301 · MEDIUM · EXAMPLES · 43 orchestrator demo scripts never executed by any test or gate**
- Motivation: tests/integration/test_example_orchestration.py:1-6 is the entire integration suite (asserts ExecutionStrategy enum values); unit tests cover the ModuleOrchestrator/models classes, not the scripts; orchestrator_coverage_metric.py (wired via autoresearch.sh:14-17) only measures existence of scripts/run_orchestrator.py per ledger-named module. The scripts themselves are high-quality and API-aligned (sampled 5: SPACE, RISK, BAYES, HEALTH, TEST - imports verified against real APIs incl. SpatialIndexingInterface methods at core/spatial_indexing.py, RiskModel.calculate_risk/run_monte_carlo at core/risk_models.py:98,165, MCMCSampler + BayesianModel abstract methods at mcmc.py:44-71 and models/base.py:12-176, DiseaseHotspotAnalyzer, QualityController.run_comprehensive_validation at src/geo_infer_test/core/validators.py:904), but nothing would catch the next API drift in the other 38 (the same class of drift now live in INTRA's SKILL.md tessellate example).
- Evidence: `GEO-INFER-EXAMPLES/tests/integration/test_example_orchestration.py:1-6` · `GEO-INFER-TEST/autoresearch.sh:14-17` · `core/spatial_indexing.py` · `core/risk_models.py:98,165` · `mcmc.py:44-71` · `models/base.py:12-176` · `src/geo_infer_test/core/validators.py:904`
- Bounded next step: Add an integration test that runs a bounded, deterministic subset (e.g. 5-8 fast orchestrators: SPACE, MATH, TIME, LOG, EXAMPLES) via subprocess asserting exit 0 and JSON status == 'ok'; optionally a manual/local-only sweep marker for the full set.
- Acceptance probe: `uv run python GEO-INFER-EXAMPLES/examples/module_orchestrators/SPACE/scripts/run_orchestrator.py exits 0 with '"status": "ok"' (verified pattern via _lib exit codes); the new test codifies it`
- Effort: M

## Minor items (141)

### Package-level surfaces

**GS-001 · MINOR · CI-GATES · ci.yml artifact order wipes performance reports before upload, mislabeling artifacts**
- Motivation: ci.yml runs system tests after performance tests but retains reports in the reverse order, while every category invocation wipes the shared results dir (run_unified_tests.py ensure_results_dir(clean=True)); by the time 'Retain performance test reports' runs, the dir holds only system output. The performance artifact then uploads system files under its name with if-no-files-found: error never firing; when the performance category is the failing one, its summary.json (carrying stdout_tail/stderr_tail per module) is exactly what gets destroyed. Unit and integration are unaffected only because their retention steps happen to precede the next wipe.
- Evidence: `ci.yml:192-219` · `run_unified_tests.py:302-303` · `run_unified_tests.py:489-517`
- Bounded next step: Move 'Retain performance test reports' directly after 'Run performance tests' (matching the unit/integration pattern); optionally give each category its own subdir (RESULTS_DIR/category/) in run_unified_tests.py so wipe order can never corrupt uploads.
- Acceptance probe: `force a performance failure, run the two steps in order, download both artifacts, and assert the performance artifact contains *_performance_results.xml files; pre-fix it contains only *_system_results.xml`
- Effort: S

**GS-004 · MINOR · CI-GATES · Coverage floor gate passes while suite is red; SHA fallback degrades to empty diff**
- Motivation: measure_module_coverage treats pytest returncode 0 or 1 as success, so a module whose tests fail still returns status "measured" with partial coverage and check_coverage_floor prints measured X% vs floor Y% -> ok, silently weakening the contract to "coverage of whatever ran before the failure". Failure philosophy is also inconsistent between the two diff-scoped steps in ci.yml: code-quality falls back to the empty tree (checks everything) while coverage-floor falls back to HEAD, where base==head yields an empty diff and an unconditional exit 0.
- Evidence: `measure_module_coverage.py:88-96` · `check_coverage_floor.py:87-101` · `ci.yml:73-78` · `ci.yml:146-151` · `check_coverage_floor.py:22-46, 60-63`
- Bounded next step: In check_coverage_floor.py, record `pytest_rc` in the verdict and fail any module whose measurement ran with rc==1; align the base fallback with the empty-tree approach (or fail closed when base is unresolvable).
- Acceptance probe: `with a temporarily failing test in one module, `uv run --with pytest-cov python GEO-INFER-TEST/check_coverage_floor.py --base HEAD~1 --head HEAD --modules <module>` must exit 1 post-fix (today it prints `-> ok`)`
- Effort: S

**GS-006 · MINOR · CI-GATES · Real 45-wheel build first executes at release time, never in PR CI**
- Motivation: The only real-workspace invocation of build_package_wheels.py is release.yml; PR CI checks packaging configuration only (validate_packaging --strict) and import-probes.yml exercises the builder logic against fixture wheels by design. A pyproject change that breaks `uv build` for one member (bad package-data globs, missing readme path, dist-info metadata disagreement — all caught by validate_wheel_contents) passes every PR check and first surfaces on a v* tag.
- Evidence: `release.yml:63-67` · `ci.yml:100` · `import-probes.yml:37-44` · `build_package_wheels.py:~130`
- Bounded next step: Add a build-smoke step (its own job or in the validate job from item 5): `uv run python GEO-INFER-TEST/build_package_wheels.py --outdir /tmp/dist --no-verify-equivalent` (i.e. without --verify), artifacts discarded; per-wheel timeout already bounds it at 300s (build_package_wheels.py:~130).
- Acceptance probe: `a PR touching one member's `[project]` table shows that member's wheel filename in the step log; today `grep -n build_package_wheels .github/workflows/ci.yml` returns nothing`
- Effort: M

**GS-007 · MINOR · CI-GATES · Unfiltered workflow triggers cause duplicate full runs and docs-only PR churn**
- Motivation: gnn-interchange.yml triggers on push:[main] AND bare pull_request (no paths filter), and the two events use different concurrency groups (pr-number vs ref), so every merge to main runs the paired interchange twice back-to-back. Each run syncs two full locked workspaces with --all-extras across a 2-leg matrix for changes (docs, GEO-INFER-TEST docs) that cannot affect the ACT↔GNN schema contract.
- Evidence: `gnn-interchange.yml:3-7` · `gnn-interchange.yml:13` · `gnn-interchange.yml:60-68`
- Bounded next step: Add `paths:` scoping (GEO-INFER-ACT/**, GEO-INFER-TEST/validate_gnn_interchange.py + its tests, .github/gnn-pair.json, the workflow file) and either drop the `push: [main]` trigger or share the concurrency group with the PR run.
- Acceptance probe: ``gh run list --workflow gnn-interchange.yml` — docs-only PRs list as skipped post-change; the next ACT-touching PR still runs both legs`
- Effort: S

**GS-008 · MINOR · CI-GATES · CI meta-gate: no lint of the workflow definitions themselves**
- Motivation: No actionlint/zizmor/static check exists anywhere for .github/workflows/*.yml (four workflows, none validated). The workflows carry non-trivial embedded shell (SHA validation and two different base-SHA fallbacks, heredoc Python in gnn-interchange.yml and release.yml) whose mistakes only surface as failed runs. All four workflows pin identical action SHAs and checksum-pinned binaries, so there is no version drift to catch today — the gap is future drift and expression/SC errors.
- Evidence: `ci.yml:73-78` · `ci.yml:146-151` · `gnn-interchange.yml:31-44`
- Bounded next step: Add a first job running `actionlint` (single static binary, optionally `zizmor`) on all workflow files, on every event.
- Acceptance probe: `introduce a deliberate `${{ invalid.expr }}` in a scratch branch — the lint job fails before any runner time is spent; clean tree exits 0`
- Effort: S

**GS-021 · MINOR · PACKAGING · Invalid trove classifier 'Creative Commons License' in 38 pyprojects**
- Motivation: No such trove classifier exists ('License :: OSI Approved' has no Creative Commons child) and CC BY-NC-SA is by definition not OSI-approved; PyPI returns 400 'Invalid classifier' on upload, blocking the wheel-release contract validate_packaging.py's docstring declares as enforced. Related metadata-family drift: 3 members use PEP 639 SPDX license strings while the other 42 use the legacy dict form.
- Evidence: `GEO-INFER-TEST/validate_packaging.py:1-8` · `GEO-INFER-ACT/pyproject.toml:20` · `GEO-INFER-AG/pyproject.toml:30` · `GEO-INFER-HEALTH/pyproject.toml:30` · `GEO-INFER-INSURANCE/pyproject.toml:9` · `GEO-INFER-PLACE/pyproject.toml:9` · `GEO-INFER-RISK/pyproject.toml:9`
- Bounded next step: Delete the bogus classifier from all 38 files (the license field already carries the license; no CC trove classifier should be substituted) and standardize on the SPDX string form the three setuptools>=77 files already use; optionally teach validate_packaging.py a classifier-validity check against a small allowlist.
- Acceptance probe: `grep -r 'Creative Commons License' --include=pyproject.toml . (expect 0 hits); uv run --no-sync python GEO-INFER-TEST/validate_packaging.py (expect exit 0)`
- Effort: S

**GS-022 · MINOR · PACKAGING · 29 pyprojects pack comma-separated keyword lists into single keyword strings**
- Motivation: Keyword strings like "ui, accessibility, dashboard, visualization, interface, responsive" become one keyword containing spaces and commas, so PyPI keyword search cannot match the individual terms; 29 files are affected while the fleet already contains ~8 correct list-form examples to mirror. The ACT string also duplicates the proper "active inference" entry.
- Evidence: `GEO-INFER-APP/pyproject.toml:15` · `GEO-INFER-ACT/pyproject.toml:15` · `GEO-INFER-API/pyproject.toml:15` · `GEO-INFER-IOT/pyproject.toml:15` · `GEO-INFER-METAGOV/pyproject.toml:15` · `GEO-INFER-NORMS/pyproject.toml:15` · `GEO-INFER-SPACE/pyproject.toml:15` · `GEO-INFER-HEALTH/pyproject.toml:15-25` · `GEO-INFER-RISK/pyproject.toml:12-22` · `GEO-INFER-LOG/pyproject.toml:15-25` · `GEO-INFER-AG/pyproject.toml:15-25` · `GEO-INFER-INSURANCE/pyproject.toml:11-20`
- Bounded next step: Mechanically split each comma-joined string into a proper keyword list matching the HEALTH/LOG style; keep the four common leading keywords ("geospatial", "active inference", "geoinformatics", domain term) and split the comma tails.
- Acceptance probe: `grep -rn ', [a-z]' GEO-INFER-*/pyproject.toml filtered to keywords lines (expect 0 hits); a built wheel's METADATA shows one keyword per concept`
- Effort: S

**GS-025 · MINOR · PACKAGING · Version floors/caps for the same dependency family disagree across root and members**
- Motivation: urllib3 floors disagree (root >=2.0.6 vs DATA >=1.26.0), shapely floors split 2.0-vs-1.8 across DATA/RISK/NORMS vs root/SEC/SPACE/PLACE/AG (1.8 with RISK's geopandas>=0.13 floor is impossible, and the 1.x/2.x API split makes the difference behavioral), numpy is capped <2.0 only in BAYES and SPACE, and uvicorn/requests floors span multiple versions. Standalone installs can therefore resolve different support surfaces than the workspace resolution.
- Evidence: `pyproject.toml:48` · `GEO-INFER-DATA/pyproject.toml:49` · `GEO-INFER-DATA/pyproject.toml:47` · `GEO-INFER-NORMS/pyproject.toml:28` · `pyproject.toml:34` · `GEO-INFER-SEC/pyproject.toml:37` · `GEO-INFER-SPACE/pyproject.toml:44` · `GEO-INFER-BAYES/pyproject.toml:30` · `GEO-INFER-SPACE/pyproject.toml:35` · `pyproject.toml:42` · `GEO-INFER-SPACE/pyproject.toml:45` · `GEO-INFER-ACT/pyproject.toml:33` · `pyproject.toml:47`
- Bounded next step: Document one floor policy per family in .agents/standards.md and align the outliers (cheapest consistent move: shapely>=2.0.0 fleet-wide — every consumer already passes current 2.x tests; urllib3>=2.0.6 in DATA).
- Acceptance probe: `grep -rn 'shapely>=1\.' GEO-INFER-*/pyproject.toml pyproject.toml (expect 0 hits post-alignment); uv lock --check`
- Effort: M

**GS-066 · MINOR · TEST-ESTATE · committed pytest-html reports, unseeded NORMS fixture, misplaced live-API script**
- Motivation: Four stale pytest-html reports are committed inside the NORMS test tree while the conftest already routes outputs to tmp_path, and NORMS' sample_geodataframe draws values from the unseeded global RNG, diverging from the seeded twin fixture in BAYES. GEO-INFER-INTRA/docs/realms/test_realms_api.py is a 637-line live-API requests script with no test_ functions but module-level side effects (makedirs outputs/test_run_*, logging config); root testpaths never collect it, but pointing pytest at GEO-INFER-INTRA/docs would import it, create directories in cwd, and hit a live API.
- Evidence: `GEO-INFER-NORMS/tests/test-outputs/{all_tests,normative_inference,report,social_norm_diffusion}.html` · `GEO-INFER-NORMS/tests/conftest.py:38-43` · `GEO-INFER-NORMS/tests/conftest.py:32` · `GEO-INFER-BAYES/tests/conftest.py:25` · `GEO-INFER-TEST/tests/conftest.py:32-35` · `GEO-INFER-INTRA/docs/realms/test_realms_api.py:24-35` · `pyproject.toml:361-364`
- Bounded next step: delete the four HTML reports and gitignore the directory; seed the NORMS fixture to match BAYES (np.random.default_rng(0)); rename docs/realms/test_realms_api.py to a non-test name (e.g. realms_api_probe.py) and drop the test_ prefix; fold the basicConfig removal (c) into the conftest sweep of GS-061 if done together
- Acceptance probe: `git status clean of new test-outputs artifacts after a NORMS run; repeated uv run --no-sync pytest GEO-INFER-NORMS/tests/unit -q produces identical fixture values; pytest --collect-only GEO-INFER-INTRA/docs/realms collects nothing named test_*`
- Effort: S

**GS-081 · MINOR · PIPELINE-SEC · Benchmark 2x flag rule enforced nowhere; in-suite H3 floor is 6.7x looser than documented**
- Motivation: The baseline doc mandates investigation when call time exceeds 2x the warm baseline (H3 > 1.5 s for 100k; PIP > 0.08 s), but nothing reads this doc: the H3 test asserts >10,000 ops/s (call time up to 10 s passes — 6.7x the 1.5 s flag threshold) and the PIP test asserts >50,000 pts/s (2 s vs the 0.08 s flag threshold). A ~5x perf regression passes CI while the doc's policy says it must block shipping a performance claim. Also a docstring/assertion mismatch: the H3 test docstring says >50k ops/sec but the assertion is >10k.
- Evidence: `GEO-INFER-TEST/docs/benchmark_baseline_2026-09-10.md:44-47` · `GEO-INFER-TEST/tests/unit/test_parametric_load_benchmarks.py:30` · `GEO-INFER-TEST/tests/unit/test_parametric_load_benchmarks.py:62` · `GEO-INFER-TEST/tests/unit/test_parametric_load_benchmarks.py:21`
- Bounded next step: Either (a) tighten the H3 floor to ~2x the documented warm baseline (~130k ops/s, still far below the measured 215k with headroom), or (b) add a CI/doc-note that the 2x rule is a manual pre-publication check only; make docstring, assertion floor, and doc agree.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-TEST/tests/unit/test_parametric_load_benchmarks.py -q still passes with the aligned floor; doc text and assertion constant derive from the same number`
- Effort: S

**GS-085 · MINOR · PIPELINE-SEC · H3 validation helpers and multi-agent init swallow every exception as invalid/default**
- Motivation: GEO-INFER-SPACE operations, h3_backend validation helpers, and the ACT h3_adapter all convert any exception from an h3 predicate call into return False, so a broken adapter state or a TypeError on a non-string index is indistinguishable from an invalid cell. Likewise multi_agent.py silently resets agent.beliefs to uniform on any exception with no log, while its sibling sites log warnings — the unlogged one is the odd site out. These are beyond the sanctioned availability-probe convention (no import under the except).
- Evidence: `GEO-INFER-SPACE/src/geo_infer_space/operations.py:813-815` · `GEO-INFER-SPACE/src/geo_infer_space/operations.py:837-839` · `GEO-INFER-SPACE/src/geo_infer_space/backends/h3/h3_backend.py:1268-1270` · `GEO-INFER-SPACE/src/geo_infer_space/backends/h3/h3_backend.py:1337-1339` · `GEO-INFER-SPACE/src/geo_infer_space/backends/h3/h3_backend.py:1353-1355` · `GEO-INFER-SPACE/src/geo_infer_space/backends/h3/h3_backend.py:1373-1375` · `GEO-INFER-ACT/src/geo_infer_act/utils/h3_adapter.py:170-177` · `GEO-INFER-ACT/src/geo_infer_act/models/multi_agent.py:380-383` · `GEO-INFER-ACT/src/geo_infer_act/models/multi_agent.py:289-292` · `GEO-INFER-ACT/src/geo_infer_act/models/multi_agent.py:306-309`
- Bounded next step: Narrow to the expected failure classes (TypeError/ValueError from bad indices) and log debug on unexpected ones; add a logger.warning to multi_agent.py:380-383 to match its siblings.
- Acceptance probe: `grep except Exception: in the three named files confirms each site either logs or re-raises outside the expected exception classes`
- Effort: S

**GS-086 · MINOR · PIPELINE-SEC · Manuscript verification runner has no timeout; one hung validator stalls the CI job**
- Motivation: The verification runner calls subprocess.run(command, shell=True, ...) with no timeout=; the VERIFICATION_COMMANDS table includes validators (run_model_audit.py --reproducible, unified-test runners) that have their own internal timeouts but the generator itself is unbounded. A hung validation command blocks the manuscript CI job indefinitely (the test leg explicitly budgets 90 min per SC-27; the manuscript job has no such guard).
- Evidence: `manuscript/generate_research_artifacts.py:1288-1310` · `manuscript/generate_research_artifacts.py:289-311` · `ci.yml:20-21`
- Bounded next step: Add timeout=<bound> per command (the unified runners already accept --timeout 600; give the bare validators the same envelope) and record a status="timeout" VerificationResult with the elapsed duration when subprocess.TimeoutExpired fires.
- Acceptance probe: `grep -n "TimeoutExpired" manuscript/generate_research_artifacts.py shows the handler; a dry probe with a stub command list containing sleep 30 and timeout 2 records a timed-out group instead of hanging`
- Effort: S

**GS-087 · MINOR · PIPELINE-SEC · config.yaml line-prefix parsing silently weakens gates on reindentation**
- Motivation: bibliography_policy reads gates by bare line prefix "  fail_on_missing: " / "  fail_on_unused: "; if bibliography's children are ever reindented (or a second same-prefixed key appears earlier), the parse misses and silently falls back to defaults — fail_on_unused defaults to False, so the orphaned-citation gate would quietly stop failing builds. Same class: refresh_config_metadata rewrites the first line matching "  version: " / "  date: " regardless of section, and write_resolved_manuscript rewrites "../output/figures/" by blind string replace — an undocumented assumption that every figure reference uses exactly that spelling.
- Evidence: `manuscript/generate_research_artifacts.py:1765-1778` · `manuscript/generate_research_artifacts.py:1654-1696` · `manuscript/generate_research_artifacts.py:1723`
- Bounded next step: Make the policy read section-aware (track the current top-level key while scanning lines) and raise on prefix-with-no-match in bibliography_policy instead of returning defaults; document the figure-path rewrite assumption next to line 1723 or make it regex-based on …/output/figures/.
- Acceptance probe: `throwaway probe that adds one level of indentation to the bibliography: block in a copied config and asserts bibliography_policy raises or reports the mismatch rather than returning fail_on_unused=False`
- Effort: S

**GS-088 · MINOR · PIPELINE-SEC · Secret-scan policy cadence doc drifts from ci.yml triggers (main+develop)**
- Motivation: The secret_scan_policy.md Cadence section says every pull request and every push to main, but ci.yml triggers on pushes/PRs to both main and develop — one-line doc fix. The rest of the policy is sound: allowlists are path- and rule-scoped, the binary is sha256-pinned, the scan is full-history fail-closed, and the egg-info PKG-INFO allowlist is history-only and justified in-policy.
- Evidence: `GEO-INFER-TEST/docs/secret_scan_policy.md §Cadence` · `ci.yml:4-7` · `ci.yml:127-133` · `ci.yml:140`
- Bounded next step: One-line doc fix: align the secret_scan_policy.md Cadence sentence with ci.yml on: triggers (main and develop). No re-scoping of allowlists needed.
- Acceptance probe: `diff the trigger sentence against ci.yml on:`
- Effort: S

### GeoCore (SPACE · TIME · DATA · MATH)

**GS-024 · MINOR · SPACE · Space 'all' extra omits declared srai and hand-duplicates the viz quartet**
- Motivation: srai>=0.10.2 is declared in the optional group, but the hand-maintained all group omits srai entirely, so pip install geo-infer-space[all] silently lacks a backend the module explicitly supports (SPACE src/backends/srai/ exists). The viz quartet is also copied verbatim into all, and SPACE is the outlier pattern: every other fleet module that aggregates extras does so narrowly or not at all.
- Evidence: `GEO-INFER-SPACE/pyproject.toml:65` · `GEO-INFER-SPACE/pyproject.toml:73-78` · `GEO-INFER-SPACE/pyproject.toml:79-93`
- Bounded next step: Add "srai>=0.10.2" to the all extra (or rebuild all as self-referencing geo-infer-space[optional,viz] following GEO-INFER-EDU's dev extra pattern at GEO-INFER-EDU/pyproject.toml:43-47, which uv.lock resolves fine).
- Acceptance probe: `grep -n "srai" GEO-INFER-SPACE/pyproject.toml pyproject.toml && uv run --no-sync python -c "import srai" (expect ImportError)`
- Effort: S — Note: cross-confirmed by GeoCore lane (SPACE packaging item)

**GS-103 · MINOR · SPACE · Two API-incompatible PlaceAnalyzer classes under one package**
- Motivation: geo_infer_space/place_analyzer.py:20 exports PlaceAnalyzer (the package __init__.py:33-43 surface, synthetic-demo semantics) while geo_infer_space/core/place_analyzer.py:22 defines a different PlaceAnalyzer(place_name, base_dir, processor) that creates data/ and outputs/ directories in its constructor and is imported only by tests. Same public name, different constructors and semantics; consumers importing from geo_infer_space.core.place_analyzer get a different object than the documented package export.
- Evidence: `GEO-INFER-SPACE/src/geo_infer_space/place_analyzer.py:20` · `GEO-INFER-SPACE/src/geo_infer_space/__init__.py:33-43` · `GEO-INFER-SPACE/src/geo_infer_space/core/place_analyzer.py:22` · `GEO-INFER-SPACE/tests/unit/test_place_analyzer.py:5`
- Bounded next step: Rename the core one (e.g. WorkspacePlaceAnalyzer) and update its single test import; verify no external doc references it.
- Acceptance probe: `uv run --no-sync python -c "import geo_infer_space, geo_infer_space.core.place_analyzer as cp; assert geo_infer_space.PlaceAnalyzer is not cp.PlaceAnalyzer" passes after rename with distinct names`
- Effort: S

**GS-104 · MINOR · SPACE · Packaging: sphinx floor mismatch, requirements.txt drift, demo scripts promoted**
- Motivation: The docs extra pins sphinx>=4.2.0 while the all extra pins sphinx>=4.0.0 (inconsistent floors). requirements.txt lists matplotlib/seaborn/plotly/folium/osmnx/laspy under 'Optional' comments but as unconditional lines, contradicting the pyproject extras split. Module root also carries demo_all_methods.py and verify_installation.py which README.md:20-27 promotes to 'Public Interface'.
- Evidence: `GEO-INFER-SPACE/pyproject.toml` · `GEO-INFER-SPACE/README.md:20-27` · `GEO-INFER-SPACE/requirements.txt`
- Bounded next step: Harmonize the sphinx floor between docs and all extras; align requirements.txt with the extras split or delete it in favor of pyproject; move demo scripts under examples/ and update README.
- Acceptance probe: `grep -n "demo_all_methods\|verify_installation" GEO-INFER-SPACE/README.md; grep -n "matplotlib" GEO-INFER-SPACE/requirements.txt`
- Effort: S — Note: srai point owned by Packaging lane

**GS-105 · MINOR · TIME · detect_change_points advertises 'pelt' and silently accepts unknown methods**
- Motivation: Docstring lists method ('cusum', 'pelt', 'binary_segmentation') but the body only branches on cusum and binary_segmentation with no else-raise; method='pelt' or any typo returns change_points_detected: 0, so a caller can't tell 'no change points' from 'method never ran'. Tests cover only the two implemented branches, leaving the hole untested.
- Evidence: `GEO-INFER-TIME/src/geo_infer_time/core/analysis.py:394` · `GEO-INFER-TIME/src/geo_infer_time/core/analysis.py:406` · `GEO-INFER-TIME/src/geo_infer_time/core/analysis.py:440` · `GEO-INFER-TIME/tests/test_temporal_analysis.py:135-151`
- Bounded next step: Add a final else: raise ValueError(...) and remove 'pelt' from the docstring (or implement it and add a test).
- Acceptance probe: `pytest GEO-INFER-TIME/tests/test_temporal_analysis.py -k change_points after adding a case asserting pytest.raises(ValueError) for method="pelt"`
- Effort: S

**GS-106 · MINOR · TIME · O(n^2) cusum scan recomputes means per index**
- Motivation: core/analysis.py:406-436 loops np.mean(values[:i]) / np.mean(values[i:]) for every candidate index — quadratic on long sensor series; easily replaced by cumulative sums (already computed at line 409). Pure performance, no behavior change.
- Evidence: `GEO-INFER-TIME/src/geo_infer_time/core/analysis.py:406-436` · `GEO-INFER-TIME/src/geo_infer_time/core/analysis.py:409`
- Bounded next step: Precompute prefix sums; keep the threshold/dedup logic identical.
- Acceptance probe: `existing test_detect_change_points_cusum (GEO-INFER-TIME/tests/test_temporal_analysis.py:135) must still pass unchanged; optional 10k-point timing check`
- Effort: S

**GS-109 · MINOR · DATA · DuckDB fast path hardcodes EPSG:4326; fallback preserves CRS**
- Motivation: The DuckDB fast path returns a GeoDataFrame with crs="EPSG:4326" unconditionally, while the fallback lets gpd.read_file infer the true CRS. The module docstring claims behaviour is consistent across environments so callers never branch on which engine ran — false for any GeoParquet/FlatGeobuf not stored in WGS84: same file, different CRS depending on whether DuckDB is installed.
- Evidence: `GEO-INFER-DATA/src/geo_infer_data/utils/duckdb_spatial.py:78` · `GEO-INFER-DATA/src/geo_infer_data/utils/duckdb_spatial.py:39-43` · `GEO-INFER-DATA/src/geo_infer_data/utils/duckdb_spatial.py:5-9`
- Bounded next step: Read the CRS from the GeoParquet metadata (or DuckDB's ST_Read CRS output) instead of hardcoding; add a non-WGS84 fixture test asserting both paths agree.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-DATA/tests/unit/test_duckdb_spatial.py -q plus a throwaway round-trip of a projected (e.g. EPSG:32610) GeoParquet through read_cloud_native_vector with use_duckdb=True vs False`
- Effort: S

**GS-110 · MINOR · MATH · Polygon.centroid is a vertex average, not the polygon centroid**
- Motivation: core/geometry.py:112-122 computes the arithmetic mean of exterior vertices. For any non-convex or irregular polygon this differs from the area centroid (L-shaped polygon: vertex mean can even fall outside the polygon). The docstring admits it, but the method name and __all__ export imply the geometric centroid; downstream consumers of spatial statistics can silently get wrong centers. The only test covers a square, where vertex average happens to be correct.
- Evidence: `GEO-INFER-MATH/src/geo_infer_math/core/geometry.py:112-122` · `GEO-INFER-MATH/tests/unit/test_geometry.py:TestPolygon.test_polygon_centroid`
- Bounded next step: Implement the standard area-weighted centroid (shoelace-based cross-section formula, holes subtracted) and add a non-convex test case.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_math.core.geometry import Polygon, Point; p = Polygon(exterior=[Point(0,0), Point(4,0), Point(4,1), Point(1,1), Point(1,3), Point(0,3)]); print(p.centroid())" — L-shape centroid must be (approx) (1.5, 1.2), not the vertex mean (1.67, 1.0)`
- Effort: S

**GS-111 · MINOR · MATH · vincenty_distance silently returns unconverged results near antipodal points**
- Motivation: The iteration loop breaks on convergence but has no post-loop check; when max_iterations is exhausted (classic Vincenty failure for near-antipodal pairs, documented behavior of the algorithm) the function returns a distance computed from the unconverged lambda — silently, with no marker. Tests only cover same-point and NYC-London; a near-antipodal pair would exercise the failure with no assertion. SKILL.md claims 'geodesic on WGS84 ellipsoid' accuracy without qualifying this.
- Evidence: `GEO-INFER-MATH/src/geo_infer_math/core/geometry.py:202-219` · `GEO-INFER-MATH/tests/unit/test_geometry.py:156-163`
- Bounded next step: Track convergence; raise ValueError (or return NaN with a documented failure contract, matching the pattern used in numerical_methods.py:805-815) when the loop exhausts without tolerance.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_math.core.geometry import vincenty_distance; vincenty_distance(40.7128,-74.0060,-40.7128,105.994)" — must raise/flag, not return a silently-wrong number; existing tests still pass`
- Effort: S

**GS-112 · MINOR · MATH · Custom optimizers always report success:True, unlike ScipyOptimizer**
- Motivation: GradientDescentOptimizer.optimize returns success: True unconditionally, as do GeneticAlgorithmOptimizer and MultiObjectiveOptimizer — even when the run diverged or exhausted max_iterations without converging (GD has no failure branch at all; it can return the initial guess as 'success'). ScipyOptimizer correctly propagates result.success and returns success: False on exception. Callers keying on result["success"] get false positives from three of four optimizers.
- Evidence: `GEO-INFER-MATH/src/geo_infer_math/core/optimization.py:163` · `GEO-INFER-MATH/src/geo_infer_math/core/optimization.py:264` · `GEO-INFER-MATH/src/geo_infer_math/core/optimization.py:505` · `GEO-INFER-MATH/src/geo_infer_math/core/optimization.py:296-314`
- Bounded next step: Track convergence (tolerance hit vs iteration budget exhausted vs non-finite objective) and set success accordingly; keep x/fun/history fields unchanged.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-MATH/tests/unit/test_optimization.py -q after adding a divergence case (e.g. lr=10 on a quadratic) asserting result["success"] is False`
- Effort: S

### Inference (BAYES · SPM · COG · ACT)

**GS-120 · MINOR · BAYES · Package __init__ carries dead None-fallbacks and a misplaced ~300-line GaussianProcess**
- Motivation: __init__.py guards SpatialGP/SparseSpatialGP/BayesianInference/PosteriorAnalysis imports with except ImportError setting them to None, but runs sibling imports unguarded (VariationalInference, MCMC, civic_intel, geo_observations); if any sibling fails, package import aborts anyway, so the None fallbacks are unreachable dead code and real breakage surfaces as a confusing TypeError at call time; additionally GaussianProcess + SpatialCovariance (~300 lines) are defined inline in __init__.py rather than in models/, yet tests import them from the package root.
- Evidence: `src/geo_infer_bayes/__init__.py:96-123` · `src/geo_infer_bayes/__init__.py:127-382` · `tests/unit/test_gaussian_process.py:17` · `tests/integration/test_bayesian_workflow.py:22`
- Bounded next step: Move GaussianProcess/SpatialCovariance into models/ (e.g. models/gaussian_process.py) with a package-root re-export; drop the dead try/except-None blocks so import failures surface honestly; no API change (all consumers import from geo_infer_bayes).
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_bayes import GaussianProcess, SpatialGP, BayesianInference" then uv run python GEO-INFER-TEST/run_unified_tests.py --module BAYES`
- Effort: S

**GS-121 · MINOR · BAYES · Zero test coverage for PyMCInterface / StanInterface / TFPInterface**
- Motivation: api/ exports three public interfaces with no test in GEO-INFER-BAYES/tests referencing any of them; they are testable without optional backends: StanInterface.create_spatial_gp_model returns generated Stan code, TFPInterface is a documented permanent pure-NumPy/SciPy backend (tfp=None permanently) needing no TensorFlow, and PyMCInterface model construction is skippable when pymc is absent.
- Evidence: `src/geo_infer_bayes/api/__init__.py:7-21` · `src/geo_infer_bayes/api/stan_interface.py:21-64` · `src/geo_infer_bayes/api/tfp_interface.py:11-17` · `src/geo_infer_bayes/api/tfp_interface.py:60` · `src/geo_infer_bayes/api/tfp_interface.py:119` · `src/geo_infer_bayes/api/tfp_interface.py:183`
- Bounded next step: Add tests/unit/test_api_interfaces.py covering Stan code-gen shape (data/parameters/model blocks, kernel params), TFP-NumPy fit->sample->predict round trip on synthetic data, and PyMCInterface tests skipif pymc missing.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-BAYES/tests/unit/test_api_interfaces.py -q`
- Effort: M

**GS-123 · MINOR · SPM · README visualization contracts unenforced; rejection paths untested, test accepts None**
- Motivation: README Visualization Contracts claim statistical and interactive maps reject invalid contrast indices, empty or non-finite coordinates/statistics, and misaligned significance arrays; the behavior exists in create_interactive_map but the only test asserts 'interactive_map is None or hasattr(interactive_map, "show")', which passes trivially if plotly is missing or the function returns None, and no test exercises any rejection path.
- Evidence: `visualization/interactive.py:41-60` · `visualization/interactive.py:88` · `tests/unit/test_visualization.py:278-289`
- Bounded next step: Parametrized tests asserting pytest.raises(ValueError) for contrast_idx=-1/non-int, empty/non-finite coordinates, misaligned stats, unknown map_type, plus a positive scattergeo figure assertion.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-SPM/tests/unit/test_visualization.py -q with new rejection tests failing on regression`
- Effort: S

**GS-124 · MINOR · SPM · ESS failure path returns a fabricated constant instead of a missing-value marker**
- Motivation: On any exception _compute_ess returns the fabricated constant np.array([1000]) with only a warning, so a consumer comparing ESS across models receives a plausible-looking fake number indistinguishable from a real measurement; sibling _compute_r_hat already returns NaN with a warning on the same failure shape. The n_draws * n_chains upper bound itself is deferred-register territory and was not re-scoped.
- Evidence: `core/bayesian.py:630-644` · `core/bayesian.py:617-627`
- Bounded next step: Return np.array([np.nan]) matching _compute_r_hat semantics, keep the warning, and note the NaN failure mode in the deferred register entry.
- Acceptance probe: `uv run --no-sync python -c "import numpy as np; from geo_infer_spm.core.bayesian import BayesianSPM; b=BayesianSPM.__new__(BayesianSPM); assert np.isnan(b._compute_ess(type('T',(),{'posterior':None})())[0])" (object raising on attribute access exercises the except branch)`
- Effort: S

**GS-125 · MINOR · COG · search_memory 'spatial_bounds' criteria ignores coordinates entirely (scale-string match)**
- Motivation: SpatialMemoryModel.search_memory accepts 'spatial_bounds' queries routed to _spatial_overlap, but that helper only compares the stored 'scale' label string (small/medium/large) from spatial_context; any bbox/geometry in the query is discarded, so an item with scale 'medium' matches a query bounding box anywhere on Earth, and items lacking a 'scale' key silently fail every spatial query; tests cover content/temporal/importance criteria but nothing pins spatial-bounds behavior.
- Evidence: `core/spatial_memory.py:541-559` · `core/spatial_memory.py:576-580` · `core/spatial_memory.py:617-626` · `tests/unit/test_spatial_memory.py:118-181`
- Bounded next step: Implement real bbox-overlap when both item spatial_context and query carry bbox (min/max lat-lon intersection); fall back to the scale match only when geometry is absent; document accepted query keys in the search_memory docstring; add overlap/no-overlap/missing-geometry tests.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_cog import SpatialMemoryModel; m=SpatialMemoryModel(); m.store_spatial_memory(content={'type':'spatial_element'}, spatial_context={'bbox':[0,0,1,1]}); assert m.search_memory({'spatial_bounds':{'bbox':[10,10,11,11]}})==[]" — fails on current code (item matches the distant bbox)`
- Effort: S

**GS-127 · MINOR · COG · REST API surface (create_cog_api_app) is fully untested**
- Motivation: SKILL documents the REST API as the optional-extra surface; api/rest_api.py implements ~12 endpoints, each with its own except Exception -> jsonify(error), 500 handler, plus a partial-init path that logs and continues with limited functionality; grep of GEO-INFER-COG/tests for rest_api|create_cog_api_app|flask returns zero matches — no endpoint has ever been exercised by the suite.
- Evidence: `api/rest_api.py:174-176` · `api/rest_api.py:207-209` · `api/rest_api.py:265-267` · `api/rest_api.py:307-309` · `api/rest_api.py:363-365` · `api/rest_api.py:409-411` · `api/rest_api.py:476-478` · `api/rest_api.py:547-549` · `api/rest_api.py:567-569` · `api/rest_api.py:601-603` · `api/rest_api.py:628-630` · `api/rest_api.py:666-668` · `api/rest_api.py:107-109`
- Bounded next step: Add tests/unit/test_rest_api.py using app.test_client() (skipif flask missing): happy paths for text analysis, entity extraction, decision analysis; one 500 path asserting the JSON error shape; partial-init degradation observable via /status.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-COG/tests/unit/test_rest_api.py -q`
- Effort: M

**GS-129 · MINOR · ACT · geo-infer-ai declared as hard runtime dependency but used only by tests**
- Motivation: pyproject.toml lists geo-infer-ai>=0.2.0 in dependencies (mirrored in README dep listings), yet no geo_infer_ai import exists anywhere under src/ — only tests import it; the genuinely used cross-module dep geo-infer-bayes is correctly declared; every downstream installer of geo-infer-act pulls geo-infer-ai unnecessarily.
- Evidence: `pyproject.toml:33` · `tests/integration/test_stigmergy.py:4` · `tests/unit/test_h3.py:357` · `tests/unit/test_h3.py:393` · `core/civic_intel.py:43-55`
- Bounded next step: Move geo-infer-ai from dependencies to the dev/test extra and update the README dependency lists to match.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-ACT/tests/unit/test_h3.py -q -k environmental still passes in the workspace env (dev extra present); grep -n geo-infer-ai GEO-INFER-ACT/pyproject.toml shows it out of dependencies`
- Effort: S

**GS-130 · MINOR · ACT · ACT couples to private BAYES helpers by underscore import**
- Motivation: core/civic_intel.py imports CRESCENT_CITY_INTEL_SCHEMA, _parse_contract_bounds, _require_list, ... from geo_infer_bayes.civic_intel — the latter two are private-by-convention names owned by another module (BAYES AGENTS.md declares module contracts via public exports only); any BAYES-internal rename silently breaks ACT's non-degraded path, and the degraded fallback would mask it into schema-string-only mode.
- Evidence: `core/civic_intel.py:44-47` · `core/civic_intel.py:56-58`
- Bounded next step: Promote the needed helpers to public names in geo_infer_bayes.civic_intel (they are already part of the cross-module crescent-city contract) and update the ACT import + BAYES __all__; alternatively inline the two small validators in ACT's degraded-mode decoder.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_bayes.civic_intel import parse_contract_bounds, require_list" succeeds; uv run --no-sync python -m pytest GEO-INFER-ACT/tests/unit/test_civic_intel.py GEO-INFER-BAYES/tests/unit/test_civic_intel.py -q`
- Effort: S

**GS-131 · MINOR · SPM · SPM package __init__ mixes try/except-None guards with unguarded sibling imports**
- Motivation: __init__.py guards SpatialAnalyzer/TemporalAnalyzer/BayesianSPM with try/except ImportError setting them to None, then runs unguarded imports for data models, utils, core.advanced, spatial_regression, and api.endpoints in the same module; any sibling failure aborts package import anyway, so the None fallbacks are dead code and real breakage surfaces as TypeError at call time — the same pattern flagged for BAYES in GS-120. Verified directly against the tree (orchestrator delta-scope directive); not present in the lane report body.
- Evidence: `src/geo_infer_spm/__init__.py:40-55` · `src/geo_infer_spm/__init__.py:58` · `src/geo_infer_spm/__init__.py:61-62` · `src/geo_infer_spm/__init__.py:68-73` · `src/geo_infer_spm/__init__.py:77` · `src/geo_infer_spm/__init__.py:80`
- Bounded next step: Drop the try/except-None blocks for SpatialAnalyzer/TemporalAnalyzer/BayesianSPM so import failures surface as honest ImportErrors, mirroring the GS-120 fix pattern; keep flat imports (SPM is self-contained, no optional deps in src).
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_spm import SpatialAnalyzer, TemporalAnalyzer, BayesianSPM, SPMAPI"`
- Effort: S — Note: Genuine on orchestrator tree-read verification @ a998ae91 (three try/except-None guards for optional analysis imports followed by unguarded sibling package imports); surfaced during itemization, not a lane-report finding

### Risk & Earth (RISK · INSURANCE · CLIMATE · EMERGENCY)

**GS-020 · MINOR · INSURANCE · Top-level requests import undeclared in dependencies; no import-vs-deps gate**
- Motivation: GEO-INFER-INSURANCE underwriting/utils/data_integration.py:20 does an unconditional top-level import requests while [project.dependencies] and requirements.txt declare only numpy/pandas; the subpackage __init__ imports it eagerly, so import geo_infer_insurance raises ModuleNotFoundError on a clean pip install of the wheel. The same file already models the correct guarded-import pattern for optional backends, and the systemic root cause is that validate_packaging.py audits requirements-vs-pyproject but never pyproject-vs-imports, so this failure class passed the gate (0 errors).
- Evidence: `GEO-INFER-INSURANCE/src/geo_infer_insurance/underwriting/utils/data_integration.py:20` · `GEO-INFER-INSURANCE/pyproject.toml:29-32` · `GEO-INFER-INSURANCE/src/geo_infer_insurance/underwriting/__init__.py:62` · `GEO-INFER-INSURANCE/src/geo_infer_insurance/underwriting/utils/data_integration.py:351` · `GEO-INFER-INSURANCE/src/geo_infer_insurance/underwriting/utils/data_integration.py:279` · `GEO-INFER-TEST/validate_packaging.py:291-336`
- Bounded next step: (a) Either add requests>=2.31.0 to INSURANCE deps and requirements.txt, or demote the line-20 import to the guarded style used at line 351; (b) add an ast-walk validate_import_parity() to validate_packaging.py comparing top-level (non-try/except-guarded) third-party import roots against declared dependency names, seeded so the INSURANCE site is its first catch.
- Acceptance probe: `grep -n '^import requests' GEO-INFER-INSURANCE/src/geo_infer_insurance/underwriting/utils/data_integration.py (expect no output post-fix); then uv run --no-sync python GEO-INFER-TEST/validate_packaging.py --diagnostics (expect exit 0 with the new gate active)`
- Effort: S

**GS-140 · MINOR · RISK · Honest Capability Register omits CatastropheInsuranceModel rate tables**
- Motivation: SKILL.md register promises every simplified conversion/stand-in probability is catalogued; the per-peril premium base rates (hurricane/earthquake/flood/wildfire latitude bands) and flat per-peril expected-loss fractions are stand-in probabilities that appear nowhere in the register.
- Evidence: `src/geo_infer_risk/core/insurance_models.py:489-563` · `SKILL.md (Honest Capability Register section)`
- Bounded next step: Add one register entry covering latitude-band base rates and the flat expected-loss fractions; add inline pointer comment in insurance_models.py
- Acceptance probe: `static-review (grep SKILL.md for a register heading mentioning catastrophe base-rate tables; inline comments cite the register)`
- Effort: S

**GS-141 · MINOR · RISK · Malformed exposure data silently disables the model**
- Motivation: _validate_and_clean_exposure_data raises ValueError for missing required columns, but _initialize_exposure_data catches every exception and sets exposure_data=None — a mis-shaped CSV degrades to no-data instead of surfacing the validation error, contradicting the validation intent; _merge_exposure_data also silently returns the original frame, dropping the second source on any merge error.
- Evidence: `core/exposure_model.py:190-192 (broad except -> exposure_data=None)` · `core/exposure_model.py:291 (raise)` · `core/exposure_model.py:279-280 (merge silently returns original frame)`
- Bounded next step: Narrow the except to I/O errors (OSError, JSON/parse errors) and let ValueError from validation propagate; make merge failure raise or log+flag rather than silently discard
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_risk.core.exposure_model import EnhancedExposureModel; import pandas as pd; p='/tmp/bad.csv'; pd.DataFrame({'lon':[0]}).to_csv(p); EnhancedExposureModel('property',{'data_sources':[f'file://{p}']})" raises ValueError instead of initializing empty`
- Effort: S

**GS-142 · MINOR · RISK · EnhancedExposureModel missing from the core export surface**
- Motivation: core/__init__.py re-exports Enhanced{Hazard,Vulnerability,Catastrophe}Model and their factories, but nothing from exposure_model.py; SKILL.md's Key Imports is forced to use the deeper module path only for exposure — inconsistent public surface.
- Evidence: `core/__init__.py (no exposure_model import; __all__ lacks EnhancedExposureModel)` · `SKILL.md:38 (from geo_infer_risk.core.exposure_model import EnhancedExposureModel)`
- Bounded next step: Export EnhancedExposureModel + the three subclasses/factories from geo_infer_risk.core (and optionally the package __init__), add to test_package_exports.py
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_risk.core import EnhancedExposureModel" succeeds`
- Effort: S

**GS-144 · MINOR · INSURANCE · create_claims_processor silently discards its config argument**
- Motivation: create_claims_processor(config) always builds ClaimsProcessingConfig() defaults, so process_claim(claim_data, config) ignores config entirely; tests must bypass the convenience function to set processing_mode=manual.
- Evidence: `underwriting/__init__.py:166-170` · `claims_processing.py:241 (ClaimsProcessor(config: Optional[ClaimsProcessingConfig] = None))` · `test_pricing_and_claims.py:107-111 (tests bypass the wrapper to set processing_mode)`
- Bounded next step: Map dict config onto ClaimsProcessingConfig fields (or accept a ClaimsProcessingConfig) and honor it; align process_claim docstring
- Acceptance probe: `process_claim(claim, {"processing_mode": "manual"}) leaves claim status UNDER_REVIEW without bypassing the wrapper`
- Effort: S

**GS-145 · MINOR · INSURANCE · UnderwritingQueue wait-time statistics are dead code**
- Motivation: add_to_queue feeds 0.0 into the wait-time running average every time (average stays 0 forever), longest_wait_time is never written, and case_id is ignored by add/remove — the queue cannot track cases.
- Evidence: `underwriting/models/underwriting_models.py:312-321 (avg update adds constant 0.0)` · `underwriting/models/underwriting_models.py:323-327 (remove_from_queue ignores case_id)` · `underwriting/models/underwriting_models.py (longest_wait_time never assigned)`
- Bounded next step: Either implement entry timestamps (datetime.now on add, wait on remove) or delete the statistics fields from the public to_dict until real tracking exists
- Acceptance probe: `unit test asserting a queued-then-removed case yields nonzero average/longest wait, or the fields no longer exist`
- Effort: S

**GS-147 · MINOR · INSURANCE · pyproject metadata drift: subpackage version 1.0.0 vs 0.2.0, author, eager import**
- Motivation: geo_infer_insurance.underwriting reports __version__ 1.0.0 while the package and pyproject are 0.2.0 — packaging/doc-truth drift; per orchestrator directive D1 this item also owns the author-field drift and eager-import metadata aspects of the pyproject record.
- Evidence: `underwriting/__init__.py:17 (reports __version__ 1.0.0)` · `pyproject.toml (version 0.2.0)`
- Bounded next step: Align subpackage version to 0.2.0 (or derive from package __version__); reconcile author metadata and eager-import behavior as part of the same pyproject cleanup
- Acceptance probe: `grep underwriting/__init__.py for 1.0.0 returns nothing`
- Effort: S — Note: Packaging lane cross-confirms; the undeclared `requests` import is tracked separately as packaging GS-020

**GS-148 · MINOR · CLIMATE · Dataset-format doc claims exceed implementation; cfgrib undeclared**
- Motivation: SKILL.md Core Capabilities and the class attribute advertise CSV/HDF5 loading, but load_dataset implements only .nc and .grib; the documented CSV/HDF5 paths fail on a clean install, so docs and the supported_formats attribute overstate the implementation; additionally .grib requires cfgrib, which is in no dependency list, so the documented GRIB path fails with ImportError on a clean install.
- Evidence: `core/climate_data.py:32 (supported_formats includes csv/hdf5)` · `core/climate_data.py:55-65 (only nc/grib branches)` · `SKILL.md (Data processing: load (NetCDF/GRIB/CSV/HDF5))` · `pyproject.toml dependencies (numpy/pandas/scipy/xarray only)`
- Bounded next step: Either implement csv/hdf5 readers (xr.open_dataset engine=... / pd.read_csv -> Dataset) or correct SKILL.md + supported_formats, and declare cfgrib (or an extra) for GRIB
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_climate import ClimateDataProcessor; ClimateDataProcessor().load_dataset('x.csv','era5')" either loads or the doc/attribute no longer lists csv`
- Effort: S — Note: cfgrib dependency aspect retained here (Packaging lane has no cfgrib item)

**GS-149 · MINOR · CLIMATE · calculate_spi silently computes normal SPI for unknown distributions**
- Motivation: Any distribution value other than gamma takes the normal path — e.g. distribution=log-logistic or a typo returns numbers without error, an SPI-flavored silent fallback.
- Evidence: `core/climate_indices.py:61-65`
- Bounded next step: Validate distribution in {gamma, normal} and raise ValueError otherwise (matches calculate_aal/exposure_years style)
- Acceptance probe: `calculate_spi(precip, distribution="bogus") raises ValueError; existing gamma/normal tests still pass`
- Effort: S

**GS-150 · MINOR · CLIMATE · project_future_climate mixes per-step trend slope with calendar years**
- Motivation: _calculate_trend fits slope against np.arange(len(time)) (per index step) but the projection multiplies it by years_ahead in calendar years. For monthly or daily historical series the extrapolation is off by 12x/365x — a defect beyond the documented illustrative linear-scaling simplification.
- Evidence: `core/projections.py:77-78 (trend * years_ahead)` · `core/projections.py:87-92 (np.arange index slope)`
- Bounded next step: Express x as elapsed years from the time coordinate (datetime or numeric-year) so slope is per year, or convert steps->years in the projection
- Acceptance probe: `monthly and annual series covering the same span yield identical projections (unit test comparing both)`
- Effort: S

**GS-151 · MINOR · CLIMATE · Mann-Kendall and Sen's slope use pure-Python O(n^2) loops**
- Motivation: The S-statistic and pairwise-slope loops are O(n^2) Python; a 30-year daily series (n~11k) means ~60-120M interpreted iterations — minutes per call — where numpy broadcast/triu versions run in milliseconds.
- Evidence: `core/temperature_trends.py:125-132 (S double loop)` · `core/temperature_trends.py:195-210 (sens_slope pairwise loop)`
- Bounded next step: Vectorize via np.sign(np.subtract.outer(x, x)) upper-triangle sum and pairwise-slope matrix, keeping the tie correction; verify identical outputs on random fixtures
- Acceptance probe: `existing mann_kendall/sens tests pass unchanged; a 10k-point series completes <1s`
- Effort: S

**GS-152 · MINOR · EMERGENCY · resource_types config is dead and its vocabulary mismatches ResourceType**
- Motivation: The constructor stores resource_types (plural engines/ambulances/rescue_units) but nothing in src reads it, and optimize_allocation converts resource dicts via ResourceType(...singular...): passing the module's own default vocabulary (type=engines) raises a bare ValueError mid-allocation.
- Evidence: `core/resources.py:85-88 (stored, never read; only docs/tests reference it)` · `core/resources.py:125 (ResourceType(res_data.get(type, engine).lower()))`
- Bounded next step: Either consume resource_types as an allowed-type filter with a plural->singular alias map, or document/delete the parameter; wrap the enum conversion with an actionable error listing valid types
- Acceptance probe: `ResourceDeployer().optimize_allocation([{"id":"r1","type":"engines","location":{...}}], ...) succeeds or fails with a message naming valid types`
- Effort: S

**GS-153 · MINOR · EMERGENCY · dynamic_redeploy move_up heuristic is a stub with a false comment**
- Motivation: The code comment says Find unit farthest from any incident but the implementation takes available[0]; redeployment quality is arbitrary while docs imply coverage-gap optimization.
- Evidence: `core/resources.py:256-258`
- Bounded next step: Implement the stated heuristic (unit whose location maximizes min-distance to pending incidents) or fix the comment to state first-available order
- Acceptance probe: `unit test with two available units at different distances asserts the expected one is chosen (or comment corrected and test pins available[0] order)`
- Effort: S

**GS-154 · MINOR · EMERGENCY · SituationalAwareness advertises fusion algorithms it does not implement**
- Motivation: Constructor defaults fusion_algorithms=[kalman,bayesian] (echoing the SKILL data-fusion capability) but fuse_data ignores self.fusion_algorithms entirely and computes weighted_average for any requested fusion_method — kalman/bayesian requests silently produce weighted averages; empty sources return an error dict instead of raising like the rest of the module.
- Evidence: `core/awareness.py:83 (stored default, never read)` · `core/awareness.py:313-370 (fuse_data body: method-agnostic weighted average; empty sources -> {error: No sources provided} return)`
- Bounded next step: Drop the constructor param or document it as a label; raise ValueError for unsupported fusion_method and for empty sources
- Acceptance probe: `fuse_data([], "kalman") raises; SKILL/docs no longer imply algorithmic fusion beyond weighted average`
- Effort: S

**GS-155 · MINOR · EMERGENCY · haversine_distance_km silently maps missing coordinates to (0,0)**
- Motivation: Missing lat/lon keys default to 0.0, so a malformed point yields distances to Null Island instead of an error — silent wrong travel times in resource deployment and SAR pattern metrics.
- Evidence: `core/geo.py:27-30`
- Bounded next step: Require both keys (KeyError/ValueError with the point's keys listed); update the two call sites if needed
- Acceptance probe: `haversine_distance_km({"lat": 45.5}, {"lat":45.6,"lon":-122.6}) raises; SKILL smoke command (both points complete) still returns 11.119`
- Effort: S

### Environment (WATER · MARINE · FOREST · ENERGY)

**GS-161 · MINOR · WATER · identify_pollution_sources breaks on >2D input; per-cell Python loop**
- Motivation: water_quality.py:293-294 computes spatial_dims from the last two dims (implying multi-dim support) but `ny, nx = hotspots_vals.shape` unpacks the full shape, so a (time,y,x) input raises 'too many values to unpack'; the refine loop :296-310 is O(n^2) Python.
- Evidence: `water_quality.py:293-294` · `water_quality.py:296-310`
- Bounded next step: Validate/squeeze to 2D with a clear ValueError; vectorize the neighbor check with shifted-array comparisons (pattern exists in GEO-INFER-FOREST deforestation.calculate_fragmentation_index).
- Acceptance probe: `call with a (time,y,x) hotspot array - pre-fix ValueError, post-fix result`
- Effort: S

**GS-162 · MINOR · WATER · check_regulatory_compliance silently falls back to EPA for unknown framework names**
- Motivation: water_quality.py:650 `limits = reg_limits.get(regulations, reg_limits['EPA'])` - a typo like 'who' is silently evaluated against EPA limits while the report echoes the requested framework name.
- Evidence: `water_quality.py:650`
- Bounded next step: Raise ValueError for unknown frameworks (or normalize case before lookup).
- Acceptance probe: `assessor.check_regulatory_compliance(samples, 'epa') raises post-fix`
- Effort: S

**GS-163 · MINOR · WATER · Green-Ampt implemented twice with divergent numerics**
- Motivation: hydrology.py:123 (implicit Newton ponding solver, xarray in/out, spatial) vs infiltration.py:37 (explicit supply-limited stepping, numpy dict out, point-scale); both tested separately (test_green_ampt.py, test_infiltration.py) so physics can silently diverge; module already established a single-owner pattern for water balance (SKILL.md:70).
- Evidence: `hydrology.py:123` · `infiltration.py:37` · `test_green_ampt.py` · `test_infiltration.py` · `SKILL.md:70`
- Bounded next step: Delegate HydrologicalModeler.green_ampt_infiltration to InfiltrationModeler (or vice versa) or document the raster-vs-point split in SKILL.md.
- Acceptance probe: ``uv run --no-sync python -m pytest GEO-INFER-WATER/tests/unit/test_green_ampt.py GEO-INFER-WATER/tests/unit/test_infiltration.py -q` stays green after consolidation`
- Effort: M

**GS-164 · MINOR · WATER · Extraterrestrial radiation silently zeroes at polar latitudes**
- Motivation: water_balance.py:284-285 `np.arccos(-tan(lat)*tan(dec))` returns NaN when the argument magnitude exceeds 1 (latitudes beyond ~66 deg near solstices); the max(0.0,...) clamp converts NaN to 0.0 so polar-summer Hargreaves PET silently becomes 0; _mean_day_length (:255-256) already clamps correctly - the two helpers diverged.
- Evidence: `water_balance.py:284-285` · `water_balance.py:255-256`
- Bounded next step: Clip the cos argument to [-1,1] before arccos.
- Acceptance probe: `hargreaves_pet at lat=75, doy=180 -> PET > 0 post-fix (currently ~0)`
- Effort: S

**GS-165 · MINOR · WATER · track_pollution_plume ignores pollutant_type**
- Motivation: water_quality.py:322 parameter accepted and echoed at :381 but never affects dispersion - all pollutants share one Gaussian.
- Evidence: `water_quality.py:322` · `water_quality.py:381`
- Bounded next step: Per-type default diffusivity, or document the parameter as metadata-only.
- Acceptance probe: `docstring states the contract; output identical across pollutant types`
- Effort: S

**GS-167 · MINOR · MARINE · Coastal vulnerability index unbounded and divides by zero**
- Motivation: coastal_analysis.py:44 `1.0 / (relative_elevation + 1.0)` - relative_elevation == -1 gives ZeroDivisionError/inf; < -1 gives negative index; unbounded above; inline comment claims 'Normalized'.
- Evidence: `coastal_analysis.py:44`
- Bounded next step: Clamp relative elevation, cap index at [0,1], fix comment.
- Acceptance probe: `relative_elevation = -1 and -5 cells -> bounded, finite vulnerability`
- Effort: S

**GS-168 · MINOR · MARINE · Bleaching risk counts cold water as thermal stress**
- Motivation: marine_ecosystems.py:77-78 `bleaching_risk = np.abs(temperature - 26)/5` - 16C SST yields bleaching_risk 2.0; unbounded ('Normalized' comment wrong); CoralReefAssessor (coral_reef.py:44-49) correctly uses a one-sided hotspot.
- Evidence: `marine_ecosystems.py:77-78` · `coral_reef.py:44-49`
- Bounded next step: One-sided np.maximum(temperature - 26, 0)/5, clamp, align semantics with CoralReefAssessor.
- Acceptance probe: `SST 16C -> bleaching_risk 0; SST 31C -> 1.0`
- Effort: S

**GS-169 · MINOR · MARINE · Offshore wind depth suitability >1 on land cells; unvalidated MSP inputs**
- Motivation: marine_spatial_planning.py:97 `1 - depth/max_depth` gives 3.0 for depth=-100 (land), inflating combined suitability; design_mpa_network quantile(1-target_coverage) raises for coverage >1 and degenerates for 0/1; analyze_coastal_erosion raises raw KeyError for missing labels and xr.concat([]) ValueError for <2 periods.
- Evidence: `marine_spatial_planning.py:97`
- Bounded next step: Clamp/exclude negative depths; validate target_coverage in (0,1]; raise clear ValueError for <2 time periods.
- Acceptance probe: `depth=-100 cell -> suitability <= wind_suitability; coverage=1.2 -> clear ValueError`
- Effort: S

**GS-170 · MINOR · MARINE · DO-saturation docstring attributes two different models**
- Motivation: marine water_quality.py calculate_do_saturation docstring names 'Garcia-Gordon (1992)' then 'the Weiss (1970) solubility formulation' in the same block; implemented constants are the Weiss (1970) set; docs-truth fix.
- Evidence: `marine water_quality.py:calculate_do_saturation`
- Bounded next step: Correct attribution to Weiss (1970).
- Acceptance probe: `static-review`
- Effort: S

**GS-171 · MINOR · FOREST · Unguarded max-normalization propagates NaN for all-zero inputs (4 sites)**
- Motivation: forest_inventory.py:38 `tree_density / tree_density.max()` gives all-NaN biomass for uniform-zero density; wildfire_risk.py:137 fuel_load/fuel_load.max(), :141 wind_speed/wind_speed.max(), :266 same in predict_fire_spread; marine already ships the guarded helper _normalized (marine_spatial_planning.py:8-21) that could be mirrored in forest utils.
- Evidence: `forest_inventory.py:38` · `wildfire_risk.py:137` · `wildfire_risk.py:141` · `wildfire_risk.py:266` · `marine_spatial_planning.py:8-21`
- Bounded next step: Add a guarded max-normalize util and use it at the four sites.
- Acceptance probe: `all-zero tree_density/fuel_load -> finite output, not NaN`
- Effort: S

**GS-172 · MINOR · FOREST · detect_canopy_gaps: min_gap_pixels dead parameter; promised gap-size statistics missing**
- Motivation: canopy_analysis.py:165 accepts min_gap_pixels (documented at :176 as 'Minimum contiguous gap size in pixels') but the body never references it; docstring :179 promises 'gap size statistics' while only gap_fraction/counts are returned in attrs.
- Evidence: `canopy_analysis.py:165` · `canopy_analysis.py:176` · `canopy_analysis.py:179`
- Bounded next step: Implement contiguous-gap labeling (scipy.ndimage.label - scipy already a dependency) honoring min_gap_pixels, or drop the parameter and fix the docstring.
- Acceptance probe: `single isolated low-NDVI pixel with min_gap_pixels=5 -> filtered post-fix`
- Effort: S

**GS-173 · MINOR · FOREST · Time-series change detection: baseline includes current observation; early z-scores NaN**
- Motivation: deforestation.py:102-103 rolling(time=window_size, min_periods=1).mean()/std() is backward-inclusive, so each NDVI value is compared against a baseline containing itself (anomaly damped); :104 xr.where(std < 0.01, 0.01, std) does not replace NaN (NaN<0.01 is False) -> NaN z-scores for early steps.
- Evidence: `deforestation.py:102-103` · `deforestation.py:104`
- Bounded next step: Shift the baseline one step (historical-only window) and coerce NaN std to the 0.01 floor.
- Acceptance probe: `step-drop series -> post-fix z-score at the drop step is finite and significant_decrease fires`
- Effort: S

**GS-174 · MINOR · FOREST · KBDI never processes the first day's weather**
- Motivation: fire_risk.py:60-63 kbdi[0] = initial_kbdi then loops from i=1 - day 0's rain (deficit reduction) and temperature (drying) are dropped.
- Evidence: `fire_risk.py:60-63`
- Bounded next step: Process day 0 starting from initial_kbdi (or document initial_kbdi as end-of-day-0).
- Acceptance probe: `1-day series, 100 mm rain, initial 400 -> post-fix KBDI < 400`
- Effort: S

**GS-175 · MINOR · FOREST · Fuel-moisture docstring attributes Nelson (2000), implements Simard EMC**
- Motivation: fire_risk.py:114-117 claims 'Nelson (2000) formulation' but the piecewise equations (:125-139) are the Simard/Jonas equilibrium-moisture-content tables; docs-truth fix.
- Evidence: `fire_risk.py:114-117` · `fire_risk.py:125-139`
- Bounded next step: Correct attribution.
- Acceptance probe: `static-review`
- Effort: S

**GS-176 · MINOR · ENERGY · transmission_capacity and max_distance are dead parameters (documented, never used)**
- Motivation: energy_grid.py:26,34 - transmission_capacity accepted by optimize_grid_network, never referenced in the body; energy_infrastructure.py:27,36 - max_distance documented but unused (docstring itself admits the demand proxy is density, not distance, :44-45).
- Evidence: `energy_grid.py:26` · `energy_grid.py:34` · `energy_infrastructure.py:27` · `energy_infrastructure.py:36` · `energy_infrastructure.py:44-45`
- Bounded next step: Remove both parameters, or implement clipping of surplus/deficit by transmission capacity for the former.
- Acceptance probe: `results identical with/without parameter pre-fix; parameter gone (or effective) post-fix; `uv run --no-sync python -m pytest GEO-INFER-ENERGY/tests/unit -q` green`
- Effort: S

**GS-178 · MINOR · ENERGY · Unknown fuel/resource types silently priced at defaults**
- Motivation: carbon_footprint.py:45 `emission_factors.get(fuel_type, 350)` bills a typo'd fuel at the natural-gas factor; renewable_resources.py:399 `capital_costs.get(resource_type, 2000)` silently prices WAVE/TIDAL (absent from the table) at 2000 USD/kW.
- Evidence: `carbon_footprint.py:45` · `renewable_resources.py:399`
- Bounded next step: Raise on unknown fuel_type; add WAVE/TIDAL entries or raise for missing resource types.
- Acceptance probe: `calculate_emissions(x, 'coalz') raises post-fix`
- Effort: S

**GS-179 · MINOR · ENERGY · Capacity-factor unit contract missing; solar branch near-zero for documented units**
- Motivation: renewable_resources.py:312 divides solar irradiance by 1000 (W/m2 assumption) but the docstring (:303-306) gives no units, while assess_solar_potential documents kWh/m2/day - feeding that raster yields CF ~0.5% silently; also hours_analyzed = len(power_fraction) (:351) counts only the first dim for (time,y,x) input.
- Evidence: `renewable_resources.py:312` · `renewable_resources.py:303-306` · `renewable_resources.py:351`
- Bounded next step: Document/validate W/m2 (or auto-detect kWh/m2/day scale), fix hours_analyzed to .size.
- Acceptance probe: `5.5 kWh/m2/day raster -> documented behavior, not silent CF ~0.005`
- Effort: S

**GS-180 · MINOR · ENERGY · Unguarded max-normalization in facility siting**
- Motivation: energy_infrastructure.py:42 `resource_potential / resource_potential.max()` gives all-NaN suitability for an all-zero potential map (demand branch :46 has the +1e-10 guard; resource branch does not); mirrors the FOREST normalization item.
- Evidence: `energy_infrastructure.py:42` · `energy_infrastructure.py:46`
- Bounded next step: Apply the same guarded normalize helper.
- Acceptance probe: `all-zero resource_potential -> finite suitability`
- Effort: S

**GS-181 · MINOR · ENERGY · fit_weibull early-return paths omit std_speed key**
- Motivation: wind_analysis.py:50 and :56 return dicts lacking 'std_speed' while the success path includes it (:62-66) - callers keying on std_speed get KeyError for degenerate (all-zero / constant) wind data.
- Evidence: `wind_analysis.py:50` · `wind_analysis.py:56` · `wind_analysis.py:62-66`
- Bounded next step: Include 'std_speed': 0.0 (or the actual std) in both early returns.
- Acceptance probe: `fit_weibull(np.zeros(5))['std_speed'] == 0.0 post-fix (KeyError pre-fix)`
- Effort: S

### Human geography (PLACE · TRANSPORT · AG · CIV · BIO)

**GS-041 · MINOR · TRANSPORT · TRANSPORT docs say seven road classes, code ships eight; install block fights uv workspace**
- Motivation: GEO-INFER-TRANSPORT/docs/index.md states 'Seven road classes are supported: motorway, trunk, primary, secondary, tertiary, residential, service, path' - the sentence lists eight and RoadClass defines exactly those 8 members including PATH. docs/getting_started.md repeats 'seven-class road hierarchy' with a table omitting PATH. Additionally getting_started.md instructs 'uv pip install -e ./GEO-INFER-TRANSPORT' plus 'uv pip install folium geopandas', which fights the root uv workspace (AGENTS.md Modular Hygiene: canonical env is root pyproject/uv.lock, module sync via 'uv sync --package geo-infer-transport') and installs unpinned deps outside the lockfile. All other index.md claims verified true against code (methods, Route fields, version 0.2.0, LOG integration).
- Evidence: `GEO-INFER-TRANSPORT/docs/index.md` · `GEO-INFER-TRANSPORT/src/geo_infer_transport/core/network.py:17-27` · `GEO-INFER-TRANSPORT/docs/getting_started.md`
- Bounded next step: fix both count claims to eight (add PATH row to the getting_started table) and replace the install block with 'uv sync --package geo-infer-transport'
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_transport.core.network import RoadClass; print(len(RoadClass))" prints 8 and docs no longer assert 'seven'; grep docs for 'uv pip install' returns none`
- Effort: S — Note: Count verified by code read of RoadClass, not by an executed probe

**GS-185 · MINOR · TRANSPORT · Reverse graph edges lack travel_time; reverse-direction routes cost ~1s per edge** · [probe-confirmed]
- Motivation: build_from_edges writes travel_time only on the forward edge; the auto-created reverse edge gets edge_id/length/speed_limit/road_class only (network.py:168-175 vs forward at 152-159). route() weights on travel_time (routing.py:128,136-138) and networkx defaults a missing weight attr to 1.0. Proven live with a uv run --no-sync probe on the current tree: forward edge cost 72.0 s, reverse edge REV_COST 1 for the same 1 km/50 km/h segment; every route that traverses any segment in its reverse direction under-costs it by ~70x, distorting shortest paths, and _apply_traffic_weights compounds it (traffic-adjusted reverse edges get weight 0 so dijkstra prefers them, routing.py:259-263 via network.py data).
- Evidence: `network.py:152-159` · `network.py:168-175` · `routing.py:128` · `routing.py:136-138` · `routing.py:259-263`
- Bounded next step: Write travel_time=travel_time_s on the reverse edge in build_from_edges; add a regression test asserting nx.dijkstra_path_length(g,'b','a',weight='travel_time') == length/(speed)*3600 and a traffic-adjusted reverse-edge test.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_transport import TransportNetwork; import networkx as nx; n=TransportNetwork(); n.build_from_edges([{'id':'e1','from':'a','to':'b','length_m':1000,'speed_limit':50}]); print(nx.dijkstra_path_length(n.graph,'b','a',weight='travel_time'))" → must print 72.0 (currently 1.0)`
- Effort: S

**GS-186 · MINOR · TRANSPORT · Unroutable origin/destination crashes route()/calculate_isochrone() instead of degrading**
- Motivation: routing.py:167 catches only nx.NetworkXNoPath; nx.NodeNotFound (raised by dijkstra_path for unknown nodes, including the 'origin'/'destination' fallback ids at routing.py:116-117 when callers pass lat/lon dicts) propagates. Same hole in accessibility.py:109 (except (nx.NetworkXError, KeyError) — NodeNotFound subclasses NetworkXException, not NetworkXError), contradicting its own warning text 'degenerates to the origin node only' (111-113). SKILL.md documents only the no-path empty-Route contract.
- Evidence: `routing.py:167` · `routing.py:116-117` · `accessibility.py:109` · `accessibility.py:111-113` · `SKILL.md`
- Bounded next step: Catch (nx.NetworkXNoPath, nx.NodeNotFound) in routing.py route()/alternatives and accessibility.py; document the missing-node behavior in SKILL.md Guidelines next to the route_source paragraph.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_transport import TransportNetwork, RoutingEngine; n=TransportNetwork(); n.build_from_edges([{'id':'e1','from':'a','to':'b','length_m':1000,'speed_limit':50}]); print(RoutingEngine(network=n).route({'node_id':'zz'},{'node_id':'b'}).route_source)" → should print 'network' with empty path, currently raises NodeNotFound`
- Effort: S

**GS-187 · MINOR · TRANSPORT · TrafficAnalyzer model_type='akcelik'/'hcm' accepted and documented but never implemented**
- Motivation: __init__ docstring advertises 'bpr', 'akcelik', 'hcm' (traffic.py:74); self.model_type is stored (78) and referenced only by a log line (81). model_congestion takes its own algorithm param and treats everything non-'bpr' as a linear fallback (traffic.py:~200-215). Any value passes construction with zero effect — a silent-fallback pattern the repo's auditors target.
- Evidence: `traffic.py:74` · `traffic.py:78` · `traffic.py:81` · `traffic.py:~200-215`
- Bounded next step: Validate model_type in {'bpr'} at construction (raise ValueError otherwise) and fix the docstring, or actually implement akcelik; either way stop storing an unused knob.
- Acceptance probe: `TrafficAnalyzer(model_type='akcelik') should raise ValueError after fix`
- Effort: S

**GS-188 · MINOR · TRANSPORT · route() accepts mode/avoid/via and silently ignores them; time_step division unguarded**
- Motivation: routing.py:91-94 parameters appear only in the signature/docstring (102-105); the body never reads them. RoutingAlgorithm.BIDIRECTIONAL similarly falls through to the plain Dijkstra branch (routing.py:~141-151) with no log. Also simulate_traffic divides by time_step_seconds unguarded (traffic.py:281) → ZeroDivisionError.
- Evidence: `routing.py:91-94` · `routing.py:102-105` · `routing.py:~141-151` · `traffic.py:281`
- Bounded next step: Either implement via-waypoint chaining (route legs through via nodes) or delete the parameters and add time_step_seconds > 0 validation; document whichever contract survives in SKILL.md.
- Acceptance probe: `grep shows zero reads of avoid/via in routing.py body post-fix; simulate_traffic(..., time_step_seconds=0) raises ValueError`
- Effort: S

**GS-189 · MINOR · TRANSPORT · No test exercises reverse-direction routing or missing-node routing**
- Motivation: tests/unit/test_network_routing.py covers route_source (256-269) and traffic-adjusted weights on forward edges (271-315); nothing asserts the reverse-edge time cost or NodeNotFound behavior — exactly the paths broken above (the reverse-edge bug survived because of this).
- Evidence: `test_network_routing.py:256-269` · `test_network_routing.py:271-315`
- Bounded next step: Add the two regression tests from the item above to test_network_routing.py alongside existing route tests.
- Acceptance probe: `uv run python -m pytest GEO-INFER-TRANSPORT/tests/unit/test_network_routing.py -k reverse -v passes post-fix, fails pre-fix`
- Effort: S

**GS-191 · MINOR · PLACE · CascadianAgriculturalH3Backend.__init__ defines the target region twice despite the cache**
- Motivation: unified_backend.py:96-98 computes _define_target_region_cached (which itself calls _define_target_region at 292 when uncached), then lines 125-127 unconditionally recompute _define_target_region(target_counties) ignoring the cached result. For the default {'CA': ['all'], 'OR': ['all']} this regenerates the full county-geometry + hexagon set at every construction, defeating the cache built two lines earlier.
- Evidence: `unified_backend.py:96-98` · `unified_backend.py:125-127` · `unified_backend.py:292`
- Bounded next step: Assign self.target_hexagons_by_state, self.target_hexagons = _hexagons_by_state, all_hexagons (the cached values) and drop the second call.
- Acceptance probe: `Single init log line "Using cached region data" with no following "Generating new region data"; or count _define_target_region invocations with a monkeypatched counter in a unit test`
- Effort: S

**GS-192 · MINOR · PLACE · Bioregion forest layer swallows all hexagon-render exceptions**
- Motivation: bioregion_visualization.py:231-233 'except Exception: continue' inside the per-hexagon loop — a data-shape bug (e.g. missing keys) silently renders an empty/partial forest layer with only the aggregate 'Added N hexagons' log (which still prints the attempted count). Contrast the module's own hardened style elsewhere (visualization_engine.py validates bounds/zoom/tiles explicitly).
- Evidence: `bioregion_visualization.py:231-233` · `visualization_engine.py`
- Bounded next step: Narrow to the expected exception (TypeError/KeyError), log the skipped hexagon id at warning level, and count skips in the layer summary.
- Acceptance probe: `Feed one malformed hexagon record; log must show the warning and the layer result must report 1 skipped`
- Effort: S

**GS-193 · MINOR · PLACE · core/comprehensive_dashboard shim still treated as canonical import path by tests**
- Motivation: core/comprehensive_dashboard.py:1-7 exists only to re-export DelNorteComprehensiveDashboard from locations/del_norte_county/comprehensive_dashboard.py; tests/unit/test_comprehensive_dashboard.py:3,11 still imports via the shim ("Source: src/geo_infer_place/core/comprehensive_dashboard.py"), while sibling tests (test_crescent_city_intel.py:386, test_del_norte_demo_orchestration.py:126) use the canonical path. Repo convention is clean cutover, no shims.
- Evidence: `core/comprehensive_dashboard.py:1-7` · `test_comprehensive_dashboard.py:3` · `test_comprehensive_dashboard.py:11` · `test_crescent_city_intel.py:386` · `test_del_norte_demo_orchestration.py:126`
- Bounded next step: Point test_comprehensive_dashboard.py at the canonical path and delete the shim module.
- Acceptance probe: `grep -r "core.comprehensive_dashboard" GEO-INFER-PLACE/src GEO-INFER-PLACE/tests → zero hits`
- Effort: S

**GS-195 · MINOR · AG · Tier2 carbon fit silently drops pools with <10 training rows; predict undercounts totals**
- Motivation: carbon_sequestration.py:189-191 'if len(y.dropna()) < 10: continue' — the pool is skipped with no warning, and predict (355-357) sums only self.predictors pools into total_sequestration_rate, so a one-pool fit yields half the expected sequestration with no signal. Related test gap: tests only cover tier2 construction and the missing-field-data error (test_carbon_sequestration.py:62-65); no tier2 fit→predict happy-path test exists.
- Evidence: `carbon_sequestration.py:189-191` · `carbon_sequestration.py:355-357` · `test_carbon_sequestration.py:62-65`
- Bounded next step: Warn (or raise) on skipped pools in fit, and record which pools were skipped in result['metadata']; add a tier2 fit→predict unit test with 12 rows asserting both pools appear.
- Acceptance probe: `uv run python -m pytest GEO-INFER-AG/tests/unit/models/test_carbon_sequestration.py -k tier2 gains a passing fit→predict test`
- Effort: S

**GS-196 · MINOR · AG · detect_growing_season documents a 'savitzky_golay' method that raises ValueError**
- Motivation: seasonal_analysis.py:61 lists ('threshold', 'derivative', 'savitzky_golay'), but the implementation only handles threshold/derivative and hits raise ValueError(f"Unsupported detection method: {method}") (206-207) for the third.
- Evidence: `seasonal_analysis.py:61` · `seasonal_analysis.py:206-207`
- Bounded next step: Remove 'savitzky_golay' from the docstring (or implement via scipy.signal.savgol_filter smoothing before the threshold pass — bounded either way).
- Acceptance probe: `SeasonalAnalysis(...).detect_growing_season(method='savitzky_golay') must not raise after fix`
- Effort: S

**GS-197 · MINOR · AG · Dead model parameter plus unguarded field_id access in assess_carbon_sequestration**
- Motivation: The model: str = "tier1" parameter (sustainability.py:52) is documented with three strategies ('tier1'/'tier2'/'century', ~line 65) but never read in the body; separately, when management_practices is passed the code does result_data["field_id"] == field_id (~line 113) with no guard — a GeoDataFrame without a field_id column raises bare KeyError instead of a named error. Test suite never passes model= (test_sustainability.py:26-79).
- Evidence: `sustainability.py:52` · `sustainability.py:~65` · `sustainability.py:~113` · `test_sustainability.py:26-79`
- Bounded next step: Delete the model param (or wire it to CarbonSequestrationModel), and raise ValueError("field data must include 'field_id' when management_practices are provided").
- Acceptance probe: `assess_carbon_sequestration(model='tier2') after fix either behaves differently or the param is gone; missing-field_id case raises the named ValueError`
- Effort: S

**GS-198 · MINOR · CIV · _check_disparate_impact ignores populations though docstring claims population adjustment**
- Motivation: policy_analysis.py:500-508 — signature takes populations: Dict[str, int], docstring says "(adjusted for population)", and set_group_impact stores self._group_populations (404), but the body (509+) uses only impacts; per-capita disparate impact (impact per capita, the standard 4/5ths framing) is never computed. analyze() passes populations in but the flags are identical with or without it.
- Evidence: `policy_analysis.py:500-508` · `policy_analysis.py:404` · `policy_analysis.py:509+`
- Bounded next step: Either implement per-capita rates (impact/population before the 4/5ths comparison) or drop the parameter and fix the docstring; add a test where raw impacts favor one group but per-capita rates invert the flag.
- Acceptance probe: `Two groups with equal absolute impact but 10x population difference must produce different flags post-fix`
- Effort: S

**GS-199 · MINOR · CIV · predict_turnout docstring claims a linear model fit; implementation is baseline + offsets**
- Motivation: civic_engagement.py:463 — implementation is baseline average + fixed contested/media offsets (no fit, no linear model anywhere in the class). Callers reasoning about model behavior (extrapolation, interaction of factors) get the wrong contract.
- Evidence: `civic_engagement.py:463`
- Bounded next step: Reword the docstring to "baseline rate for the election type with contestedness and media-coverage offsets"; alternatively fit an actual per-type regression if the linear claim is intentional.
- Acceptance probe: `Docstring grep no longer says "linear model" OR a fit step exists in the method`
- Effort: S

**GS-200 · MINOR · CIV · Duplicate stakeholder group names silently collapse in compute_impact_matrix only**
- Motivation: policy_analysis.py compute_impact_matrix keys the result by impact.group_name (~line 316-336), so two StakeholderImpact entries for the same group keep only the last, while compute_aggregate_score (330-345) population-weights every record — the two public methods disagree on the same data with no warning.
- Evidence: `policy_analysis.py:~316-336` · `policy_analysis.py:330-345`
- Bounded next step: Warn on duplicate group names (or aggregate per-group in the matrix too), and add a test with a duplicated group.
- Acceptance probe: `Two impacts for one group → matrix reports the merge (or a warning) instead of the silent last-wins row`
- Effort: S

**GS-202 · MINOR · BIO · load_custom_soil_data detects lat/lon/lng headers then hardcodes row.latitude**
- Motivation: soil.py:205-210 resolves the actual latitude/longitude column names (accepting "lat", "lon", "lng"), but the coordinate dicts hardcode row.latitude / row.longitude (229-230). A CSV using the accepted short headers passes validation, then crashes with AttributeError inside iterrows. latitude_column/longitude_column are otherwise unused.
- Evidence: `soil.py:205-210` · `soil.py:229-230`
- Bounded next step: Use row[latitude_column] / row[longitude_column] and add a fixture test with a lat,lng-headed CSV.
- Acceptance probe: `SoilDataIntegrator().load_custom_soil_data(lat_lng_csv, coords, {...}) returns a SoilDataset post-fix; AttributeError pre-fix`
- Effort: S

**GS-203 · MINOR · BIO · EMP quality filter assumes a ph column and KeyErrors on custom metadata**
- Motivation: microbiome.py:144 metadata[metadata["ph"].between(0, 14)] runs unconditionally when quality_filters=True; load_emp_data(metadata_path=...) explicitly supports arbitrary local metadata files (which may lack ph), and _apply_quality_filters only guards the lat/lon columns (127-137). The exception surfaces as a bare KeyError from deep inside the loader.
- Evidence: `microbiome.py:144` · `microbiome.py:127-137`
- Bounded next step: If "ph" in metadata.columns guard with a debug log for the skipped filter; add a no-ph-column fixture test.
- Acceptance probe: `load_emp_data(metadata_path=no_ph.tsv, quality_filters=True) returns a dataset post-fix`
- Effort: S

**GS-204 · MINOR · BIO · /analyze/file leaks delete=False temp files when processing raises**
- Motivation: rest_api.py:136-144 writes two delete=False temp files; cleanup is only the unconditional tail at 186-188, so any exception between (bad CSV in pd.read_csv(spatial_path), malformed FASTA, validation errors) leaves orphaned files in the system temp dir per failed request.
- Evidence: `rest_api.py:136-144` · `rest_api.py:186-188`
- Bounded next step: Wrap the load/analyze section in try/finally that unlinks both paths (only those that were created).
- Acceptance probe: `Request with a corrupt spatial CSV in the TestClient; assert fasta_path no longer exists after the 500`
- Effort: S

### Infrastructure & operations (API · APP · GIT · OPS · LOG · SEC · COMMS)

**GS-026 · MINOR · COMMS · COMMS mypy python_version pinned 3.12 while all 44 other members use 3.11**
- Motivation: GEO-INFER-COMMS's [tool.mypy] config sets python_version = "3.12" where all 44 other member configs say "3.11" — fleet mypy target drift.
- Evidence: `GEO-INFER-COMMS/pyproject.toml`
- Bounded next step: Set the COMMS [tool.mypy] python_version to "3.11".
- Acceptance probe: `grep -n 'python_version' GEO-INFER-COMMS/pyproject.toml (expect 3.11)`
- Effort: S — Note: INSURANCE __version__/author-metadata portion of this report item omitted per D3 (owned by RiskEarth lane)

**GS-064 · MINOR · COMMS · five exported spatial helper functions untested (buffer_point latitude-blind)**
- Motivation: calculate_distance, create_bounds_from_points, buffer_point, geojson_to_geospatial_point and geospatial_point_to_geojson are public exports but grep across GEO-INFER-COMMS/tests finds zero references (test_spatial_models.py covers distance_to and SpatialIndex, not these). buffer_point uses a flat 1deg ~= 111,000 m conversion with no latitude scaling and create_bounds_from_points raises on empty input; both unverified public behavior.
- Evidence: `GEO-INFER-COMMS/src/geo_infer_comms/models/spatial.py:417-480` · `geo_infer_comms/__init__.py:90-95` · `geo_infer_comms/__init__.py:521-526` · `GEO-INFER-COMMS/src/geo_infer_comms/models/spatial.py:440-441` · `GEO-INFER-COMMS/src/geo_infer_comms/models/spatial.py:424-425`
- Bounded next step: add unit tests to test_spatial_models.py pinning: distance wrapper equals point.distance_to; bounds construction incl. the empty-list ValueError; buffer dimensions at two latitudes (documents the equator approximation); geojson<->point round trip
- Acceptance probe: `uv run --no-sync pytest GEO-INFER-COMMS/tests/unit/test_spatial_models.py -q green; grep calculate_distance GEO-INFER-COMMS/tests -r non-empty`
- Effort: S

**GS-210 · MINOR · API · Packaging: four declared runtime deps are never imported by the package**
- Motivation: pyproject.toml (and requirements.txt) declare httpx, python-dotenv, python-multipart, and requests; grep across the module shows none are imported in src/ (requests appears only in examples/python_client.py; uvicorn only under app.py __main__). Every install pulls four unneeded packages and the lists propagate through 9 README copies.
- Evidence: `GEO-INFER-API/pyproject.toml:29-36` · `requirements.txt:2` · `requirements.txt:5-7` · `examples/python_client.py:13` · `app.py:77`
- Bounded next step: Drop httpx/python-dotenv/python-multipart/requests from pyproject dependencies (move requests to an examples extra if examples stay shipped); regenerate the README dependency lists.
- Acceptance probe: `uv run --no-sync python -c "import geo_infer_api.app" with SECRET_KEY set succeeds; grep -rE 'import (httpx|requests)|dotenv|multipart' GEO-INFER-API/src returns nothing`
- Effort: S

**GS-211 · MINOR · API · Robustness: unbounded in-memory polygon store grows without cap**
- Motivation: POLYGON_FEATURES dict has no size cap; POST /collections/polygons/items appends forever. SKILL.md discloses process-local memory but not the unbounded-growth leak on a long-running worker.
- Evidence: `geojson_router.py:44`
- Bounded next step: Configurable max-size in Settings with 409-or-eviction on overflow, documented in SKILL.md.
- Acceptance probe: `Targeted pytest via TestClient creating cap+1 features asserting the bound behavior`
- Effort: S

**GS-213 · MINOR · APP · Doc truth: module docstring describes a nonexistent GEO-INFER-AGENT integration**
- Motivation: api/agent_api.py claims it provides integration with GEO-INFER-AGENT and initialize() says connect to agent service, but the class is an in-process JSON-persisted registry (SKILL.md states this explicitly).
- Evidence: `api/agent_api.py:2-9` · `api/agent_api.py:69-70` · `SKILL.md:86`
- Bounded next step: Rewrite the module docstring and the initialize/create_agent docstrings to describe the in-process registry.
- Acceptance probe: `grep -n 'connect to agent service|integration with GEO-INFER-AGENT' GEO-INFER-APP/src/geo_infer_app/api/agent_api.py returns nothing`
- Effort: S

**GS-216 · MINOR · GIT · Packaging: colorlog declared but never imported**
- Motivation: pyproject.toml declares colorlog>=6.7.0; no colorlog import anywhere in GEO-INFER-GIT/src or tests.
- Evidence: `pyproject.toml:29`
- Bounded next step: Drop the dep or route utils/logging_utils.py through colorlog; update the 9 README dependency lists.
- Acceptance probe: `grep -rn colorlog GEO-INFER-GIT/src GEO-INFER-GIT/pyproject.toml shows no declared+unimported dep`
- Effort: S

**GS-217 · MINOR · GIT · Maintainability: standalone clone scripts duplicate the main pipeline**
- Motivation: clone_script.py (246 lines, does not rely on module imports, subprocess cloning) and clone_repos.py re-implement the src/geo_infer_git/main.py pipeline; README.md still lists clone_script.py functions as Public Interface. The duplicate ignores CloneConfig, retry/rate-limit handling, and gitignore-entry logic main.py has.
- Evidence: `GEO-INFER-GIT/clone_script.py` · `clone_repos.py` · `README.md:22-27` · `main.py:44-101` · `main.py:217-238`
- Bounded next step: Reduce README Public Interface to the package surface; delete or shrink the standalone scripts to a thin wrapper over geo_infer_git.main.
- Acceptance probe: `grep -n 'clone_script' GEO-INFER-GIT/README.md empty; uv run --no-sync python -m geo_infer_git.main --help still works`
- Effort: S

**GS-218 · MINOR · GIT · Test gap: console-script CLI untested**
- Motivation: pyproject.toml installs geo-infer-git = geo_infer_git.cli:main; cli.py (317 lines) has arg validation, exit-code semantics (clone_command sys.exit(1) on partial failure), and yaml/json dispatch that no test exercises.
- Evidence: `pyproject.toml:67-68` · `cli.py`
- Bounded next step: Argparse-level tests for load_repo_list (yaml/json/bad type) and command handlers with a monkeypatched RepoManager asserting exit codes.
- Acceptance probe: `New tests/unit/test_cli.py passes under uv run --no-sync python -m pytest GEO-INFER-GIT/tests -k cli -q`
- Effort: S

**GS-221 · MINOR · OPS · Packaging/doc truth: app.py import side effects + stale version string**
- Motivation: app.py runs app, config = create_app() at import, loading config/local.yaml and mutating root logging handlers merely from importing geo_infer_ops.app; app.py hardcodes FastAPI version=0.1.0 while __version__ is 0.2.0 (pyproject.toml), so OpenAPI metadata and /version disagree.
- Evidence: `app.py:86` · `app.py:53` · `pyproject.toml:7`
- Bounded next step: Guard app construction under __main__/factory; read version from geo_infer_ops.__version__.
- Acceptance probe: `uv run --no-sync python -c "import geo_infer_ops.app" adds no root handlers (mirror test_logging_hygiene.py check)`
- Effort: S

**GS-223 · MINOR · LOG · Error handling: blanket except Exception to HTTP 400 masks server faults**
- Motivation: Every handler in api/transport.py (e.g. plan_route ~157-171, compare_routes, network endpoints) wraps calls in except Exception as e: raise HTTPException(400, str(e)). AttributeError/TypeError (server faults) become 400s with internal exception text in detail.
- Evidence: `api/transport.py:157-171`
- Bounded next step: Map domain ValueErrors to 400; let unexpected exceptions surface as 500 via a shared handler (pattern exists in GEO-INFER-API ErrorHandlerMiddleware).
- Acceptance probe: `Targeted TestClient test asserting TypeError to 500 and bad request to 400`
- Effort: S

**GS-225 · MINOR · SEC · Robustness: CLI failures exit 0**
- Motivation: command_anonymize logs Input file must contain geospatial data and returns; main() dispatches without checking success, so every handled failure (non-geospatial CSV, unsupported format) yields exit 0. Scripted pipelines cannot detect failure.
- Evidence: `cli.py:285-287` · `cli.py:706-714`
- Bounded next step: Command handlers return bool; main() sys.exit(1) on failure (mirrors GEO-INFER-GIT/cli.py semantics).
- Acceptance probe: `geo-infer-sec anonymize plain.csv out.geojson exits non-zero`
- Effort: S

**GS-228 · MINOR · COMMS · Robustness: publish_event crashes on flat-key geospatial context and skips spatial indexing for it**
- Motivation: core/events.py spatial indexing only accepts nested geo_data[location][longitude/latitude]; a flat context {latitude, longitude} is silently not indexed, and a location dict missing keys raises uncaught KeyError mid-publish (inside the lock, after the event was stored but before queueing; metrics not updated). The filter path accepts flat keys via _extract_geospatial_coord (events.py:31-44, 599-602) — two accepted shapes for the same field, one crashes, one under-delivers.
- Evidence: `core/events.py:150-158` · `events.py:31-44` · `events.py:599-602`
- Bounded next step: Route indexing through _extract_geospatial_coord; validate and raise typed ValueError before mutating state.
- Acceptance probe: `Targeted pytest — flat-key publish + filtered subscription delivers; malformed location raises ValueError leaving events/metrics consistent`
- Effort: S

### Society (ECON · EDU · HEALTH · METAGOV · NORMS · ORG · PEP)

**GS-236 · MINOR · ECON · Inconsistent empty-baseline error handling across assess_* methods**
- Motivation: assess_fiscal_policy raises ValueError("Baseline GDP data required...") when baseline is missing, but assess_infrastructure_policy and assess_environmental_policy read baseline_data.get("gdp", {}) with no check and silently return empty PolicyImpact dicts. Same input class, three different failure modes.
- Evidence: `GEO-INFER-ECON/src/geo_infer_econ/core/policy_engine.py:135-136` · `GEO-INFER-ECON/src/geo_infer_econ/core/policy_engine.py:200` · `GEO-INFER-ECON/src/geo_infer_econ/core/policy_engine.py:292` · `GEO-INFER-ECON/tests/unit/test_policy_engine.py:138-147`
- Bounded next step: Extract a shared _require_baseline("gdp") helper used by all assess methods; add a test that each raises when baseline is absent.
- Acceptance probe: `pytest -k no_baseline extended to infrastructure/environmental (currently only fiscal is tested, test_policy_engine.py:138-147)`
- Effort: S

**GS-237 · MINOR · ECON · EconomicAnalysisAPI accepts ANY bearer token; 401 path is dead code**
- Motivation: _authenticate_request falls through to try: return "authenticated_user" for any token that isn't an API key; the except -> HTTPException(401) branch is unreachable (a bare return cannot raise), so invalid tokens authenticate despite the docstring claiming advanced authentication/authorization. Tests only cover a MISSING header, never an invalid token.
- Evidence: `GEO-INFER-ECON/src/geo_infer_econ/api/economic_api.py:236-253` · `GEO-INFER-ECON/tests/unit/test_enhanced_capabilities.py:417`
- Bounded next step: Reject unknown tokens with 401 (or at minimum add a test pinning Bearer garbage -> 401 so the hole is visible until real JWT lands).
- Acceptance probe: `uv run --no-sync python -c with starlette TestClient posting /api/models/execute with Authorization: Bearer wrong -> expect 401 (currently 200-class success)`
- Effort: S

**GS-238 · MINOR · ECON · Hourly rate limit is unenforceable**
- Motivation: _check_rate_limit purges every entry older than minute_ago (60s) before counting, so recent_hour can never observe more than one minute of history; requests_per_hour (default 1000) can never trigger.
- Evidence: `GEO-INFER-ECON/src/geo_infer_econ/api/economic_api.py:210-219`
- Bounded next step: Keep two windows (don't purge < 1h entries) or drop the hourly knob from config.
- Acceptance probe: `unit test sending requests_per_minute+1 spread over 61 simulated seconds via monkeypatched time.time`
- Effort: S

**GS-239 · MINOR · ECON · networkx declared but never imported**
- Motivation: pyproject.toml declares networkx>=2.8.0 (README Dependencies section repeats it), but grep over GEO-INFER-ECON/src finds zero import networkx.
- Evidence: `GEO-INFER-ECON/pyproject.toml:38`
- Bounded next step: Remove from dependencies (and README deps list) or wire it where it was intended.
- Acceptance probe: `uv run --no-sync python -m pytest GEO-INFER-ECON/tests/unit/test_import_smoke.py still green after removal`
- Effort: S

**GS-240 · MINOR · EDU · Comment/code drift in competency downgrade logic**
- Motivation: Comment says "Update only if new level is higher or same with higher confidence", but the guard is only _COMPETENCY_LEVEL_ORDER[level] >= _COMPETENCY_LEVEL_ORDER[record.level]; confidence is never compared (record.confidence + 0.1 unconditionally). SKILL.md's no-silent-downgrade guarantee still holds, but the confidence half of the documented rule does not exist.
- Evidence: `GEO-INFER-EDU/src/geo_infer_edu/core/progress.py:247-248` · `GEO-INFER-EDU/src/geo_infer_edu/core/progress.py:251`
- Bounded next step: Either implement the confidence comparison for equal-level updates or correct the comment.
- Acceptance probe: `unit test: two same-level assessments with different confidence -> assert documented behavior`
- Effort: S

**GS-241 · MINOR · EDU · register_learner unconditionally wipes mastery data on re-registration**
- Motivation: self._mastery_data[profile.learner_id] = {} runs for every register_learner call; create_pathway always calls register_learner, so creating a second pathway for an existing learner destroys their accumulated mastery state used by recommendation/spaced-repetition paths.
- Evidence: `GEO-INFER-EDU/src/geo_infer_edu/core/personalization.py:145-161` · `GEO-INFER-EDU/src/geo_infer_edu/core/personalization.py:163`
- Bounded next step: Only initialize _mastery_data when the learner is new (if profile.learner_id not in self._learner_profiles:).
- Acceptance probe: `register learner -> update mastery -> create_pathway again -> assert mastery preserved`
- Effort: S

**GS-243 · MINOR · HEALTH · Hotspot loader crashes on non-point geometries and missing dates**
- Motivation: run_hotspot_analysis builds Location(latitude=row.geometry.y, longitude=row.geometry.x), raising AttributeError on polygon/multipolygon inputs, and report_date=row.get("report_date", None) — if the column is absent, DiseaseReport receives None for a required datetime field and pydantic validation aborts the whole run with a raw traceback.
- Evidence: `GEO-INFER-HEALTH/src/geo_infer_health/cli.py (run_hotspot_analysis)`
- Bounded next step: Reproject-and-extract centroid for non-point geometry; fail with a clear message when report_date is missing.
- Acceptance probe: `run analyze hotspots against a one-polygon GeoJSON fixture`
- Effort: S

**GS-244 · MINOR · HEALTH · Runtime log artifact tracked in module tree**
- Motivation: GEO-INFER-HEALTH/logs/health.log exists in the working tree and the module has no .gitignore (path not found), so CLI runs re-dirty the tree.
- Evidence: `GEO-INFER-HEALTH/logs/health.log`
- Bounded next step: Delete the log file and add logs/ to a .gitignore (or rely on root ignore if one covers it).
- Acceptance probe: `git status --porcelain GEO-INFER-HEALTH clean after a CLI smoke run`
- Effort: S

**GS-246 · MINOR · METAGOV · integrations extra covers only 1 of 4 shipped integration adapters**
- Motivation: pyproject.toml declares integrations = ["geo-infer-space"], but integrations/__init__.py unconditionally imports adapters for organizational (geo_infer_org), normative (geo_infer_norms), and security (geo_infer_sec) too (per-file guards degrade silently). Installing the extra as documented does not deliver the modules three of the four adapters bridge to.
- Evidence: `GEO-INFER-METAGOV/pyproject.toml` · `GEO-INFER-METAGOV/src/geo_infer_metagov/integrations/__init__.py:3-8` · `GEO-INFER-METAGOV/src/geo_infer_metagov/integrations/organizational.py:9-13` · `GEO-INFER-METAGOV/src/geo_infer_metagov/integrations/normative.py:9-12` · `GEO-INFER-METAGOV/src/geo_infer_metagov/integrations/security.py:9-12`
- Bounded next step: Add the workspace members to the extra (geo-infer-org, geo-infer-norms, and either ship or drop the SEC adapter), or document the partial coverage in SKILL.md.
- Acceptance probe: `uv run --no-sync python -c "import geo_infer_metagov as m; print(m.INTEGRATIONS_AVAILABLE)" in an env with only the extra installed`
- Effort: S

**GS-247 · MINOR · NORMS · compliance_level documented as 0.0-1.0 but unvalidated**
- Motivation: Field comment "0.0 to 1.0" and ComplianceStatus.create docstring, but nothing clamps or validates; evaluate_compliance aggregates weighted metric levels, so an out-of-range value propagates into compliance_level percentages reported by get_entity_compliance. ComplianceMetric.set_threshold/set_range DO validate evaluation_type — the validation pattern exists but is applied inconsistently.
- Evidence: `GEO-INFER-NORMS/src/geo_infer_norms/models/compliance_status.py:~30`
- Bounded next step: Validate in ComplianceStatus.create (raise ValueError outside [0,1]) + one unit test.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_norms.models.compliance_status import ComplianceStatus; ComplianceStatus.create('e','r',True,7.5)" -> expect ValueError post-fix`
- Effort: S

**GS-248 · MINOR · NORMS · Silent warn-and-continue on unknown norm IDs**
- Motivation: add_norm_relationship and set_prior_belief log a warning and silently return when a norm ID doesn't exist (no exception), so a typo'd ID produces a relationship/prior that simply doesn't exist downstream with no signal at query time.
- Evidence: `GEO-INFER-NORMS/src/geo_infer_norms/core/normative_inference.py`
- Bounded next step: Raise KeyError/ValueError for unknown IDs (callers pass IDs returned by add_norm, so this can't break the happy path); keep the warning only if a deprecation argument exists.
- Acceptance probe: `unit test pytest.raises(KeyError) on both methods with a bogus ID`
- Effort: S

**GS-250 · MINOR · PEP · Docstring contradicts implemented (and documented) failure contract**
- Motivation: Docstring claims "Raises: RuntimeError: If candidate not found or not in offer accepted state", but the code returns False for both cases and SKILL.md correctly documents False. Anyone coding against the docstring will write unreachable error handling.
- Evidence: `GEO-INFER-PEP/src/geo_infer_pep/methods.py:114` · `GEO-INFER-PEP/src/geo_infer_pep/methods.py:125-126` · `GEO-INFER-PEP/src/geo_infer_pep/methods.py:135`
- Bounded next step: Fix the docstring to match the return-False contract.
- Acceptance probe: `grep-level check that docstring Raises section lists only ValueError (missing candidate_id)`
- Effort: S

**GS-251 · MINOR · PEP · No coverage for the benefits_client/learning_client integration contract**
- Motivation: Grep over GEO-INFER-PEP/tests for benefits_client|learning_client|TypeError returns zero matches; SKILL.md documents these callables as invoked with the new employee, and the partial-store bug (GS-249) lives exactly on this path.
- Evidence: `GEO-INFER-PEP/tests (no matches for benefits_client|learning_client|TypeError)`
- Bounded next step: Add two tests to tests/unit/test_methods.py: callables invoked with the created employee; non-callable raises (post-fix) rather than returning False.
- Acceptance probe: `uv run --no-sync pytest GEO-INFER-PEP/tests/unit/test_methods.py -q -k client`
- Effort: S

### Agents & meta (AGENT · AI · ANT · ART · SIM · IOT · REQ)

**GS-065 · MINOR · AI · resolve_rng contract untested; resolve_optional_rng is a dead export**
- Motivation: AI's resolve_rng deliberately deviates from the four-module family by resolving None to fixed seed 0 instead of fresh entropy, and is the module's only call site, but no test anywhere imports geo_infer_ai.utils.rng while siblings RISK and BAYES each ship full suites pinning this exact contract. resolve_optional_rng has zero callers in src and tests: an exported function with no observable contract.
- Evidence: `GEO-INFER-AI/src/geo_infer_ai/utils/rng.py:6-9` · `GEO-INFER-AI/src/geo_infer_ai/utils/rng.py:30` · `GEO-INFER-AI/src/geo_infer_ai/utils/rng.py:33` · `GEO-INFER-AI/src/geo_infer_ai/utils/explainability.py:14` · `GEO-INFER-AI/src/geo_infer_ai/utils/explainability.py:147` · `GEO-INFER-RISK/tests/unit/test_rng.py:13-82` · `GEO-INFER-BAYES/tests/unit/test_rng.py:13` · `GEO-INFER-AI/src/geo_infer_ai/utils/rng.py:51-59` · `GEO-INFER-AI/src/geo_infer_ai/utils/__init__.py:3` · `GEO-INFER-AI/src/geo_infer_ai/utils/__init__.py:5`
- Bounded next step: add GEO-INFER-AI/tests/unit/test_rng.py pinning the None->DEFAULT_SEED determinism (the deliberate difference), Generator pass-through (is identity), RandomState bridge; delete resolve_optional_rng or give it a caller
- Acceptance probe: `uv run --no-sync pytest GEO-INFER-AI/tests/unit/test_rng.py -q green; grep confirms no uncalled export remains`
- Effort: S

**GS-260 · MINOR · AGENT · api: AgentInterface.start_agent schedules a fire-and-forget task it never retains** · [probe-confirmed]
- Motivation: asyncio.create_task(agent_registry.start_agent(agent_id)) discards the task reference; CPython keeps only a weak reference, so the coroutine can be GC'd before completion and any exception inside it is unobservable while the facade has already returned True. The start path is also untested (no start_agent tests in test_interface_and_cli.py; test_api_endpoints.py never POSTs /agents/{id}/start).
- Evidence: `src/geo_infer_agent/api/interface.py:105` · `core/agent_registry.py:~230` · `api/agent_endpoints.py:138-155` · `tests/unit/test_api_endpoints.py:97`
- Bounded next step: Hold the task in a module-level pending set with a done-callback that logs exceptions and discards itself (or delegate to registry.agent_tasks); add one interface-level test that starts a real agent and asserts registry.is_agent_running becomes True within a tick.
- Acceptance probe: `uv run --no-sync python -c "import asyncio; from geo_infer_agent.api.interface import AgentInterface; from geo_infer_agent.api.agent_endpoints import agent_registry as r; i=AgentInterface(); aid=asyncio.run(i.create_agent('default',{},agent_id='s1')); ok=asyncio.run(i.start_agent(aid)); asyncio.run(asyncio.sleep(0.2)); print(ok, r.is_agent_running('s1'))" → must print True True; plus a test that a start failure surfaces in logs rather than vanishing.`
- Effort: S — Note: Live probe was run and observed: start_agent('ghost') returned False via is_agent_running KeyError — missing-ID contract fine; defect is the retained-task/exception hole on the success path.

**GS-261 · MINOR · AGENT · docs: README dependency list drifted from pyproject (psutil/pytest listed as core deps)**
- Motivation: README.md lists psutil and pytest packages as module dependencies, but pyproject puts psutil in the optional integrations extra (guarded import) and pytest packages are dev-only. The stale list is copy-pasted into every subpackage README.
- Evidence: `README.md:42-45` · `pyproject.toml:40-44` · `api/telemetry.py:593-597` · `pyproject.toml:45-52` · `core/README.md:47` · `api/README.md:54` · `models/README.md:51`
- Bounded next step: Regenerate the Dependencies sections from pyproject (core deps vs extras vs dev); keep one canonical source.
- Acceptance probe: `grep READMEs for 'psutil>' returns only extras/dev sections; uv run --no-sync python -c "import geo_infer_agent" still succeeds without psutil installed.`
- Effort: S

**GS-262 · MINOR · AGENT · cli+core: agent-type→class mapping hand-duplicated in three places**
- Motivation: cli.py AGENT_MODULES, AgentRegistry.agent_types, and list_agents_command descriptions are three manually-synced copies of the same 7-entry registry; adding an agent type requires touching all three or CLI/registry/description drift.
- Evidence: `cli.py:81-90` · `core/agent_registry.py:68-77` · `cli.py:~200`
- Bounded next step: Make cli import the registry's map (registry as single source; CLI derives class paths and descriptions from it, or move both to one constants module both import).
- Acceptance probe: `grep shows a single definition of the type→path mapping; a new test asserts set(AGENT_MODULES) == set(AgentRegistry().agent_types) == set(list_agents_command keys).`
- Effort: S

**GS-263 · MINOR · AGENT · core: llm_proxy exported but unused; its rate_limit policy field is dead config**
- Motivation: llm_proxy is re-exported public API but has no consumer besides __init__ re-exports and its tests. Inside it, LLMProxyPolicy.rate_limit_per_minute is never read — enforce_llm_proxy_policy rate-limits only when the caller separately constructs and passes a TokenBucket, so the declared policy field silently does nothing.
- Evidence: `core/llm_proxy.py:12-13` · `core/llm_proxy.py:36-37` · `core/llm_proxy.py:160-166`
- Bounded next step: Either wire the field (enforce_llm_proxy_policy builds a TokenBucket from policy.rate_limit_per_minute when client_id is given) or delete the field; mark the module clearly as a library-for-future-use or move it behind a clearly-labeled seam.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_agent import LLMProxyPolicy, enforce_llm_proxy_policy; enforce_llm_proxy_policy(LLMProxyPolicy(rate_limit_per_minute=1), model='m', client_id='c')" run twice — second call must raise LLMProxyPolicyError once the field is wired (today it silently passes).`
- Effort: S

**GS-265 · MINOR · AI · core/training: MAPE denominator (y_test + 1e-10) wrong for negative targets**
- Motivation: MAPE divides by (y_test + 1e-10) instead of |y_true|; for negative targets (temperature anomalies, profit/loss) the epsilon-shifted raw denominator produces sign-flipped, inflated nonsense, and values near -1e-10 still explode.
- Evidence: `src/geo_infer_ai/core/training.py:249-251`
- Bounded next step: Use np.abs(y_test) as denominator with an explicit zero-guard; document behavior at y==0.
- Acceptance probe: `evaluate_model on a regression fixture with a negative target; assert mape equals the numpy-computed reference value.`
- Effort: S

**GS-266 · MINOR · AI · core/training: best_score/best_model shared mutable state across task types and calls**
- Motivation: A single best_score=-inf accumulator is compared against accuracy by train_classifier and R² by train_regressor — a trainer used for both task types keeps whichever model scored higher on an incomparable scale. best_model/best_score are never reset between fits (no public reset), so a second, worse run still reports stale best.
- Evidence: `src/geo_infer_ai/core/training.py:87-88` · `src/geo_infer_ai/core/training.py:132-134` · `src/geo_infer_ai/core/training.py:192-194`
- Bounded next step: Track best per task_type (dict keyed by task type) or reset at the start of each train_* call; document which in SKILL.md.
- Acceptance probe: `Train a classifier then a regressor on the same trainer; assert best_model is the regressor after the regressor call (currently it can keep the classifier).`
- Effort: S

**GS-267 · MINOR · AI · core/explainability: compute_shap_like_values makes >10k predict calls at defaults**
- Motivation: For each feature j and observation i it builds a perturbed matrix and calls predict individually: with defaults n_obs=100, n_features=10, bg_size=100 that is >11,000 separate sklearn predict invocations. Fully batchable: per feature, one predict over the stacked perturbed matrix.
- Evidence: `src/geo_infer_ai/core/explainability.py:~130-160`
- Bounded next step: Hoist pred_original out of the loop (one predict on all X) and batch the per-feature perturbation into a single predict per feature (n_features+1 predicts total); results identical, runtime ~1000x lower on the inner loop.
- Acceptance probe: `Time compute_shap_like_values before/after on a 100×10 fixture; assert shap_values allclose to the pre-refactor values on a fixed seed.`
- Effort: S

**GS-268 · MINOR · ANT · __init__: setup_ant_module configures an IntegrationManager and then throws it away**
- Motivation: setup_ant_module builds an IntegrationManager and calls setup_integrations with the instance never returned or stored; the entire call has no observable effect beyond logging. The manager has no other consumer in the repo and zero test references.
- Evidence: `src/geo_infer_ant/__init__.py:99-103` · `utils/integration.py:10-28`
- Bounded next step: Either return the configured manager from setup_ant_module (and add a round-trip test) or delete the IntegrationManager plumbing until a real consumer exists.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_ant import setup_ant_module; print(setup_ant_module())" — after the fix the returned/derived manager must expose is_enabled() for the supplied config.`
- Effort: S

**GS-269 · MINOR · ANT · __init__: get_available_components hand-maintains a second copy of the export surface**
- Motivation: get_available_components duplicates the exact class/function names already listed in __all__; any export added to one and not the other drifts (e.g. a new analysis class appears in __all__ but get_available_components omits it).
- Evidence: `src/geo_infer_ant/__init__.py:112-134` · `src/geo_infer_ant/__init__.py:~136-166`
- Bounded next step: Derive the components dict by introspecting the package namespace against __all__ (or a single name→component map both consume).
- Acceptance probe: `A test asserting every name in __all__ (minus metadata) appears in get_available_components() output and vice versa.`
- Effort: S

**GS-271 · MINOR · ART · generation: simplex_noise is a silent alias of perlin_noise, not a distinct algorithm**
- Motivation: _generate_simplex_noise body is a comment plus a call to _generate_perlin_noise, while SKILL.md advertises 23 procedural algorithms with simplex_noise listed as distinct. The all-algorithms test only checks a 200×200 image is produced, so identical Perlin output passes — a doc-truth/stub in a shipped public surface.
- Evidence: `src/geo_infer_art/core/generation/procedural_art.py:1099-1105` · `src/geo_infer_art/core/generation/procedural_art.py:32-55` · `tests/unit/test_procedural_art.py:168-197`
- Bounded next step: Implement true simplex noise (~40 lines of numpy, open-simplex or classic Perlin-derived simplex with different gradient lattice) or, minimally, have _generate_simplex_noise apply a distinct gradient/rotation so outputs differ, and note the approximation in the class docstring.
- Acceptance probe: `Generate noise_field vs simplex_noise with the same seed and assert the pixel arrays differ (uv run --no-sync python -c comparing np.asarray(art.image)).`
- Effort: M

**GS-272 · MINOR · ART · place: from_place_name promises ValueError but silently fabricates random coordinates**
- Motivation: PlaceArt.from_place_name docstring says it raises ValueError for unknown places, but the actual behavior falls through to random.uniform(-80,80)/(-179,179) with country Unknown for any unrecognized name. Sibling CulturalMap.from_region raises for unknown regions — inconsistent contracts for the same user question. The only from_place_name test mocks _fetch_place_data, so the fallback path is untested.
- Evidence: `src/geo_infer_art/core/place/place_art.py:170-171` · `src/geo_infer_art/core/place/place_art.py:87-95` · `src/geo_infer_art/core/place/cultural_map.py:240-244` · `tests/unit/test_place_art.py:73-78`
- Bounded next step: Pick one contract — raise ValueError on unknown places (matching CulturalMap and the docstring) or document the synthetic-coordinates fallback — and add a test for the unknown-place path (deterministic coordinates for a fixed name).
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_art.core.place.place_art import PlaceArt; print(PlaceArt._fetch_place_data('atlantis'))" — must either raise or return documented deterministic metadata; test asserts the chosen behavior.`
- Effort: S

**GS-273 · MINOR · ART · packaging: models/ and api/ subpackages are empty 0-byte shells shipped in the wheel**
- Motivation: geo_infer_art/models/__init__.py and geo_infer_art/api/__init__.py are 0 bytes, each carrying README.md/AGENTS.md scaffolding; setuptools.packages.find includes them, so installers get two dead packages (pure scaffolding with no docstring even).
- Evidence: `src/geo_infer_art/models/__init__.py (0 bytes)` · `src/geo_infer_art/api/__init__.py (0 bytes)`
- Bounded next step: Delete both directories (and their doc trees) or populate them with the surfaces their READMEs claim.
- Acceptance probe: `After removal, uv run --no-sync python -c "import geo_infer_art" and the wheel file list (uv build --sdist && tar -tf) no longer contain geo_infer_art/{models,api}.`
- Effort: S

**GS-276 · MINOR · SIM · packaging: io/, analysis/, models/, utils/ subpackages are docstring-only dead shells**
- Motivation: io/__init__.py, analysis/__init__.py, models/__init__.py, utils/__init__.py each contain a single docstring line and zero symbols; repo-wide grep finds no importer or reference. They ship in the wheel and imply functionality that does not exist.
- Evidence: `src/geo_infer_sim/io/__init__.py` · `src/geo_infer_sim/analysis/__init__.py` · `src/geo_infer_sim/models/__init__.py` · `src/geo_infer_sim/utils/__init__.py`
- Bounded next step: Delete the four subpackages (or move the promised utilities in — e.g. the JSON coercion in core/mesa_bridge.py:370-373 is a natural utils resident).
- Acceptance probe: `Grep confirms zero references; uv run python -m pytest GEO-INFER-SIM/tests/ -q green after removal.`
- Effort: S

**GS-278 · MINOR · IOT · performance: hard-coded 0.0 latency/queue metrics drive a never-firing threshold**
- Motivation: Placeholder metrics (processing_latency_ms = 0.0, queue_size = 0 with would-need-to-track comments) feed _check_performance_thresholds, so the latency_threshold check can never fire — permanently dead threshold contradicting the repo's no-placeholder rule. Additionally the surrounding code computes a rate then no-ops (re-assigns the same object into history) and silently drops the computed rate when history is empty.
- Evidence: `src/geo_infer_iot/performance/__init__.py:217-219` · `src/geo_infer_iot/performance/__init__.py:223` · `src/geo_infer_iot/performance/__init__.py:~60` · `src/geo_infer_iot/performance/__init__.py:~247-249`
- Bounded next step: Instrument real ingest latency (timestamp at IoTDataIngestion.ingest_measurement entry/exit, surfaced via the IoTSystem) or delete processing_latency_ms/queue_size and their thresholds; simplify the write-back to a single guarded append.
- Acceptance probe: `Unit test feeds a monitor an iot_system stub with measured timestamps and asserts processing_latency_ms > 0; threshold test asserts the latency warning can fire.`
- Effort: M

**GS-279 · MINOR · IOT · core/ingestion: ingest_measurement converts every exception to bare False, hiding bugs**
- Motivation: The whole ingestion path is wrapped in except Exception: logger.error(...); return False. A programming error inside spatial indexing or QC (TypeError, AttributeError) is indistinguishable from an invalid measurement; callers checking the bool cannot distinguish bad data from a broken install, and the log line omits exc_info.
- Evidence: `src/geo_infer_iot/core/ingestion.py:224-226`
- Bounded next step: Narrow the catch to expected input errors (ValueError/KeyError/TypeError from parsing) and log unexpected exceptions with logger.exception, or store self.last_ingest_error for callers.
- Acceptance probe: `Ingest a measurement with a malformed value; assert False; then trigger an internal bug (monkeypatched QC raising RuntimeError) and assert it propagates or is distinguishable.`
- Effort: S

**GS-280 · MINOR · REQ · core: compute_priority_scores trusts caller weights unvalidated; custom path untested**
- Motivation: compute_priority_scores accepts an arbitrary weights dict; a missing key raises a bare KeyError from inside the scoring loop, and negative or non-summing weights silently distort all scores. Tests only exercise the default path — no custom-weights test exists.
- Evidence: `src/geo_infer_req/core/requirements.py:207-213` · `src/geo_infer_req/core/requirements.py:257-261` · `tests/unit/test_requirements.py:96-100` · `tests/unit/test_acceptance_req.py:98-121`
- Bounded next step: Validate required keys present and values finite/non-negative (normalize to sum 1 or document otherwise); add a custom-weights test including the missing-key error.
- Acceptance probe: `uv run --no-sync python -c "from geo_infer_req import RequirementsAnalyzer, Requirement, RequirementType; a=RequirementsAnalyzer(); a.add_requirement(Requirement('R1','t','desc here',RequirementType.FUNCTIONAL)); print(a.compute_priority_scores(weights={'priority':1.0}))" — must raise a clear ValueError today-KeyError after fix.`
- Effort: S

**GS-281 · MINOR · REQ · core: build_dependency_graph silently drops dependencies on unknown req IDs**
- Motivation: Edges are added only if dep_id is a known requirement; dangling dependencies vanish from nodes/edges/topological_order/critical_path and are only discoverable later via check_completeness().orphaned_requirements. A caller reading just the graph gets a silently-wrong critical path when a dependency ID was mistyped.
- Evidence: `src/geo_infer_req/core/requirements.py:160-165` · `tests/unit/test_requirements.py:150-190`
- Bounded next step: Log a warning per dropped dependency, or surface dangling IDs as a DependencyGraph field (breaking change → keep field additive).
- Acceptance probe: `Build a graph with one dangling dependency; assert a warning is logged (caplog) or the new field lists it; existing cycle tests stay green.`
- Effort: S

### Platform docs & test (TEST · INTRA · EXAMPLES)

**GS-040 · MINOR · INTRA · modules/*.md 'Module README' links resolve one dir level short; link-gate blind**
- Motivation: From GEO-INFER-INTRA/docs/modules/, ../../GEO-INFER-*/README.md resolves to GEO-INFER-INTRA/GEO-INFER-*/README.md - nonexistent (glob-verified 'Skipped missing paths: GEO-INFER-INTRA/GEO-INFER-INSURANCE'); correct depth is ../../../ (modules/index.md itself uses ../../../ correctly). Two defect layers: (1) geo-infer-insurance.md:88 carries it as a real markdown link any link checker would flag; (2) the other ~45 pages carry it as bare text, which validate_documentation.py's '](...'-based _iter_links never parses - and the module pages are not in AUTHORITATIVE_DOCS anyway. So ~46 pages ship a dead first-link while the strict gate reports green.
- Evidence: `GEO-INFER-INTRA/docs/modules/geo-infer-insurance.md:88` · `GEO-INFER-INTRA/docs/modules/geo-infer-water.md:13` · `GEO-INFER-INTRA/docs/modules/geo-infer-transport.md` · `GEO-INFER-TEST/validate_documentation.py:61-89` · `GEO-INFER-INTRA/docs/modules/index.md`
- Bounded next step: one scripted pass converting the bare references to [Module README](../../../GEO-INFER-*/README.md) across GEO-INFER-INTRA/docs/modules/*.md (matching index.md's working depth), then add the per-module pages (or a targeted link-check list) to AUTHORITATIVE_DOCS so the class cannot regress
- Acceptance probe: `uv run python GEO-INFER-TEST/validate_documentation.py --strict (with the page list extended) exits 0; grep confirms zero ](../../GEO-INFER- occurrences under docs/modules/`
- Effort: M — Note: Report graded effort S/M; recorded M as scripted pass over ~46 pages plus gate config. Link-break verified by glob ('Skipped missing paths'), not by an executed probe

**GS-042 · MINOR · INTRA · UV_MIGRATION_GUIDE.md stale: 36-module count, 'setup.py Removed', bare python3 calls**
- Motivation: GEO-INFER-INTRA/docs/guides/UV_MIGRATION_GUIDE.md says 'All 36 GEO-INFER modules have been migrated' and shows the standard module structure with 'setup.py # Removed (deprecated)'. Current tree: 45 GEO-INFER-* module dirs (glob-verified, matches README's 45) and 40 setup.py files present (incl. ACT, BAYES, SPACE, API). ISA.md states the correct contract: 'pyproject.toml is the canonical packaging surface; setup.py is compatibility only. Existing setup.py files parse successfully.' The guide also invokes migration scripts as bare 'python3 GEO-INFER-INTRA/scripts/migrate_to_uv.py', contradicting the repo's uv-only command surface (AGENTS.md Standard Commands all use 'uv run python'); scripts themselves exist (migrate_to_uv.py, cleanup_pyproject_deps.py, validate_uv_setup.py all glob-verified).
- Evidence: `GEO-INFER-INTRA/docs/guides/UV_MIGRATION_GUIDE.md` · `GEO-INFER-INTRA/ISA.md` · `GEO-INFER-INTRA/AGENTS.md` · `GEO-INFER-INTRA/scripts/migrate_to_uv.py`
- Bounded next step: update the guide to '45 modules (see README.md)' or replace the hard-coded count with a pointer, restate setup.py as 'compatibility shim retained; pyproject.toml canonical' per ISA.md, and switch command examples to 'uv run python ...'
- Acceptance probe: `grep the guide for '36' returns no module-count claim; the 40 setup.py files no longer contradict the text; no bare 'python3 ' invocations remain in the guide`
- Effort: S

**GS-062 · MINOR · TEST · root manuscript tests escape the no-skip contract via importorskip + validator blind spot**
- Motivation: The repo's own source-level contract forbids importorskip/skip/xfail outright and the runtime policy converts skipped reports to failures, but test_files() only globs GEO-INFER-*/tests/**/*.py so the root manuscript suite escapes the scan. test_manuscript_figures.py uses importorskip for matplotlib and yaml, both hard root dependencies, so both guards are dead weight and would violate the no-skip contract at runtime if they ever fired.
- Evidence: `GEO-INFER-TEST/validate_test_contracts.py:16-25` · `GEO-INFER-TEST/conftest.py:104-111` · `GEO-INFER-TEST/conftest.py:114-120` · `GEO-INFER-TEST/validate_test_contracts.py:30` · `tests/test_manuscript_figures.py:17` · `tests/test_manuscript_figures.py:146` · `pyproject.toml:55` · `pyproject.toml:51`
- Bounded next step: replace both importorskip calls with plain imports (an ImportError then fails loudly, which is the contract); extend validate_test_contracts.test_files() (validate_test_contracts.py:28-30) to also scan ROOT/tests/**/*.py so the blind spot closes for root manuscript tests
- Acceptance probe: `before fix uv run --no-sync python GEO-INFER-TEST/validate_test_contracts.py --strict (with the glob fix) reports tests/test_manuscript_figures.py lines 17/146; after fix it passes and uv run --no-sync pytest tests/test_manuscript_figures.py -q stays green`
- Effort: S

**GS-063 · MINOR · TEST · dead self-tests in test_ecosystem_health.py duplicate the validator**
- Motivation: test_test_functions_follow_naming walks the AST and then pass-es on every finding, so it can never fail and only burns 45xN AST parses per CI run. Its sibling asserts a 0.3 docstring floor directly under a comment allowing up to 50% missing (code permits 70%), duplicating the real strict gate in validate_test_contracts.py run in CI.
- Evidence: `GEO-INFER-TEST/tests/integration/test_ecosystem_health.py:146-162` · `GEO-INFER-TEST/tests/integration/test_ecosystem_health.py:140` · `GEO-INFER-TEST/tests/integration/test_ecosystem_health.py:136` · `GEO-INFER-TEST/validate_test_contracts.py:100-113`
- Bounded next step: delete the no-op naming test and the 0.3 docstring gate (and the AST-parse duplicate) from TestTestFileQuality, keeping only the directory-structure checks that validate_test_contracts does not own (module dir/README/tests-dir existence)
- Acceptance probe: `uv run --no-sync pytest GEO-INFER-TEST/tests/integration/test_ecosystem_health.py -q green after deletion; uv run --no-sync python GEO-INFER-TEST/validate_test_contracts.py --strict still passes (contracts remain enforced)`
- Effort: S

**GS-292 · MINOR · TEST · validate_skills.py advertises an unimplemented --fix-names flag**
- Motivation: Usage docstring documents 'python GEO-INFER-TEST/validate_skills.py --fix-names', but main()'s parser defines only --verbose and --check-xrefs; no other flag exists anywhere in the 306-line file. Passing the advertised flag exits 2 with an argparse error - a doc-truth bug in the validator that polices doc truth.
- Evidence: `GEO-INFER-TEST/validate_skills.py:17` · `GEO-INFER-TEST/validate_skills.py:242-246` · `GEO-INFER-TEST/validate_skills.py:248-251`
- Bounded next step: Either implement --fix-names (rewrite name: in each SKILL.md frontmatter to geo-infer-<module>) or delete the line from the docstring; add one unit test asserting the CLI parses the documented flags.
- Acceptance probe: `uv run --no-sync python GEO-INFER-TEST/validate_skills.py --fix-names currently exits 2 ('unrecognized arguments'); after the fix it should exit 0 or 1 (validation result), never 2`
- Effort: S

**GS-293 · MINOR · TEST · check_coverage_floor.py crashes (KeyError) on renamed/deleted modules in a diff**
- Motivation: check_coverage_floor.py:101 does floor = entries[module]['floor_percent'] for every module derived from git diff --name-only (lines 34-52); the completeness check at lines 88-93 validates all_modules - entries against the current tree only, so a diff that deletes or renames GEO-INFER-X yields a module key absent from coverage_baseline.json and no tests dir in the current tree -> unhandled KeyError traceback instead of the gate's clean _fail output. The gate is CI-critical (ci.yml:154).
- Evidence: `GEO-INFER-TEST/check_coverage_floor.py:101` · `GEO-INFER-TEST/check_coverage_floor.py:34-52` · `GEO-INFER-TEST/check_coverage_floor.py:88-93` · `.github/workflows/ci.yml:154`
- Bounded next step: Intersect the diff-derived module set with the entries keys and _fail with a named-module message for any leftover.
- Acceptance probe: `run check_coverage_floor.py --base <sha-before-rename> --head <rename-sha> on a scratch clone; expect clean failure message, not traceback`
- Effort: S

**GS-294 · MINOR · TEST · TEST/docs/index.md broken code fences; gate docs drift from CI**
- Motivation: GEO-INFER-TEST/docs/index.md opens its install block with a single backtick (backtick-bash at line 9) and the Programmatic API block with backtick-python - both render as prose, not code. Separately, TESTING.md:15-27 canonical gate omits validate_packaging.py --strict and validate_active_inference_contract.py, which CI runs, and TESTING.md:16 uses bare 'python -m compileall' despite TESTING.md:29 mandating 'uv run' (same bare-python line in root AGENTS.md:34 and README.md:42).
- Evidence: `GEO-INFER-TEST/docs/index.md:9` · `TESTING.md:15-27` · `.github/workflows/ci.yml:100,104` · `TESTING.md:16` · `TESTING.md:29` · `AGENTS.md:34` · `GEO-INFER-TEST/README.md:42`
- Bounded next step: Fix the two fences; reconcile the canonical-gate lists with ci.yml; wrap compileall in 'uv run'.
- Acceptance probe: `fence-balance count per file equals an even number of triple-backtick lines that all start a block; the set of GEO-INFER-TEST validator commands in AGENTS.md vs ci.yml shrinks to intentional-only diffs`
- Effort: S

**GS-295 · MINOR · TEST · SKILL.md test commands bypass canonical runner (INTRA + EXAMPLES)**
- Motivation: GEO-INFER-INTRA/SKILL.md:81 and GEO-INFER-EXAMPLES/SKILL.md:94 instruct 'uv run python -m pytest <module>/tests/ -v', while both modules' READMEs define the canonical entry as 'uv run python GEO-INFER-TEST/run_unified_tests.py --module <NAME>'. Direct pytest skips the runner's zero-skip/exit-code-5 JUnit contract enforcement (run_unified_tests.py:103-110, junit_contract_errors).
- Evidence: `GEO-INFER-INTRA/SKILL.md:81` · `GEO-INFER-EXAMPLES/SKILL.md:94` · `GEO-INFER-TEST/run_unified_tests.py:103-110`
- Bounded next step: Point both SKILL.md Guidelines at the canonical module command.
- Acceptance probe: `grep SKILL.md files for '-m pytest GEO-INFER-' returns 0 hits`
- Effort: S

**GS-296 · MINOR · INTRA · SKILL.md flagship example calls nonexistent H3Backend.tessellate**
- Motivation: GEO-INFER-INTRA/SKILL.md:44 and :62 use H3Backend().tessellate(region, resolution=7) in the module's two headline cross-module snippets; 'tessellate' has zero hits in all of GEO-INFER-SPACE/src (the backend's real entry is polygon_to_cells at backends/h3/h3_backend.py:190, or create_h3_grid_for_bounds at backends/h3/operations.py:156). The EXAMPLES module already fixed the same drift in its SPACE orchestrator (uses polygon_to_cells). The other SKILL.md example symbols (H3Backend, MoranI, SpatialGP, FileConnector + read_geospatial) all verified real.
- Evidence: `GEO-INFER-INTRA/SKILL.md:44` · `GEO-INFER-INTRA/SKILL.md:62` · `GEO-INFER-SPACE/src/geo_infer_space/backends/h3/h3_backend.py:190` · `GEO-INFER-SPACE/src/geo_infer_space/backends/h3/operations.py:156` · `GEO-INFER-EXAMPLES/examples/module_orchestrators/SPACE/scripts/run_orchestrator.py`
- Bounded next step: Replace both 'tessellate' lines with polygon_to_cells(region, resolution=7) (returns the cell list the example indexes).
- Acceptance probe: `grep -rn 'tessellate' GEO-INFER-INTRA/ returns 0 hits; grep -c 'def tessellate' GEO-INFER-SPACE/src is 0 both before and after (doc-side fix)`
- Effort: S

**GS-297 · MINOR · INTRA · SKILL.md overclaims API-reference scope that docs/api/reference.md disclaims**
- Motivation: SKILL.md:21 'Consolidated API documentation for all 45 modules' and :32 'api/ # Consolidated API reference' vs docs/api/reference.md:1-6: 'This repository contains several package APIs rather than one universal service reference' - the page is a navigation policy stub, not a reference.
- Evidence: `GEO-INFER-INTRA/SKILL.md:21` · `GEO-INFER-INTRA/SKILL.md:32` · `GEO-INFER-INTRA/docs/api/reference.md:1-6`
- Bounded next step: Reword SKILL.md to 'API navigation and per-module reference index' matching the stub.
- Acceptance probe: `grep INTRA for 'Consolidated API documentation for all' returns 0 hits`
- Effort: S

**GS-298 · MINOR · INTRA · Legacy INTRA/tests/run_tests.py duplicates canonical runner, violates policy**
- Motivation: GEO-INFER-INTRA/tests/run_tests.py:71 shells out via bare 'python3 -m pytest' (repo rule: uv-only toolchain; TESTING.md:29 mandates 'uv run'); :61 advertises --skip-slow which injects a marker expression that would silently skip tests - forbidden by the strict contract (TESTING.md:21-22, zero-skip policy); :111 applies --cov=. with no source filter; and it duplicates run_unified_tests.py --module INTRA which INTRA/README.md and CI define as canonical. It is a dead parallel surface.
- Evidence: `GEO-INFER-INTRA/tests/run_tests.py:71` · `GEO-INFER-INTRA/tests/run_tests.py:61` · `GEO-INFER-INTRA/tests/run_tests.py:111` · `TESTING.md:21-22` · `TESTING.md:29`
- Bounded next step: Delete tests/run_tests.py (no references outside the module); keep SKILL/README pointing at the unified runner (also fixes INTRA SKILL.md:81).
- Acceptance probe: `grep -rn 'run_tests.py' AGENTS.md GEO-INFER-INTRA/README.md .github/workflows returns no live references after removal`
- Effort: S

**GS-299 · MINOR · INTRA · visual-preview symbols re-exported through three parallel surfaces**
- Motivation: The six preview symbols are defined once (src/geo_infer_intra/core/documentation/visual_preview.py) but surfaced three ways: package __init__.py imports straight from core.documentation, utils/visual_preview.py is a whole-file re-export shim, and utils/__init__.py:12-15 imports from that shim; tests import both the top level (tests/integration/test_module_integration.py:14) and geo_infer_intra.utils.config. The utils shim is weightless indirection with two maintenance surfaces.
- Evidence: `GEO-INFER-INTRA/src/geo_infer_intra/core/documentation/visual_preview.py` · `GEO-INFER-INTRA/src/geo_infer_intra/__init__.py:3-10` · `GEO-INFER-INTRA/src/geo_infer_intra/utils/visual_preview.py` · `GEO-INFER-INTRA/src/geo_infer_intra/utils/__init__.py:12-15` · `GEO-INFER-INTRA/tests/integration/test_module_integration.py:14`
- Bounded next step: Delete utils/visual_preview.py and repoint utils/__init__.py:12 to core.documentation.visual_preview; keep the package-level re-export (tests and examples target it).
- Acceptance probe: `grep -rn 'geo_infer_intra.utils.visual_preview' GEO-INFER-INTRA/src GEO-INFER-INTRA/tests returns 0 hits after collapse; uv run python GEO-INFER-TEST/run_unified_tests.py --module INTRA stays green`
- Effort: S

**GS-302 · MINOR · EXAMPLES · Root run_orchestrator.py is misnamed relative to what it runs**
- Motivation: GEO-INFER-EXAMPLES/run_orchestrator.py imports main from core.module_orchestrator (defined at module_orchestrator.py:1089 as 'Demonstration entrypoint: report health and run the first workflow') - it does not drive the 43 examples/module_orchestrators/*/scripts/run_orchestrator.py demos its sibling name implies, and it instantiates ModuleOrchestrator() with monitoring_enabled=True (the SKILL examples deliberately pass False). A user running it gets an arbitrary first-workflow demo, not example orchestration.
- Evidence: `GEO-INFER-EXAMPLES/run_orchestrator.py` · `GEO-INFER-EXAMPLES/examples/module_orchestrators/module_orchestrator.py:1089`
- Bounded next step: Either rename the root file (e.g. demo_orchestrator.py) or rewire it to dispatch the per-module orchestrators through _lib.run_module_orchestrator with a module argument; update README.md Contents list accordingly.
- Acceptance probe: `after change, uv run python GEO-INFER-EXAMPLES/run_orchestrator.py SPACE (if rewired) exits 0 and prints the SPACE JSON payload`
- Effort: S

**GS-303 · MINOR · EXAMPLES · EXAMPLES SKILL.md test command bypasses the canonical runner**
- Motivation: SKILL.md:94 ('uv run python -m pytest GEO-INFER-EXAMPLES/tests/ -v') vs canonical run_unified_tests.py --module EXAMPLES (README Module Metadata); see TEST-lane item 6 (GS-295) for the paired INTRA instance.
- Evidence: `GEO-INFER-EXAMPLES/SKILL.md:94`
- Bounded next step: Point SKILL.md:94 at the canonical command.
- Acceptance probe: `grep for '-m pytest GEO-INFER-EXAMPLES' in SKILL.md returns 0 hits`
- Effort: S

## Suggested execution order

Waves are themes, not gates; ids are the contract and the partition is exact (every item appears in exactly one wave). Order within a wave is flexible.

1. **Probe-confirmed bugs — fix and pin the live-observed failure as the regression test** (6): GS-166, GS-177, GS-185, GS-235, GS-245, GS-260
2. **Silent-fabrication family — wrong math, fabricated fallbacks, sentinels, ignored contracts; fix + pin** (82): GS-082, GS-083, GS-085, GS-100, GS-101, GS-105, GS-107, GS-108, GS-109, GS-110, GS-111, GS-112, GS-124, GS-125, GS-126, GS-131, GS-141, GS-143, GS-144, GS-145, GS-149, GS-150, GS-152, GS-153, GS-155, GS-160, GS-161, GS-162, GS-164, GS-165, GS-167, GS-168, GS-169, GS-171, GS-172, GS-173, GS-174, GS-176, GS-178, GS-179, GS-180, GS-181, GS-186, GS-187, GS-188, GS-190, GS-191, GS-192, GS-195, GS-197, GS-200, GS-202, GS-203, GS-204, GS-211, GS-215, GS-223, GS-225, GS-226, GS-228, GS-236, GS-237, GS-238, GS-241, GS-243, GS-247, GS-248, GS-249, GS-263, GS-264, GS-265, GS-266, GS-267, GS-271, GS-272, GS-274, GS-277, GS-278, GS-279, GS-280, GS-281, GS-293
3. **Packaging & manifest coherence — wheel correctness, metadata, declared-vs-used deps** (20): GS-020, GS-021, GS-022, GS-024, GS-025, GS-026, GS-104, GS-129, GS-142, GS-147, GS-148, GS-201, GS-210, GS-216, GS-221, GS-239, GS-246, GS-261, GS-273, GS-276
4. **Test gaps — bring behavior-defining surfaces under the canonical runner** (21): GS-060, GS-061, GS-062, GS-063, GS-064, GS-065, GS-121, GS-122, GS-123, GS-127, GS-146, GS-189, GS-212, GS-214, GS-218, GS-224, GS-227, GS-242, GS-251, GS-290, GS-301
5. **Docs & API honesty — every agent-facing claim resolves to real code** (29): GS-040, GS-041, GS-042, GS-080, GS-081, GS-088, GS-102, GS-128, GS-140, GS-154, GS-170, GS-175, GS-194, GS-196, GS-198, GS-199, GS-213, GS-222, GS-240, GS-250, GS-291, GS-292, GS-294, GS-295, GS-296, GS-297, GS-300, GS-302, GS-303
6. **CI gates & structural cleanup — wall-time, dead code, monoliths, duplication, hygiene** (31): GS-001, GS-002, GS-003, GS-004, GS-005, GS-006, GS-007, GS-008, GS-023, GS-066, GS-084, GS-086, GS-087, GS-103, GS-106, GS-120, GS-130, GS-151, GS-163, GS-193, GS-217, GS-219, GS-220, GS-244, GS-262, GS-268, GS-269, GS-270, GS-275, GS-298, GS-299

## Provenance and method

Read-only review with file:line evidence; lanes ran bounded `uv run --no-sync` probes only where a finding demanded runtime proof (the 6 probe-confirmed items). Lane yields: P1 8, P2 7, P3 3 of 6 findings (3 directed to owning lanes), P4 7 of 8 (COMMS evidence absorbed by M6), P5 9, M1 13, M2 12, M3 16, M4 22, M5 20, M6 19, M7 17, M8 22, M9 14. Cross-lane integration: GS-024 (srai) absorbs the GeoCore cross-confirm; GS-147 carries the Packaging cross-ref; GS-226/227/228 absorb TestEstate COMMS evidence; GS-291 absorbs the DocsTruth cookbook + validate_documentation blind spot; GS-102 and GS-296 cross-link the SPACE/INTRA phantom-API spread; GS-148 retains the cfgrib aspect (Packaging lane has no cfgrib item); GS-131 is extractor-verified on tree; GS-201 re-tiered MAJOR→MEDIUM per rubric.
