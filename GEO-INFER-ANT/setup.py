"""Setup shim for GEO-INFER-ANT.

Canonical packaging metadata lives in pyproject.toml; this shim exists only
so legacy tooling can invoke setuptools against the source tree directly.
"""

from setuptools import setup

setup()