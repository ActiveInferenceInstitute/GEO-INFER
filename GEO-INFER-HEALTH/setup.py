"""Setup shim for GEO-INFER-HEALTH.

All package metadata lives in pyproject.toml; this file exists only so
legacy tooling that expects setup.py keeps working.
"""

from setuptools import setup

setup()
