# GEO-INFER-ORG API schema — DESIGN ONLY
#
# GEO-INFER-ORG is a Python library. It does NOT expose a REST service:
# there is no HTTP server, router, or endpoint implementation in this
# module, and nothing here links to or advertises a deployed API.
#
# The canonical programmatic surface is the Python API:
#
#   from geo_infer_org import (
#       OrganizationModel, OrgUnit, Role, Resource,
#       OrgStructureType, RoleLevel, OrgMetrics,
#       VotingEngine, ConsensusModel, VotingMethod, DecisionStatus,
#       Vote, Proposal, VotingResult,
#       CollaborationNetwork, TeamFormation, CollaborationEdge,
#       CollaborationType, TeamMember, NetworkMetrics, TeamFormationResult,
#   )
#
# If a REST layer over these classes is ever implemented, replace this
# file with an OpenAPI spec generated from the real routes at that time.
